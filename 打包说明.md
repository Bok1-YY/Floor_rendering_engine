# Floor engine —— 打包成单一程序 + 源码防泄漏

> 2026-07-14 已按当前商业主线复核。上级 `test/PACKAGING.md` 是面向交付的完整打包/验收手册；本文件保留仓库内实现说明。不要误用上级旧原型的 `build.bat`、`floor_engine.spec`、`run_app.py`。

目标：把「Python FastAPI 后端 + Next.js 前端」两部分合成**一个 Windows 单文件 exe**，
双击即用（后端 + 前端同端口 7870，自动开浏览器），且**核心 Python 源码编译成机器码，无法直接反编译还原**。

方案：前端静态导出 → 由后端托管（一个进程一个端口）→ 用 **Nuitka** 把后端编译成原生二进制 `--onefile`。

> **U 盘交接推荐入口**：当前已生成独立的上级目录
> `test/Windows_Nuitka_Build_Kit/`。它内含生产源码、资源、已验证的 `web/out`、
> SHA-256 清单和根目录 `BUILD_FLOOR_ENGINE.bat`。整个文件夹拷到 Windows 后
> 双击 BAT 即可，正常打包不需要 Node.js。下文的 `build_windows.bat` 仍保留为
> 从开发仓库原地重建前端的开发者入口。

---

## 一、为什么是这套方案

| 诉求 | 做法 |
|---|---|
| 单一程序 | 前端 `next build` 出纯静态 `web/out`，FastAPI 挂在 `/` 托管；Nuitka `--onefile` 把后端 + 前端 + 资源打成 1 个 exe |
| 防源码泄漏（后端） | **Nuitka 把 .py 编译成 C → 原生机器码**，不是 PyInstaller 那种可被 `pyinstxtractor` 提取再反编译的 .pyc。这是目前 Python 防源码最强的一档 |
| 防源码泄漏（前端） | 前端是浏览器里跑的 JS，**天然对客户可见**，只能做到 `next build` 的压缩/混淆，无法真正“隐藏”。真正值钱的提示词逻辑全在后端，已被机器码保护 |

> ⚠️ **不能跨平台编译**：Nuitka 必须在**目标系统**上编译。你要 Windows 的 exe，就得在一台 **Windows** 机器上跑打包脚本。Linux 上编不出 Windows exe。

---

## 二、我已经改好的东西（在 Linux 开发机上完成）

1. `web/next.config.ts` → `output: 'export'`（静态导出）。
2. `web/src/lib/api.ts` → 生产环境走**同源相对地址**（客户从 localhost 或局域网 IP 打开都能连上后端）。
3. `web/.env.production` → 生产构建使用同源 API；`.env.development` 默认指向 :7870。
4. `web/out/` → **已构建好的前端静态站**（平台无关，可直接随文件夹拷到 Windows）。
5. `server_api.py` → 末尾把 `web/out` 挂到 `/`，兼容源码运行与冻结后路径。
6. `config.py` → 路径检测**同时识别 Nuitka**（原来只认 PyInstaller 的 `sys.frozen`），
   否则 `engine_config.json`（含 API key）、`output_files/` 会被写进临时目录、关程序就丢。
7. `serve.py` → 单一程序启动入口（uvicorn 单进程，纯 Python h11，启动自动开浏览器）。
8. `build_windows.bat` → Windows 一键打包脚本。

源码运行方式不变，仍可 `python serve.py`（或旧的 `python -m Floor_engine_server.server_api`）。

---

## 三、Windows 上出 exe 的步骤

### 前提
- Windows 装 64 位 **Python 3.11~3.12**（推荐 3.12，安装时勾选 *Add Python to PATH*）。当前 NumPy 已要求 Python ≥3.11。
- 首次编译需**联网**（Nuitka 自动下载 MinGW64 编译器，不用装 Visual Studio）。
- Windows 安装 Node.js 20.9+；脚本会按锁文件自动安装并构建前端。

### 操作
1. 把整个 `Floor_engine_server` 文件夹拷到 Windows（比如 `D:\Floor_engine_server`）。
2. 进文件夹，**双击 `build_windows.bat`**（或在此目录 cmd 里执行它）。
3. 等待：首次 5~15 分钟（下编译器 + 编译）。完成后产物在：
   ```
   dist\FloorEngine.exe
   ```

### 运行 / 交付给客户
- 把 `dist\FloorEngine.exe` 单个文件给客户即可。
- 双击 → 启动服务 → 自动打开浏览器到 `http://127.0.0.1:7870/`。
- 首次运行会在 **exe 同级目录**生成 `engine_config.json`（存 API key）、`output_files\`（出图）、`_ng_uploads\` 等。
  → 建议让客户把 exe 放在一个**有写权限的普通文件夹**（别放 `C:\Program Files`）。

---

## 四、如果前端要在 Windows 上重新构建（可选）

打包脚本会自动执行 `npm ci` 和生产构建；手工构建时执行：
```
cd Floor_engine_server\web
npm install
npx next build
```
生成新的 `web\out` 后再跑 `build_windows.bat`。

---

## 五、注意事项 / 常见坑

- **杀毒误报**：Nuitka/PyInstaller 打的单文件 exe 常被杀软误报（onefile 自解压特征）。
  给客户前可做代码签名证书签名，或让客户加白名单。
- **启动稍慢**：onefile 首次运行要把内含资源解压到缓存目录（脚本已设 `{CACHE_DIR}\FloorEngine\版本号`，
  同版本第二次启动就快了）。
- **端口/host**：默认且仅支持 `127.0.0.1:7870`。本程序没有远程认证，不允许绑定局域网或公网地址；不想自动开浏览器设 `FLOOR_NO_BROWSER=1`。
- **想隐藏控制台黑窗**：编译加 `--windows-console-mode=disable`（但就看不到日志、也没法 Ctrl+C 退出，
  只能任务管理器关；服务型程序一般保留控制台）。
- **更新版本**：改了 `build_windows.bat` 顶部 `VERSION`，缓存目录会自动分版本，不会和旧版打架。
- **前端源码保护有限**：这是 Web 架构的固有属性，非本方案缺陷。核心逻辑在后端已被机器码保护。
