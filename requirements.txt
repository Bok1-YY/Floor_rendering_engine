# floor_engine — 地板 AI 智能提示词引擎
# 安装: pip install -r requirements.txt
#
# 说明: 下面这些是代码直接用到的第三方库, 其余 os/json/re/asyncio/threading
#       等都是 Python 标准库, 无需安装。
#
# nicegui 已于 2026-06-29 随旧 webui.py 一并退役删除（见 DEVGUIDE.md §八 / 开发日志.md）。
# 它原本会间接带来 fastapi/uvicorn，现已显式直列于下；去掉 nicegui 后服务仍可独立跑。

# ── server_api.py(FastAPI 无头服务层)直接依赖 ──
fastapi==0.139.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32
Pillow==12.2.0
requests==2.34.2
numpy==2.4.6
opencv-python-headless==4.13.0.92
onnxruntime==1.27.0
tokenizers==0.22.1
deep-translator==1.11.4
urllib3==2.7.0
python-pptx==1.0.2
PyMuPDF==1.28.2
keyring==25.7.0
SecretStorage==3.3.3; sys_platform == "linux"
