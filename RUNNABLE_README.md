# Floor Engine 桌面可运行副本

这是从已推送提交 `433c16e2dab99c32c1f2b89c187f5b36481889a0` 创建的完全独立副本。它包含独立的 Python 环境、Node 依赖、模型资产和 2026-08-27 数据快照；没有 junction、symlink 或 Git object alternates，也不依赖原开发目录才能读取项目图片。

## 启动

双击桌面的 **Floor Engine - 可运行副本** 快捷方式，或直接双击本目录的 `start-windows.bat`。保留弹出的服务窗口；浏览器会自动打开 `http://127.0.0.1:7870/`。全屋设计入口是 `http://127.0.0.1:7870/design/`。

如果 7870 已经有本副本服务在运行，只需刷新浏览器，不要重复启动。停止服务时，在服务窗口按 `Ctrl+C` 或关闭该窗口。

## 本次真机发现并修复的问题

- 列表 API 为节省体积会省略 `plan_summary`，旧页面却直接读取它，导致 Edge 显示 “This page couldn’t load”。副本现在会先读取项目详情再渲染。
- 页面没有可滚动容器，底部按钮无法到达。副本现在拥有独立、受约束的纵向滚动区域。
- 两张草稿共用一个精修输入状态，左侧输入会同步到右侧。副本改为按 `candidate_id` 隔离状态。
- 两张 2K 候选图原本不能放大。副本加入全屏滚轮缩放、拖动和平移查看器。
- 数据 JSON 内含原开发目录绝对路径。副本已迁移 5 个文件、49 处路径，并验证 13 个项目文件引用均存在；迁移前文件保存在 `data/_runnable_path_migration_backup_20260827`。

## 验证结果

- 后端：19 个 pytest 全部通过。
- 前端：7 个 Node 合约测试全部通过。
- Next.js 生产构建通过，生成 7 个静态路由。
- HTTP：health、项目列表、主页和 `/design/` 均返回 200。
- Edge：真实点击验证了长页面滚动、两个候选独立输入、两张 2K 图片放大、新建入口以及历史项目恢复；没有触发任何 Gemini/FAL 付费调用。

完整机器可读记录见 `RUNNABLE_BASELINE.json`。

## 版本边界

用户选择的基线提交仍包含旧的 Pro 4K 精修流程。本副本只额外加入恢复可用性所必需的本地 hotfix，不包含后来未提交的 Goal0/人工锚点开发。代码 hotfix 是本地提交 `c01dd43874f1b3a703f91f6422fe5e15854c87df`，没有 push。

