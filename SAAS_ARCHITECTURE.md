# Floor AI 在线订阅版目标架构

> 状态：规划稿（不代表当前代码已经实现）
> 基线：当前单机版 FastAPI + Next.js + 本地文件存储
> 目标：面向地板行业的多租户网页订阅服务，支持多供应商生图、可靠排队、额度计费和持续扩容
> 更新日期：2026-07-15

---

## 1. 产品目标

在线版不只是把当前程序部署到公网，而是把它改造成一个可计费、可扩容、可恢复的多租户 SaaS。

目标能力：

1. 用户通过网页注册、订阅套餐并购买额外额度。
2. 每个用户、团队、项目的数据严格隔离。
3. 生图请求进入持久化队列，不因网页关闭、服务重启或单个 API 故障而丢失。
4. 同一种能力可以接入多个合规供应商和多个正式项目额度池。
5. 调度器根据模型能力、剩余额度、并发、延迟、成功率和成本选择线路。
6. 上游限流时自动等待、换线或降级，不让大量请求直接失败。
7. 订阅额度准确预扣、结算和退款，成本可追踪、可告警。
8. 图片与记录进入对象存储和数据库，可按项目长期管理。
9. 支持水平扩容、灰度发布、监控告警、备份和灾难恢复。

非目标：

- 不通过大量个人 Key 轮换来规避供应商限额。
- 不承诺所有模型永远即时出图；拥堵时应诚实显示排队位置和预计等待时间。
- 不在第一阶段拆成大量微服务。先保持模块化单体，只有任务执行器独立扩容。

> 多 API 的正确含义是“多供应商、多区域、多个经过供应商许可的组织项目或企业额度池”。路由和重试必须遵守各供应商服务条款、配额规则和数据政策。

---

## 2. 建议的整体架构

```text
                         ┌──────────────────────────────┐
                         │ DNS / CDN / WAF / DDoS 防护  │
                         └──────────────┬───────────────┘
                                        │ HTTPS
                         ┌──────────────▼───────────────┐
                         │ Next.js Web                  │
                         │ 登录·项目·生成·记录·订阅·后台 │
                         └──────────────┬───────────────┘
                                        │ REST / SSE
                  ┌─────────────────────▼─────────────────────┐
                  │ API Gateway / FastAPI 控制面              │
                  │ 鉴权·租户·项目·任务·额度·签名上传·审计     │
                  └───────┬─────────────┬──────────────┬──────┘
                          │             │              │
                 ┌────────▼──────┐ ┌────▼─────┐ ┌─────▼─────────┐
                 │ PostgreSQL    │ │ Redis    │ │ Object Storage│
                 │ 业务真源      │ │ 缓存/限流 │ │ 原图/结果/导出 │
                 └────────┬──────┘ └────┬─────┘ └─────┬─────────┘
                          │             │              │ CDN
                  ┌───────▼─────────────▼───────┐      │
                  │ 持久化任务队列 / 调度器      │      │
                  │ 优先级·公平调度·重试·超时    │      │
                  └──────────────┬───────────────┘      │
                                 │                      │
                    ┌────────────▼────────────┐         │
                    │ Image Worker 弹性池      │─────────┘
                    │ 提示词·上传·生图·后处理  │
                    └────────────┬────────────┘
                                 │ Provider Router
              ┌──────────────────┼──────────────────┐
              │                  │                  │
      ┌───────▼────────┐ ┌───────▼────────┐ ┌──────▼─────────┐
      │ Google Gemini  │ │ Fal / 聚合线路  │ │ 后续备用供应商 │
      │ 项目/区域池     │ │ 模型能力池      │ │ 合规适配器     │
      └────────────────┘ └────────────────┘ └────────────────┘

        ┌────────────────────────────────────────────────────┐
        │ 横切能力：支付·邮件·KMS·日志·指标·追踪·告警·审计   │
        └────────────────────────────────────────────────────┘
```

### 2.1 架构原则

- **PostgreSQL 是业务真源**：用户、订单、额度、项目、任务、评审都不能只放内存或 JSON。
- **对象存储保存二进制**：数据库只保存对象键、尺寸、哈希和元数据，不保存大图片 base64。
- **API 控制面不执行长任务**：创建任务后立即返回，Worker 独立执行，Web 进程可随时扩容或重启。
- **至少一次执行，业务幂等**：队列可能重复投递，依靠幂等键和状态机避免重复扣费、重复保存。
- **供应商适配层隔离差异**：业务代码只描述所需能力，不直接依赖某一家模型的请求格式。
- **先模块化单体，再按压力拆分**：早期减少运维复杂度，同时保留清晰边界。

---

## 3. 服务与模块边界

### 3.1 Web 前端

继续使用 Next.js，但在线版增加以下页面：

- 注册、登录、找回密码、邮箱验证。
- 控制台首页：剩余额度、排队情况、近期项目、系统公告。
- 项目与客户管理。
- 生成工作台、任务中心、历史记录、评审复盘。
- 套餐、账单、充值、发票信息。
- 团队成员、角色与邀请。
- API 状态页和故障通知。
- 管理后台：用户、套餐、订单、任务、供应商、成本和审计。

浏览器不得持有模型供应商 Key。上传大文件时，前端向后端申请短期签名 URL，直接上传对象存储，避免所有图片流量穿过 API 服务。

### 3.2 FastAPI 控制面

建议把当前 `server_api.py` 逐步拆成领域模块，但第一阶段仍可在一个部署单元中运行：

```text
app/
├── auth/          # 登录、会话、OAuth、密码与 MFA
├── tenants/       # 租户、团队、成员、角色
├── projects/      # 客户、项目、素材、交付版本
├── generation/    # 请求校验、提示词快照、任务状态机
├── providers/     # 供应商适配器、能力表、线路健康
├── billing/       # 套餐、额度账本、预扣、结算、退款
├── records/       # 结果、候选、评审、配方、导出
├── storage/       # 签名上传、对象键、生命周期管理
├── admin/         # 运营后台与人工补偿
└── observability/ # 审计、指标、追踪上下文
```

控制面职责：

- 校验用户、租户、套餐和参数。
- 创建不可变的生成快照。
- 预扣额度并写入任务表。
- 向队列提交轻量任务消息。
- 提供任务查询、取消、SSE 推送和结果签名地址。
- 处理支付回调和供应商回调。

控制面不得直接执行 4K 生图、图片压缩、校色或 PPTX 构建。

### 3.3 Worker 执行面

Worker 按能力分队列，避免轻任务被 4K 长任务堵住：

| 队列 | 任务 | 资源特点 |
|---|---|---|
| `preview` | 1K 快速预览 | 高优先级、短超时 |
| `generation` | B2/Pro/4K 生图 | 外部 API 等待为主 |
| `inpaint` | 生成式移除/添加候选 | 外部 API 等待、一次可能多候选 |
| `postprocess` | 磨缝、校色、格式转换 | CPU/内存较高 |
| `export` | HTML、PPTX、交付包 | 低优先级、可延迟 |
| `maintenance` | 缩略图、清理、统计聚合 | 后台低优先级 |

建议早期采用成熟的 Redis 队列实现；当跨步骤工作流、定时等待、人工审批和故障恢复复杂到难以维护时，再升级为 Temporal 一类持久工作流引擎。不要在第一版同时引入两套任务框架。

### 3.4 数据层

- **PostgreSQL**：业务数据、状态机、账本、配置元数据。
- **Redis**：短期缓存、分布式限流、并发租约、SSE 事件、队列；不能作为账单真源。
- **S3 兼容对象存储**：上传原图、生成结果、缩略图、导出文件。
- **CDN**：结果图和缩略图分发，私有文件使用短期签名 URL。
- **搜索**：第一阶段使用 PostgreSQL 全文和结构化索引；数据量确实增长后再引入 OpenSearch。

---

## 4. 多供应商 API 与配额调度

这是在线版最关键的新增模块。

### 4.1 Provider Adapter

每个供应商实现统一接口：

```python
class ImageProvider:
    def capabilities(self) -> ProviderCapabilities: ...
    async def submit(self, request: CanonicalImageRequest) -> ProviderJob: ...
    async def poll(self, provider_job_id: str) -> ProviderResult: ...
    async def cancel(self, provider_job_id: str) -> None: ...
    def classify_error(self, error: Exception) -> ProviderError: ...
```

统一请求描述：

- 工作流类型和模型能力。
- 输入图片对象键，不传本地路径。
- 分辨率、宽高比、图片数量。
- 是否要求图生图、多图输入、编辑、透明通道。
- 最大可接受等待时间、最高可接受成本。
- 数据驻留区域和内容安全要求。

统一错误分类：

- `RATE_LIMITED`：限流，可等待或换线。
- `CAPACITY`：供应商容量不足，可换线。
- `AUTH`：密钥或项目配置错误，立即熔断并告警。
- `INVALID_INPUT`：用户参数错误，不重试。
- `SAFETY`：内容策略拒绝，不自动换线绕过。
- `TRANSIENT_NETWORK`：网络暂态，可退避重试。
- `PROVIDER_INTERNAL`：供应商内部错误，可重试或换线。
- `UNKNOWN`：先有限重试，再进入人工检查。

### 4.2 能力池，而不是 Key 轮询

建立 `provider_endpoints` 能力表，每条线路记录：

- 供应商、账号/组织项目、区域、模型。
- 支持的分辨率、工作流和输入类型。
- 官方 RPM、并发数、日/月配额。
- 当前在途数、令牌桶余额、配额重置时间。
- 最近 5/15/60 分钟成功率、P95 延迟和错误分布。
- 单次预计成本、币种和结算规则。
- 启用状态、熔断状态、维护窗口。

Key 只作为某条正式线路的凭证，通过云 KMS/Secret Manager 加密保存。数据库不能保存明文 Key，日志、异常和追踪中也必须脱敏。

### 4.3 路由评分

符合能力和数据政策的线路才进入候选集，再计算评分：

```text
score =
  可用容量权重
  + 历史成功率权重
  + 延迟权重
  + 成本权重
  + 区域亲和权重
  - 近期错误惩罚
  - 接近配额上限惩罚
```

不同套餐可以使用不同策略：

- 免费/试用：低成本线路、低优先级、严格日限额。
- 标准版：成本和速度平衡。
- 专业版：更高优先级，可使用高质量模型。
- 企业版：保留并发、指定数据区域、可选专属供应商项目。

同一个任务切换供应商前必须检查能力等价性。不能把只支持 1K 的线路当成 4K 备用，也不能把编辑模型当成普通文生图模型。

### 4.4 限流与背压

采用四层限制：

1. **用户级**：防止单用户瞬间提交过多任务。
2. **租户级**：套餐允许的同时运行数和队列上限。
3. **模型级**：不同模型各自并发池。
4. **供应商线路级**：官方 RPM、并发和余额限制。

每层使用分布式令牌桶或租约。拿不到线路容量时，任务保持 `waiting_capacity`，而不是不断请求上游制造更多 429。

队列必须有最大长度和入口背压：当预计等待超过产品阈值时，前端明确提示并允许用户取消，不能无限接单。

### 4.5 重试、换线与熔断

- 网络暂态：指数退避并加入随机抖动。
- 429：尊重 `Retry-After`，更新线路额度账本后再调度。
- 供应商 5xx：有限次数同线重试，再切换等价备用线。
- 认证错误：立即停用该线路，不用其他任务反复验证。
- 连续失败超过阈值：熔断；经过冷却时间后少量半开探测。
- 用户主动取消：尽力取消供应商任务；已产生的真实成本按规则结算。

每次外部提交都保存：`provider`、`endpoint_id`、`provider_job_id`、请求幂等键、尝试序号、开始/结束时间、费用和错误分类。

### 4.6 容量估算

上线前为每个能力池维护简单容量模型：

```text
安全吞吐 ≈ 可用线路数 × 单线并发 × 60 / P95 生成秒数 × 安全系数
```

安全系数初期建议取 `0.6~0.7`，为抖动、重试和突发流量留余量。监控实际到达率、平均在途数和等待时间，根据数据调整，而不是只看服务器 CPU。

---

## 5. 任务状态机与可靠性

### 5.1 推荐状态

```text
draft
  → queued
  → waiting_quota
  → waiting_capacity
  → dispatching
  → provider_running
  → downloading
  → postprocessing
  → succeeded

任意执行态 → cancel_requested → cancelled
任意执行态 → retry_wait → dispatching
任意执行态 → failed
```

状态变化必须通过事务更新，同时写 `job_events`。前端 SSE 断线后可以用事件序号续传；即使事件丢失，也能通过 `GET /jobs/{id}` 获取数据库真状态。

### 5.2 幂等设计

- 创建任务接受 `Idempotency-Key`，网络重发不能创建两份任务或扣两次额度。
- 每个供应商尝试有唯一 `attempt_id`。
- 结果对象键包含任务和尝试 ID，重复回调不会覆盖其他版本。
- 账本写入使用唯一业务键，预扣、结算和退款分别只能成功一次。
- Worker 使用数据库行版本或租约，避免两个 Worker 同时执行一个任务。

### 5.3 超时和僵尸任务

- 区分排队超时、供应商提交超时、供应商运行超时和下载超时。
- Worker 定期续租；租约超时后由回收器判断是否重新执行。
- 已拿到 `provider_job_id` 的任务优先查询供应商状态，不能盲目重复提交。
- 定时任务扫描长时间无事件的任务并告警。

---

## 6. 订阅、额度与计费

### 6.1 套餐模型

建议不要只用“每月可生成多少张”，因为 1K、4K、B2、Pro 和二改成本不同。采用内部积分或额度单位：

| 维度 | 套餐可控制内容 |
|---|---|
| 月额度 | 每周期发放的积分 |
| 并发数 | 同时运行任务数 |
| 队列优先级 | 标准/高优先级 |
| 分辨率 | 1K、2K、4K |
| 功能权限 | Pro、批量、校色、PPTX、团队 |
| 存储 | 原图和结果保留量/时间 |
| 团队席位 | 成员数量和角色 |

套餐示例应由真实 API 成本、失败率、支付手续费、存储/CDN 和目标毛利测算后再定，不能只按供应商标价加价。

### 6.2 额度账本

额度不能只在 `users.balance` 上直接加减。使用不可变账本：

- `grant`：订阅周期发放。
- `purchase`：购买加油包。
- `reserve`：提交任务预扣。
- `settle`：成功后按实际规格结算。
- `release`：任务取消或未产生上游成本时释放。
- `adjustment`：管理员补偿，必须记录原因和操作者。
- `expire`：按套餐规则过期。

每笔余额变化都能追溯到订单、任务或人工操作。当前余额由账本聚合或维护的事务快照得出。

余额口径必须固定：`available = grants + purchases + adjustments - expired - spent - reserved`。`reserve` 只把可用额度转为预留；`settle` 把实际消耗从 `reserved` 转入 `spent`，不能再次从 available 重复扣减；`release` 仅释放未结算的预留。事务快照至少分开保存 `available/reserved/spent`，并以账本重算结果定期校验。

### 6.3 预扣—结算—退款

```text
提交任务
  → 计算最高可能费用
  → 事务内检查余额并 reserve
  → 执行任务
  → 按实际模型/图片数/分辨率 settle
  → 释放多预扣部分

未调用供应商即失败：全额释放
供应商已计费但结果失败：按产品补偿政策处理，并保留真实成本记录
```

必须分开记录“向用户扣除的额度”和“供应商实际成本”，才能计算毛利和发现异常线路。

### 6.4 支付

- 使用成熟支付平台处理银行卡、订阅和税务信息；服务端不接触原始卡号。
- Webhook 必须验签、幂等、落原始事件并异步处理。
- 订阅状态以支付平台事件和本地状态机共同管理。
- 支持支付失败宽限、取消续订、退款、升级按比例结算。
- 国内外支付渠道应通过独立适配器接入，不污染核心额度账本。

---

## 7. 多租户数据模型

建议的核心表：

| 表 | 作用 |
|---|---|
| `users` | 用户身份与状态 |
| `tenants` | 个人空间或企业团队 |
| `memberships` | 用户与租户角色关系 |
| `subscriptions` | 套餐、周期和支付状态 |
| `credit_ledger` | 不可变额度账本 |
| `customers` | 地板商的客户资料 |
| `projects` | 项目、订单或楼盘 |
| `assets` | 上传、生成、缩略图和导出文件元数据 |
| `recipes` | 系统、租户和用户配方 |
| `generation_requests` | 不可变输入参数和提示词快照 |
| `jobs` | 任务当前状态、优先级和租约 |
| `job_attempts` | 每次供应商尝试及实际成本 |
| `job_events` | 状态变化和 SSE 事件源 |
| `results` | 候选图、版本父子关系和最终版本 |
| `reviews` | 评审状态、标签、备注和最佳图 |
| `provider_endpoints` | 供应商能力、配额和健康状态 |
| `audit_logs` | 敏感操作审计 |
| `webhook_events` | 支付/供应商回调幂等记录 |

所有租户归属的业务表都带 `tenant_id`；`users`、供应商能力目录等全局表通过 membership 或显式外键关联租户。查询层对租户表默认注入租户条件。仅靠前端隐藏数据不构成隔离；后端必须逐请求鉴权，数据库可进一步使用 Row Level Security 加固。

### 7.1 图片版本关系

`results.parent_result_id` 保存非破坏性编辑链：

```text
生成原图
├── 磨缝结果
├── 二改结果
│   └── 校色结果
└── 另一版校色结果
```

这样可以撤销、回到任意版本，并准确计算每一步成本。

---

## 8. 文件上传与图片访问

### 8.1 上传流程

1. 前端提交文件名、MIME、大小和哈希。
2. 后端校验套餐、配额和文件类型，返回一次性签名上传地址。
3. 浏览器直传对象存储。
4. 存储事件或前端回调触发安全扫描、解码校验和缩略图生成。
5. 校验完成后资产状态从 `uploading` 变为 `ready`。

必须防护：伪造 MIME、图片解压炸弹、超大尺寸、恶意 SVG、重复上传和对象键越权。

### 8.2 对象键与生命周期

```text
tenant/{tenant_id}/project/{project_id}/uploads/{asset_id}/original.jpg
tenant/{tenant_id}/job/{job_id}/attempt/{attempt_id}/result.jpg
tenant/{tenant_id}/result/{result_id}/thumb.webp
tenant/{tenant_id}/exports/{export_id}/proposal.pptx
```

- 原图私有，不开放永久公网 URL。
- 下载使用短期签名 URL。
- CDN 缓存缩略图时仍需防止跨租户猜测。
- 删除先进入软删除和保留期，再异步清对象。
- 套餐到期后的保留、归档和删除规则必须在购买前明确展示。

---

## 9. 身份、安全与合规

### 9.1 身份与权限

- 邮箱密码或 OAuth 登录，密码使用可靠的自适应哈希。
- 会话使用安全、HttpOnly、SameSite Cookie；写操作启用 CSRF 防护。
- 企业版支持 MFA、成员邀请和角色：Owner、Admin、Editor、Reviewer、Viewer。
- 管理后台与普通用户后台分离权限，敏感操作要求二次验证。

### 9.2 密钥与数据

- 供应商 Key 进入 Secret Manager/KMS，不写配置文件、不回传前端。
- 数据库、对象存储和备份均加密。
- 日志不得记录 Key、支付敏感信息、完整提示词中的个人信息或签名 URL。
- 所有下载、编辑、导出接口都校验租户和资源归属。
- 对外 API 启用速率限制、请求体限制、WAF 和机器人防护。

### 9.3 内容与隐私

- 明确告知用户图片会发送到哪些第三方模型供应商。
- 记录用户选择的数据区域和供应商数据政策。
- 提供数据导出、账户删除和项目彻底删除流程。
- 对上传的人像、室内照片和客户信息制定隐私政策与保留期限。
- 内容安全拒绝不能通过自动换供应商来绕过。

---

## 10. 实时状态与用户体验

- 创建任务立即返回 `job_id` 和预扣额度。
- 前端优先使用 SSE，带 `Last-Event-ID` 断线续传；SSE 不可用时退化为轮询。
- 显示 `排队中 / 等待供应商容量 / 生成中 / 后处理中`，不要统一写成“处理中”。
- 返回队列位置和预计等待区间，但避免承诺精确秒数。
- 用户关闭网页不影响任务；完成后通过站内通知、邮件或浏览器通知提醒。
- 系统拥堵时允许用户选择：继续等待、切换较慢/较贵线路、降低规格、取消退款。

---

## 11. 可观测性和运营后台

### 11.1 必备指标

- API 请求量、错误率、P50/P95/P99 延迟。
- 各队列长度、最老任务等待时间、任务完成率。
- 按供应商/模型/区域统计成功率、429、5xx 和 P95 生成时间。
- 在途任务、可用令牌、配额剩余和熔断状态。
- 每张图实际成本、用户收入、毛利和未计价任务。
- 额度预扣未结算数量、支付回调积压。
- 对象存储量、CDN 流量和数据库容量。

### 11.2 日志与追踪

每次请求贯穿以下关联 ID：

```text
request_id → tenant_id → job_id → attempt_id → provider_job_id
```

使用结构化日志和分布式追踪。错误平台收集异常，但上传内容和供应商响应需做脱敏与采样。

### 11.3 管理后台

管理员需要看到：

- 当前队列和卡住任务。
- 供应商健康、配额、成本和熔断开关。
- 用户订阅、余额和异常消费。
- 任务重放、取消、人工退款和额度补偿。
- 系统公告、维护窗口和功能开关。
- 审计日志：谁在何时修改了线路、套餐、额度和用户状态。

人工补偿必须通过账本，不允许直接改余额且不留记录。

---

## 12. 部署拓扑

### 12.1 起步生产环境

第一阶段建议采用托管基础设施，减少运维面：

```text
CDN/WAF
  ├── Next.js Web：2 实例或平台托管
  ├── FastAPI API：至少 2 实例
  ├── Worker：按队列 2+ 实例，可弹性扩容
  ├── 托管 PostgreSQL：高可用 + PITR
  ├── 托管 Redis：高可用
  └── S3 兼容对象存储 + CDN
```

API 和 Worker 使用同一份应用镜像，但启动命令不同。数据库迁移作为独立发布步骤，不能由每个实例启动时竞争执行。

### 12.2 环境隔离

- `development`：本地开发，可使用模拟供应商。
- `staging`：独立数据库、存储桶、支付测试环境和低额度供应商项目。
- `production`：正式账号、正式域名和独立密钥。

禁止 staging 复用生产数据库、存储桶或支付 Webhook Secret。

### 12.3 发布策略

- 容器镜像不可变，按 commit SHA 标记。
- CI 执行 Python 测试、前端 lint/build、数据库迁移检查和依赖漏洞扫描。
- 先部署 staging 冒烟，再滚动或蓝绿发布 production。
- Worker 发布时先停止领取新任务，等待当前任务完成或安全转移。
- 高风险供应商路由通过功能开关灰度给少量租户。

---

## 13. 可用性、备份与灾难恢复

### 13.1 建议目标

早期可采用：

- Web/API 月可用性目标：99.9%。
- API 只有在任务和额度预扣事务持久提交后才确认接收；单实例/Worker 故障不得丢任务，区域级灾难的数据损失边界按 RPO 明确展示。
- 数据恢复点目标 RPO：任务状态与额度账本依靠同步高可用/WAL 归档争取接近 0；其余数据库数据不超过 15 分钟。若早期部署做不到，产品文案不得宣称灾难场景“零丢失”。
- 服务恢复时间目标 RTO：不超过 2 小时。

不要把第三方模型供应商的可用性直接承诺成自身 SLA。状态页要区分“平台故障”和“上游容量不足”。

### 13.2 备份

- PostgreSQL 自动备份 + Point-in-Time Recovery。
- 对象存储启用版本控制或跨区域复制，按成本分阶段开启。
- KMS、基础设施配置和数据库迁移脚本纳入灾备清单。
- 每季度做恢复演练；只有真正恢复验证过的备份才算有效。

---

## 14. 成本控制

主要成本来源：

1. 图像模型调用。
2. 生成失败和重复重试。
3. 图片存储、下载和 CDN。
4. Worker、数据库和 Redis。
5. 支付手续费、邮件和监控。

控制措施：

- 提交前按模型和规格估算成本并预扣。
- 按供应商实际账单做每日对账。
- 设置租户日消费、平台日消费和单线路预算上限。
- 对异常重试、重复任务和大量下载告警。
- 原图和结果按套餐设置生命周期，缩略图使用 WebP/AVIF。
- 预览优先走低成本模型，确认后再升 4K。
- 路由不能只选最便宜线路，要把失败重试成本计入有效成本。

核心经营指标：

```text
单次成功生成有效成本 = 所有尝试成本 / 最终成功任务数
毛利 = 用户确认收入 - 供应商成本 - 支付费 - 基础设施分摊
```

---

## 15. 在线版 API 草案

```text
POST   /api/v1/auth/register
POST   /api/v1/auth/login
POST   /api/v1/auth/logout

GET    /api/v1/me
GET    /api/v1/tenants/current
GET    /api/v1/subscription
GET    /api/v1/credits
GET    /api/v1/credit-ledger

POST   /api/v1/uploads/sign
POST   /api/v1/uploads/complete

GET    /api/v1/projects
POST   /api/v1/projects
GET    /api/v1/projects/{id}
PATCH  /api/v1/projects/{id}

POST   /api/v1/jobs                 # 支持 Idempotency-Key
GET    /api/v1/jobs
GET    /api/v1/jobs/{id}
GET    /api/v1/jobs/{id}/events     # SSE，可续传
POST   /api/v1/jobs/{id}/cancel
POST   /api/v1/jobs/{id}/retry

GET    /api/v1/results/{id}
POST   /api/v1/results/{id}/edit
POST   /api/v1/results/{id}/inpaint       # 创建候选任务，返回 job_id
POST   /api/v1/results/{id}/color-match
POST   /api/v1/results/{id}/review

POST   /api/v1/exports
GET    /api/v1/exports/{id}

POST   /api/v1/billing/checkout
POST   /api/v1/billing/portal
POST   /api/v1/webhooks/payment     # 独立验签和限流
```

所有列表 API 使用游标分页；所有写操作返回明确错误码；API 版本从第一天开始保留 `/v1`。

---

## 16. 从当前单机版迁移的路线

不建议一次性重写。按以下阶段迁移，每阶段都能独立上线验证。

### Phase 0：代码边界整理

- 将当前引擎从本地路径和全局配置中解耦。
- 生成请求改用不可变快照。
- 建立 Provider Adapter，先把现有 Google、Fal、Omakase 接入。
- 引入统一错误分类、幂等键和任务状态机测试。
- 保持现有单机版可运行，避免同时失去稳定基线。

验收：现有 132 项回归通过；同一引擎既能接受本地文件，也能接受对象存储资产描述。

### Phase 1：单租户云端闭环

- PostgreSQL 替代 JSON 记录和进程内任务历史。
- 对象存储替代本地 `output_files/` 和 `_ng_uploads/`。
- Redis 队列 + 独立 Worker。
- API 部署两个实例，SSE 使用共享事件通道。
- 暂不开放注册和支付，只供内部账号使用。

验收：网页关闭、API 重启、Worker 重启后任务仍能查询和恢复；图片不依赖本地磁盘。

### Phase 2：多租户与项目制

- 用户、租户、成员、角色。
- 客户、项目、资产和版本树。
- 全链路 `tenant_id` 隔离和权限测试。
- 数据导出与账户删除。

验收：自动化测试证明租户 A 无法通过枚举 ID、对象键或导出接口访问租户 B。

### Phase 3：订阅和额度

- 套餐、订阅、支付 Webhook。
- 不可变额度账本。
- 预扣—结算—释放。
- 套餐并发、队列优先级和存储限制。
- 管理后台的补偿和审计。

验收：重复提交、重复 Webhook、任务重试和取消均不会重复扣费；每日账单能和供应商成本对账。

### Phase 4：多供应商智能路由

- 完成能力池、配额账本、健康评分和熔断。
- 接入至少一个等价图像备用供应商。
- 按套餐和能力路由，支持拥堵等待和自动换线。
- 增加供应商状态页、成本和限流告警。

验收：人为关闭主线路后，新任务自动进入等价备线；已有任务不重复提交、不丢结果、额度结算正确。

### Phase 5：规模化与企业能力

- Worker 自动扩缩容。
- 企业 SSO/MFA、团队席位和专属额度池。
- 跨区域存储和灾备。
- 数据仓库、经营报表和质量推荐。
- 对外开放 API 时增加 API Token、签名、配额和开发者文档。

---

## 17. 第一版推荐技术组合

为了尽量复用当前代码，建议第一版采用：

| 层 | 推荐 |
|---|---|
| 前端 | 继续 Next.js + React |
| API | 继续 FastAPI，按领域拆模块 |
| 数据库 | 托管 PostgreSQL |
| 缓存/队列 | 托管 Redis + 一套成熟 Python 任务队列 |
| Worker | Python Worker，复用现有引擎、Pillow、NumPy |
| 文件 | S3 兼容对象存储 + CDN |
| 身份 | 成熟托管身份服务或可靠认证库，不自造密码协议 |
| 支付 | 成熟订阅支付平台 + Webhook |
| 密钥 | 云 Secret Manager / KMS |
| 部署 | 容器化，API 与 Worker 分开扩容 |
| 监控 | 指标 + 结构化日志 + 错误平台 + OpenTelemetry |

云厂商和支付平台的具体选择应根据目标客户地区、备案、支付渠道、数据跨境要求和团队运维能力决定。架构层保持 S3、PostgreSQL、Redis、OpenTelemetry 等通用接口，避免过早锁死。

---

## 18. 上线前检查清单

### 产品

- [ ] 套餐权益、额度消耗和退款规则已明确展示。
- [ ] 上游拥堵时有真实状态和取消入口。
- [ ] 用户知道图片会发送给第三方模型供应商。
- [ ] 数据保留和账户删除规则清晰。

### 可靠性

- [ ] API/Worker 重启不丢任务。
- [ ] 重复请求和重复回调不会重复扣费。
- [ ] 429 不会形成重试风暴。
- [ ] 主供应商故障时能等待、换线或明确失败。
- [ ] 数据库和对象存储完成恢复演练。

### 安全

- [ ] 无模型 Key 暴露在浏览器、数据库明文或日志中。
- [ ] 所有资源接口验证租户归属。
- [ ] 上传文件完成类型、尺寸和解码校验。
- [ ] 支付 Webhook 验签并幂等。
- [ ] 管理员改额度和线路均有审计。

### 成本

- [ ] 每个模型有成本配置和预算上限。
- [ ] 用户扣费与供应商成本分开记录。
- [ ] 每日自动对账并报告差异。
- [ ] 异常重试、下载流量和存储增长有告警。

---

## 19. 最终建议

近期最值得先做的不是继续增加图片编辑滑杆，而是依次完成：

1. Provider Adapter 与统一错误模型。
2. PostgreSQL 任务状态机和对象存储。
3. Redis 持久队列与独立 Worker。
4. 用户、租户、项目和权限。
5. 额度账本与订阅支付。
6. 多供应商能力池、限流调度、熔断与成本路由。
7. 监控、审计、备份和恢复演练。

完成前 3 项后，程序才真正从“部署在服务器上的单机应用”变成“可以可靠运行的在线服务”；完成前 6 项后，才适合以订阅制向外部客户规模化开放。
