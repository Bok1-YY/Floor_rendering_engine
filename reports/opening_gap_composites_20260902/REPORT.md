# Opening gap source/model composites v2

Each of the nine source raw crops is paired with its own isolated Blender close-up. No Layer-1 render is reused. All semantics, cuts, pairs, adjacency, score, and build fields remain fail-closed.
