# 开发日志

> 每次与 Claude Code 协作的改动记录，**最新在最上**。便于下次接手时快速了解上次做了什么、为什么这么做。
> 详细功能/架构看 [DEVGUIDE.md](DEVGUIDE.md)；本文件只记"每次会话改了啥、为什么"。

---

## 2026-08-07 主目录合并、生成工作台升级与 GitHub 首页重构

### 本次改动
- 将独立 `portfolio-showcase` 工作树的完整新版合并回 `main`，包括生成页、记录页、复盘页、应用外壳和任务卡的新版布局；清理临时工作树前确认全部修改已提交并合并。
- 新增地板画面占比参数：后端 `10..80` 范围、默认 `40/50`、最小值不大于最大值；参数贯穿 Gemini、电影规划和 SD 提示词。前端改为双端 Slider，拖动时本地预览、松手提交，并自动修复旧草稿中的越界值。
- 保留并明确“电影真实感”开关：B2 / Pro 生图前规划可信机位、动作、视线与现实光源，失败时使用本地 fallback，不阻断正式任务。
- 修复 `start-windows.bat` 只判断 `web/out` 是否存在的问题：现在会比较前端源码与产物时间，过期才自动重建；实际使用脚本启动 7870 并从 OpenAPI 验证新版字段后关闭测试实例。
- 参考优秀图像类仓库的 GitHub 阅读节奏，重构中英文 README：去掉徽章墙、首屏指标表和连续架构图，改成 Why → What It Does → 大图产品画廊 → 产品规则 → 生产证据 → 工程与启动。新增电影真实感、地板占比双滑块和过期产物自动重建说明。

### 验证
- 后端 `.venv\Scripts\python.exe -m pytest`：**211 passed**。
- 前端 `npm run lint` 与 `npm run build` 通过，Next.js 16.2.9 完成 8 个静态页面构建。
- 双滑块在真实浏览器中从 10/11 拖动到 40/50，刷新后保持；未触发任何付费出图。
- README 中英文结构、指标和功能描述保持对应；所有展示图使用仓库相对路径。

## 2026-08-06 GitHub AI 产品作品集重构

### 为什么
仓库原首页更像工程说明书，最重要的真实使用、生产效率、通过率和业务闭环没有在首屏体现，也缺少可快速浏览的实机素材。对求职展示而言，招聘方很难在短时间内理解“为谁解决了什么问题、做出了什么结果、本人承担了什么”。

### 本次改动
- 重构中英文 README：按业务结果、用户工作流、核心能力、产品取舍、个人职责、架构证据和快速启动组织；把 AI 智能选区的详细原理下沉到可展开区域。
- 新增中英文产品案例，完整记录目标用户、旧流程、问题定义、产品策略、关键迭代、指标口径、职责边界、局限和下一步。
- 使用脱敏后的真实运行数据采集产品概览、校色对比和智能选区交互，制作轻量 WebP/GIF 与 GitHub 社交预览图。
- 根据首轮展示反馈重做首屏动图：按“色板规划 → 批量生产 → 多模型候选 → 自动校色 → 智能修补 → 评审交付”轮播真实功能，记录页缩短为最后一帧；新增四宫格功能总览，避免首页视觉被单一记录页面占据。
- 展示真实内部数据：约 10 名用户、近 2,000 张累计出图、约 80% 综合成本降低；Pinterest 约 4% 点赞到下单仅标注为团队月度运营观测，不做系统单一因果归因。

### 验证口径
- 中英文页面的关键数字、能力、个人职责和局限保持对应；图片使用相对路径并控制体积。
- README 仍保留开发手册、架构、安全与 AGPL/商业授权入口，招聘展示不会替代工程文档。

## 2026-08-06 生成式修补 AI 智能选区与真机点击修复

### 为什么
生成式添加/移除原本只能靠画笔涂抹，复杂家具边缘慢且容易漏选。地板校色已经具备离线 MobileSAM 蒙版能力，本轮复用其模型运行时与图像编码缓存，新增面向通用物件和承载区域的智能选区，同时完整保留画笔工作流作为精修与降级入口。

### 本次改动
- `floor_segmentation.py` 新增通用 `scan_object_masks` 与 `segment_mask_at_point`：1280 工作尺寸、8×6 自动采样、多尺度 mask、置信度/稳定度/面积过滤、点击连通域裁剪、IoU/包含率去重、最多 24 候选；新增行优先 RLE 编解码、3 项扫描 LRU 缓存，并复用原 MobileSAM embedding。
- 新增 `POST /api/inpaint/segment` 与 Pydantic/TypeScript 契约。端点复用生成式修补的 job/record/room 源图解析和路径守卫，支持 `strategy=scan_objects|point`，不允许前端绕过守卫读取任意文件。
- `InpaintDialog` 增加青色候选轮廓、红色已选区域和多物件切换；移除模式后台自动扫描，添加模式点击地面/墙面/桌面取得初始区域。最终 mask 为 AI 已选区域与画笔补选的并集，再减去橡皮排除；移除/添加分别保留编辑状态，原画笔、橡皮、撤销和清空全部保留。
- 模型不可用、没有稳定候选或复杂反射/遮挡识别不完整时，界面给出 warning 并继续允许手绘，不回退成全图修改。

### 真机反馈与修复
- 初版虽通过模型与路由测试，但用户真机点击时出现“没反应”。复查真实浏览器交互后确认两个原因：后台扫描未完成且点击未命中候选时前端静默 return；同时完整扫描长期占有推理锁，单点识别只能排队到扫描结束。
- 修复后，任何未命中点击都会立即显示“正在识别点击的物件…”并发起单点提示；完整扫描改为每个网格点之间释放共享锁，让显式点击优先，之后再合并后台候选且不覆盖用户选择。
- Windows 实际界面复测：移除模式点击椅子后正确显示红色选区和“已选 1 个物件”；添加模式点击地面后正确选中承载区域并显示置信度。隔离后端并发计时中，单点约 **5.14 秒**返回，完整扫描约 **16.26 秒**完成，证明点击不再等待整轮扫描。该时间仅代表本次机器和测试图。

### 验证
- 后端 `.venv/bin/python -m pytest`：移植到 `Floor_rendering_engine/main` 后 **207 passed**；新增 RLE 往返、候选过滤去重、点选策略、缓存与 `/api/inpaint/segment` 路由契约回归。
- 前端 `npm run lint`、`npx tsc --noEmit`、`npm run build` 均通过；`git diff --check` 通过。
- 使用独立测试端口完成浏览器真机交互，测试服务已停止，不影响用户原 7870 进程；代码更新后需完整重启后端与前端才能加载新路由和新组件。

## 2026-07-30 Windows 独立克隆与启动兼容

### 本次改动
- `serve.py` 按实际仓库目录名动态导入包，GitHub 默认克隆名 `Floor_engine_Linux` 可直接运行，不再要求手工改名为 `Floor_engine_server`。
- 新增 `start-windows.bat` 与 `dev-windows.bat`，分别支持前后端一体运行和双进程开发模式；Windows 运行数据通过 `FLOOR_DATA_DIR` 集中到仓库内 `data/`。
- pytest 兼容历史包名与默认克隆目录名；README、DEVGUIDE 和忽略规则同步更新。
- 桌面统一依赖安装脚本负责 Python 虚拟环境、后端测试依赖、前端 npm 依赖与生产构建。

## 2026-07-20 AI 地板蒙版局部校色

### 为什么
全图校色在地板接近目标色时仍会同步改变墙面、家具和环境光；矩形框又无法表达被家具遮挡、透视收窄和不规则边缘。本轮把默认流程改为“AI 初稿 + 正负画笔修正 + 严格蒙版内校色”，旧全图模式继续保留。

### 本次改动
- 新增离线 `floor_segmentation.py` 与 `/api/color-match/segment`：MobileSAM ONNX 自动提地板，正/负笔触参与提示，OpenCV GrabCut 做边缘细化；模型不可用时只采用明确手绘区域，绝不扩成全图。
- 新增 `ColorMaskEditor`：绿色补地板、红色排除、笔刷大小、撤销、清空和重新识别；`ColorMatchDialog` 默认局部模式并可切回兼容全图框选。
- 局部自动算法改为 mask 内 median/MAD 稳健统计，只迁移 LAB a/b 并限制方差比，保留 L 明暗；羽化只向 mask 内收，蒙版外输出逐像素等于原图。
- 局部预览/结果使用无损 PNG；保存结果时同时留存 `_mask.png` 和 scope/强度/羽化元数据，任务候选与历史记录两条路径一致。
- Windows Nuitka 打包加入 ONNX Runtime 和 `assets/mobile_sam`；依赖、README、DEVGUIDE、打包说明与路由契约同步更新，端点 67 → 68，测试 174 → 178。

### 验证
- 局部/全图算法、严格区外不变、手绘降级和 PNG 预览新增回归测试。
- `.venv/bin/pytest -q`：**178 passed**；`npm run lint`：0 错误/0 警告；`npx tsc --noEmit` 与 `npm run build` 均通过。
- 用现有 4K 工作图做离线实跑：CPU 约 3.8 秒返回有效蒙版，工作尺寸 1600×1195、覆盖约 24.5%；Windows 独立打包包校验 **198 files OK**。

## 2026-07-20 自由创作多图工作流

### 为什么
原有生成流程面向地板效果图，会由系统模板组装并隐藏内部提示词。新增一条与旧流程隔离的“自由创作”线路，让用户可以原样输入完整指令，并按明确顺序上传多张参考图交给平台生成，同时不改变原有工作流的提示词和出图行为。

### 本次改动
- 新增独立 `POST /api/jobs/free`：接受原始 `prompt`、1–3 个有序 `image_paths`、B2/Pro 模型目标、比例和分辨率；自由任务不进入 `prompts.py` 的地板提示词编译管线。
- Google 与 Fal 调用支持按 Slot 1 → Slot 2 → Slot 3 顺序传递参考图；旧模式继续使用原有 style/room/bevel/floor 组图顺序。
- 前端新增“自由创作”工作流和 Slot 1–3 上传面板，支持替换、删除、前后移动；该模式只显示 B2/Pro，并隐藏预览、批量、润色和地板专用参数。
- 自由任务复用现有任务队列、SSE、取消、重试、重抽、编辑和局部重绘；记录中明文保存用户提示词及有序图片，支持从记录恢复全部自由创作参数。
- 增加 schema、路由契约、记录落盘和 Google/Fal 请求载荷测试；端点契约由 66 个更新为 67 个，后端测试由 170 项更新为 174 项。
- 修复 `FloorVisualizeDialog` 自动预览 effect 的两个 Hook 依赖警告：提取 `hasPreview` 布尔值，保留 550ms 自动刷新且避免直接依赖预览 URL 造成循环请求。

### 验证
- `.venv/bin/python -m pytest`：**174 passed**。
- `npm run lint`：**0 errors, 0 warnings**。
- `npx tsc --noEmit`：通过。
- `npm run build`：Next.js 16.2.9 生产构建通过。
- Google/Fal 请求通过 mock 验证提示词原样传输和三张图片顺序；未调用真实计费接口。

## 2026-07-17 去屎山重构启动（阶段 0-1:契约安全网 + 颜色子系统抽取）

### 为什么
全项目屎山化评估结论:server_api.py(3192 行/66 端点)中度屎山、prompts.py 的 save_task_files_html(850 行/26 参数)是最大单点债务、api.py 混入 640 行颜色算法、模块封装被下划线跨模块 import 击穿。启动六阶段治理(计划见本次会话),硬约束:HTTP 契约不变、golden 快照逐字节不变、Nuitka 打包不破坏、每阶段独立 commit 可 revert。

### 阶段 0:清场
- 新增 `tests/test_route_contract.py`:固化 66 端点 (path, method) 快照,拆分 server_api 的安全网。端点变更需显式改 EXPECTED_ROUTES。
- 删除 Windows 迁移残留的字面路径垃圾目录 `web/C:\Users\...`(一次性 .next 缓存产物,无活成因)。

### 阶段 5:records.py 拆分 + api.py 收尾 + 异常卫生(重构收官)
- records.py 1210 → 758 行,拆出三个模块:`usage_stats.py`(用量统计)、`exports.py`(HTML/PPTX 导出)、`reveal_security.py`(XOR 混淆 + 揭示密码,纯层无 records 依赖 —— 计划原想把迁移函数也放进去,但 persist_jobs 依赖混淆、迁移依赖记录 IO,会成环,故迁移/揭示函数留在 records,记录在案)。
- api.py:模块级 `global _style_cache` 收敛为 `_StyleCache` 类(懒加载+锁+原子写,接口不变);新增 `_notify_stage()` 进度回调守护统一替换 15 处重复的 `try: on_stage(...) except: pass`(现在留 debug 痕);Fal/ComfyUI 取消请求的静默吞异常加 debug 日志。剩余 2 处 `resp.close()` 清理守护属合理静默,保留。
- **刻意不做**:call_gemini_generate 等长函数的内部提函数 —— 该路径(4K 主编排/真实网络调用)无测试覆盖,无法验证的重写违背本次"纯搬移零重写"纪律,记入 DEVGUIDE TODO。
- 新增 `tests/test_usage_stats.py` 5 用例;手动验证 HTML/PPTX/收藏夹导出对真实记录全部 ✅(顺手修了拆分时 exports 漏带的 html/base64 别名 import)。
- 收官状态:**170 测试全绿、66 端点契约不变、golden 逐字节不变、uvicorn 冒烟通过、AST 验证全仓无跨模块下划线 import**。

### 阶段 4:save_task_files_html 上帝函数拆解(850 行/26 参 → 70 行编排 + 5 阶段)
- 26+ 参数签名**原样保留**为兼容入口;函数体改为流水线:`prepare_swatch_png`(新模块 image_prep.py)→ `_derive_translations` → `_build_material_instructions` → `_compose_prompt` → `_derive_pro_prompt` → `_persist_task_record`。p 携带原始参数,v 携带阶段间中间变量。
- 拆分是 **AST 自动推导输入/输出变量集 + 机械生成**,段内正文与拆分前逐字一致;条件赋值变量(如 SEAMLESS_NEGATIVE 仅无缝分支存在)用守卫式导出/`v.get()` 解包,语义不变。
- 新增 `image_prep.py`:小样 PNG 规范化(ICC→sRGB)+ `analyze_floor_tone`/`convert_to_srgb`/`get_srgb_save_kwargs` 从 prompt_data 迁入 —— prompt_data 回归纯数据层。
- `_compose_prompt`(四模式组装,~330 行)刻意保留为单函数:它本质是"按工作流分派 + 每分支一个模板",再按分支拆会给每个 60-80 行分支挂 ~25 行解包样板,收益为负。
- golden 快照 13 个逐字节全绿(硬门槛);165 测试全绿。
- server_api.py 只保留:lifespan(state.init_runtime + 启动恢复)、CORS/同源守卫、healthz、include_router ×6、静态/缩略图服务、前端挂载、main 入口。
- 新模块:`server_state`(全部可变状态:三注册表/信号量/spawn,路由经 `state.X` 访问 → 测试注入有唯一入口)、`server_schemas`(27 个 Pydantic 请求模型)、`server_helpers`(URL 映射/job_view/路径守卫/上传落盘/导出响应)、`image_ops`(纯 PIL mask 处理)、`routes_jobs`(任务队列+全部生图协程,1020 行)、`routes_previews`、`routes_library`、`routes_config`、`routes_tools`、`routes_inpaint`。
- routes 之间互不 import;依赖方向 routes_* → state/schemas/helpers/image_ops → 引擎模块,无循环。
- 领域枚举(WORKFLOW_MODES/SEAM_TYPES/GLOSSINESS/RESOLUTIONS/ASPECT_RATIOS)归位 prompt_data。
- 端点函数体纯剪切粘贴零重写;66 端点契约测试逐项全绿(注意:FastAPI 0.139 的 include_router 包成 _IncludedRouter,契约测试遍历已适配 original_router)。
- 全部测试 patch 目标同步迁移(setattr(server_api,...) → server_state/server_helpers/routes_*);165 测试全绿;uvicorn 起服冒烟:healthz/options/jobs/models/前端静态站全部 200,lifespan 正常恢复历史任务。

### 阶段 2:任务注册表统一(TaskRegistry)
- 新增 `task_registry.py`:泛型 TaskRegistry 类,把 jobs/previews/inpaints 三套复制粘贴的「dict + 锁 + 取消集合 + 上限 + trim」收敛为一个容器;行为逐字对齐原实现(trim 只删最旧终态、in-flight 永不删、取消集合免锁、成员管理加锁)。
- server_api 改用三个实例:`JOBS`(60, newest_first, on_persist=records.persist_jobs)、`PREVIEWS`(20)、`INPAINTS`(20, on_evict=删临时候选文件);原 20+ 处手动 `_persist_jobs()` 散调统一为 `JOBS.persist()`;复合操作(背压检查+插入、apply 状态抢占)走 `locked()` 逃生口。
- 刻意不合并 `_run_job_bg`/`_edit_bg`/`_generate_sd35_model` 巨型协程本体(分支差异真实,强行抽象收益存疑),只把其中对旧全局的引用换成 registry 调用。
- 新增 `tests/test_task_registry.py` 11 个用例直接测公共契约;更新 4 个白盒测试的 patch 目标;165 测试全绿、路由契约不变。

### 阶段 1:颜色子系统抽取 + 封装治理
- 新增 `color_match.py`(664 行):api.py 的 LAB 色彩迁移/识色诊断/高级校色全部搬入,纯 numpy/PIL 无状态;api.py 减至 1955 行。函数签名逐字未动。
- 跨模块私有函数全部转正(23 个改名,不留兼容别名):records 的 `_load_records→load_records_file`、`_save_api_result_jpg→save_api_result_jpg` 等;config 的 `_load_config→load_config`、`_short_text→short_text`;api 的 `_infer_aspect_ratio_from_b64` 转正;prompt_data 的 srgb 工具转正。命名避开 server_api 既有端点函数(load_records/delete_record)。
- `prompts.py`/`prompt_data.py` 的 `__all__ = dir()` 全量导出改为显式清单。
- AST 级验证:全仓不再有任何跨模块下划线 import;154 测试全绿。

### 用户实测结论
- **生成式移除通过实际工作流验收，已达到可用程度**：本轮重新拆分模型推理 mask 与最终合成 mask，并按模式区分外扩/羽化后，先前容易出现的选区错位、边缘污染和重复有损落盘问题得到实质改善。
- 成熟度重新划分：**生成式移除 = 可用功能**；生成式添加仍受具体模型、提示词和场景影响，继续按实验能力看待。
- 上一条“仍不及 Lightroom、暂停投入”是旧版本 R3 的阶段性结论，已被本轮修正后的用户实测结果取代。

### 本轮稳定化改动
- mask 管线拆为二值 `engine_mask` 与独立 `blend_mask`：移除自动适度外扩覆盖物体边缘/阴影；添加默认 grow=0、只向有效选区内部羽化，区外逐像素不变。
- 移除与添加使用不同上下文范围；FLUX/Qwen/Gemini/ComfyUI 分别补齐边界、透视、光照、接触阴影和材质保持指令。
- BRIA/Finegrain/LaMa 无可控 seed，服务端把多候选请求强制收口为 1 次，避免重复出相似图和重复计费；提交时快照实际引擎，排队期间改设置不会改变任务模型。
- 修补候选和最终结果统一无损 PNG；增加队列背压、取消清理、重复提交/apply 竞态保护及 Fal SD/AuraSR 队列句柄恢复。
- 房间 JPEG 增加 EXIF 方向归一化；遮罩严格限制为有效 PNG/base64 且不超过 500 万像素；默认 ComfyUI workflow 去掉重复 GrowMask。
- 前端按 remove/add 分开保存外扩与羽化参数，专职 Eraser 禁用多候选按钮，并准确提示实际处理范围和所需 API Key。

### 验证与提交
- 自动回归：pytest **132 passed**；TypeScript、ESLint、Python 编译检查、Next.js production build、`git diff --check` 全部通过。
- 用户完成本地真实操作验收并确认效果可用；本次将代码、测试、README、DEVGUIDE、SD35/SaaS 同步文档统一提交（不包含密钥、运行期输出和付费测试素材）。

## 2026-07-15 深夜（生成式修补收尾：实测定论"仍不及 Lightroom"，方向暂停 + 全量入库）

### 用户最终实测结论（R3 工程套件之后）
- **仍不达标，暂停投入**：移除后的新贴图**无法适配原图**——经常出现**像素偏移**或**糊成一团**。R3 的裁剪回贴/自适应膨胀/抽卡确有提升（单场景对照成立），但在实际工作图上稳定性不够，与 Lightroom 原生生成式移除仍有明显差距。
- **决定**：功能**保留可用**（标记为实验），但不再在此方向继续投入；代码与文档收编入库（commit 不 push）。
- **后续如重启可排查**：①贴回链路的缩放/取整——裁剪区长边>2048 时先缩到工作分辨率、引擎输出再放大贴回，两次重采样可能是"糊/偏移"的来源（可试仅对小选区启用裁剪、大选区整图直送，或强制引擎原分辨率）；②gemini-mark 引擎一直没实测（配置里 Gemini Key 失效），换有效 Key 后值得 A/B 一次；③本地 ComfyUI / ComfyUI@Fal 扩展位仍预留。

### 本次收尾改动
- README：功能特性加「生成式修补（实验）」条目（含已知局限声明）、配置表加 inpaint 组、测试数 96→116。
- DEVGUIDE：新增 §4.6 生成式修补（管线/引擎/计费/**已知局限与暂停状态**）、端点目录加 `/api/inpaint` 两段式、前端结构加 InpaintDialog、测试数 116。
- 本条日志；随后把 SD 3.5 接入 + SaaS 规划 + 修补 R1~R3 的全部未提交改动一次性 commit（不 push）。

## 2026-07-15 晚（生成式修补 R3：复刻 Lightroom 工程套件 + 引擎实测大洗牌）

### Lightroom Generative Remove 调研结论（知己知彼）
- 底层 = Adobe Firefly 云模型（断网不可用，架构与我们一样是云 API）；**好用的关键不是模型碾压，而是三件工程套件**：①对象+阴影感知的选区自动扩展 ②一次 3 变体抽卡（把生成不稳定做成了产品体验）③选区级裁剪回贴（Firefly 生成上限 ~2K，小选区裁上下文送云端 = 等效原生分辨率）。
- Firefly Fill API 存在（image+mask 同构）但**仅企业合同开放**（~$1000/月起，无自助通道）——此路不通，工程套件可以自己造。

### 本轮改动
1. **裁剪回贴**（`api.py _crop_inpaint_context/_stitch_inpaint_result`）：mask bbox 外扩 max(长边×0.75, 256px) 裁上下文窗口（长边>2048 缩到工作分辨率）→ 引擎 → 缩回贴回 → 原有羽化合成。在 `call_image_inpaint` 调度器统一做，**所有引擎受益**——之前整图直送是清晰度差距的决定性原因。
2. **自适应 mask 膨胀**（`_prepare_inpaint_mask`）：外扩 = max(用户 grow, 8%×选区 bbox 长边, 上限 64px)——LaMa/业界经验 mask 宁大勿小，阴影残留是"移除失败感"最大来源。
3. **候选抽卡重构**：旧三个 fire-and-forget 端点（/jobs/{jid}/inpaint、/records/inpaint、/uploads/room/inpaint）统一为两段式 `POST /api/inpaint`（target 描述目标，n=1-3 并发候选进 `_inpaint_candidates/` 临时目录）→ 前端轮询挑选 → `POST /api/inpaint/{iid}/apply` 才落目标（job 候选/记录 append/room 上传）。**usage 在生成时记（n 张 n 次），apply 不计费**；apply/cancel/trim 清理临时文件。前端 InpaintDialog 改为：涂抹 → 生成 → 候选网格 + CompareSlider 对比 → 使用这张/🎲 再抽。
4. **新引擎**：`call_gemini_mark_inpaint`（红色标记法：mask 区叠 α0.45 红 + 指令"移除红标物体及阴影"，复用 call_gemini_edit 重试机制，2K 档，走 gemini_api_key）；`call_fal_qwen_inpaint`（fal-ai/qwen-image-edit/inpaint，schema 已 OpenAPI 核实：prompt/image_url/mask_url，输出复数 images[]）。

### 引擎实测（付费 ~$0.25，同一坐墩场景走完整生产链路）
- **❌ Qwen-Image-Edit inpaint 做移除全线失败**（重要经验）：其 inpaint 管线**强条件于原图内容**——mask 区物体被原样重绘。strength=1.0、描述式 prompt+负面词、红色标记法、专职 remove-element LoRA（把红标物体涂成红色！）四种玩法全不行。**指令编辑类模型的 mask 只限定改动区域，模型看不见 mask 本身**；remove-element LoRA 需要文字描述物体（与画笔 UX 不符）。→ qwen-inpaint 从移除列表摘除，仅保留在『添加』列表（描述新内容是它的正常用法）。
- **✅ BRIA 走新管线效果大幅提升**：坐墩连同阴影干净移除、板缝自然延续、无接缝残迹——对比 R2 整图直送的"重建质量一般"，裁剪回贴+自适应膨胀就是差距所在。**移除默认维持 bria-eraser**。
- **⚠ gemini-mark 链路已通但被 Google 拒绝**："API key not valid"——配置里的 gemini_api_key 已失效（这也解释了用户平时走 Fal 线路）。换有效 Key 后即可在设置里切换实测。
- 实测花费：Qwen 系 6 次 ≈ $0.2 + BRIA 1 次 $0.04。

### 验证
- pytest **116 passed**（新增 crop/stitch 坐标、自适应膨胀、qwen 仅限 add、gemini-mark 标记图/指令等单测）；桩引擎全链路（三 target 校验、n=3 候选、apply 写回、临时文件清理）；tsc/eslint/Next 构建通过。

## 2026-07-15 下午（生成式修补 R2：移除模式换专职 Eraser 模型）

### 背景：用户实测发现 FLUX Fill 做不了"生成式移除"
- **经验教训 ①**：FLUX Fill 是**填充/替换**模型——给它一个 mask 它倾向于在选区里生成新内容（脑补物体），而不是擦除并延续背景。Lightroom 式移除要用**专职 eraser 模型**。
- **经验教训 ②**：fal 上**没有**文档化的"任意 ComfyUI workflow JSON 直提 API"；`fal-ai/comfy-server` 是社区插件(ComfyUI-fal-Connector)的私有格式端点，不可依赖。官方路线是 comfy.new/dashboard 网页端把 workflow 部署成**私有 app**，之后经 queue.fal.run 调用——所以 "ComfyUI@Fal" 需要用户先在网页部署，本轮只留扩展位（见 config.get_inpaint_provider 注释），第三轮加 `inpaint_fal_comfy_endpoint` + 薄封装即可。

### 改动
- **remove/add 分模型**（provider=fal 时）：`config.py` 新增 `inpaint_remove_model`（默认 `bria-eraser`，可选 finegrain-eraser/lama/flux-fill）与 `inpaint_add_model`（暂只 flux-fill）+ `get_inpaint_models()`；`inpaint_provider` 键值不变，老配置零迁移；comfyui 时模型键不生效。
- **`api.py`**：`call_fal_mask_eraser`（BRIA/Finegrain/LaMa 三模型共用薄封装：无 prompt/seed；mask **强制二值化**再发——BRIA 硬性要求 255/0，阈值 128 恰是羽化坡中点；LaMa 的 mask 字段名是 `mask_image_url` 与 BRIA `mask_url` 不同；输出是**单数 image** → `_fal_image_from_result(plural=False)`）；`resolve_inpaint_engine(mode)` 统一解析 (provider, model_key, usage_label) 供调用与记账共用；`call_image_inpaint` 返回值扩为 4 元组 `(img, err, provider, usage_label)`，fal+remove 走 eraser（忽略用户 prompt）、fal+add 走 FLUX Fill、comfyui 原样；羽化 mask 合成回贴对所有引擎照旧。
- **`server_api.py`**：三个 bg 函数改用 4 元组与 usage_label 记账（新标签 BriaEraser/FinegrainEraser/LaMa）；ConfigPatch/_config_view 加两模型键。
- **前端**：设置页修补引擎卡加「移除模型」下拉（含各模型一句话提示）与「添加模型」固定文案，成本卡加 BriaEraser/Finegrain 单价行；`InpaintDialog` 挂载时拉一次 config，移除模式且移除模型为专职 eraser 时隐藏 prompt 输入（后端本就忽略）。
- **failure_kb**：新增 `eraser_endpoint`（修补失败 + HTTP 403/404 → 提示换模型）。

### 真实付费 A/B（已获用户授权，实付约 $0.08）
- 素材：`../参考样图/灰橡木_看书客厅.webp`（955×955），mask 盖住黄色针织坐墩及投影，走真实生产路径（_prepare_inpaint_mask grow=8/feather=1% → eraser → 合成回贴）。
- **BRIA Eraser**（$0.04, 16.6s）：坐墩干净移除、木纹延续自然、无脑补；**保持原分辨率 955×955**。
- **Finegrain Eraser Express**（$0.04, 24.2s）：同样干净移除且阴影处理好；但 **Express 档把输出降到 768×768**（合成回贴时选区内被放大回原尺寸 → 细节变软）——对本项目 4K 出图是硬伤，Standard/Premium 档（$0.13/$0.22）或许保分辨率但未实测。
- **结论：默认 `bria-eraser`**（原分辨率 + 同价 + schema 全确认）；Finegrain 留在下拉里备选。
- 对比图存于会话 scratchpad（ab_compare.jpg），未入库。

### 验证
- pytest **110 passed**（96 存量 + 14 新增：`tests/test_inpaint_engine.py` 覆盖默认值/非法回落/comfyui 优先级/mask 二值化/LaMa 字段名）；tsc + eslint + Next.js 静态导出构建通过。

## 2026-07-15（生成式修补：类 Lightroom 画笔移除/添加）

### Mask Inpainting 双引擎（Fal FLUX Fill / ComfyUI 可切换）
- **能力**：前端画笔涂抹选区 → 选区内 AI 重绘（移除物体并重建背景 / 按描述添加物体），**选区外像素严格不变**（后端用羽化 mask 把引擎输出合成回原图，兜掉整图 VAE 往返的像素漂移）。
- **引擎层**（`api.py`）：`call_image_inpaint` 调度器按 `inpaint_provider` 分派，**不做自动 failover**（两引擎风格差异大）。
  - `call_fal_inpaint`：`fal-ai/flux-pro/v1/fill` 真 inpainting，image 走 JPEG q95 data URI、mask 黑白 PNG data URI（Fal 官方支持 data URI，无需公网 URL）；复用 `_call_fal_queue_json` 持久队列（不重交、可取消）。FLUX Fill 的 prompt 必填 → 移除模式留空时注入 `DEFAULT_INPAINT_REMOVE_PROMPT`（可被 `inpaint_remove_prompt` 配置覆盖）。
  - `call_comfyui_inpaint`：外部 ComfyUI 实例（HTTP：/upload/image → /prompt → 轮询 /history → /view；取消=/interrupt+queue delete；`trust_env=False` 防内网请求被代理劫持）。workflow 模板占位符注入（`__INPAINT_IMAGE__/MASK/PROMPT/NEGATIVE/SEED`），内置默认模板 `assets/comfy_workflows/inpaint_default.json`（全内置节点：VAEEncodeForInpaint 链，**需自改 ckpt_name**），`comfyui_workflow_path` 可指向任意自定义 workflow（含 Flux Fill GGUF）。
- **服务层**（`server_api.py`）三个入口：
  - `POST /api/jobs/{jid}/inpaint`：job 结果二改，校验同 color-match（候选归属 + running 409），结果并入该 stage 候选（‹n/N› 对比），SSE 零改动走 `pro_stage`。
  - `POST /api/records/inpaint`：仿 record_edit 建临时任务卡进队列，完成 `append_edited_result_to_record`。
  - `POST /api/uploads/room/inpaint`：房间图生成前清理家具，仿 `_previews` 独立状态表 `_inpaints`（不进 `_job_history`），`GET /api/inpaint/{iid}` 轮询 + cancel；结果落 UPLOAD_DIR（`room_*_clean.jpg`）由前端回填。
  - 公共：`_prepare_inpaint_mask`（二值化→外扩→羽化；外扩用「高斯+低阈值」近似膨胀，PIL MaxFilter 大核在 4K 上慢到不可用；宽高比 ±1% 内 NEAREST 对齐否则 400；空 mask 400）；`_model_semaphores['inpaint']` 独立信号量恒 1；**mask 绝不写入 JobRecord**（防 .queue_state.json 膨胀）；`GET /api/inpaint/comfyui/ping` 后端代理探测（浏览器直连内网撞 CORS）。
- **配置计费**：`inpaint_provider`(默认 fal)/`comfyui_base_url`/`comfyui_workflow_path`/`comfyui_timeout`(60-3600)/`inpaint_remove_prompt`；usage 模型标签 `FluxFill`/`ComfyUI`，operation=`inpaint`/`record_inpaint`/`room_prep`，单价表加 `FluxFill`（ComfyUI 不配单价成本显示 —）。
- **前端**：新组件 `InpaintDialog.tsx`（画笔 canvas：可调笔刷/橡皮=destination-out/撤销栈 20 层/清空；mask 画布长边≤2048 导出黑白 PNG b64；**容器收缩包裹图像**避免 object-contain 信箱错位；移除/添加双模式 + 高级区 grow/feather/seed；room 目标弹窗内轮询 + CompareSlider 确认「使用此图」）。入口三处：JobCard 每图槽「🖌️ 修补」、记录页结果卡「🖌️」、ParamsForm 地板替换房间图旁「🧹 清理家具」。设置页新增「生成式修补引擎」卡（引擎切换/ComfyUI 地址+测试连接/自定义 workflow/超时/移除默认提示词）+ FluxFill 单价。
- **失败知识库**：新增 `comfyui_unreachable`/`comfyui_workflow`/`inpaint_mask` 三条（排在 network 兜底前）。
- **验证**：pytest **96 passed**（存量零回归）；`tsc --noEmit` + Next.js 静态导出构建通过；mask 处理/合成语义单测通过；桩引擎起真实服务全链路验证（提交→后台→落盘→轮询 done；半分辨率 mask 对齐正确；选区外像素=原图、选区内=引擎输出）。**未做真实付费验证**：Fal FLUX Fill 出图需有效 Fal Key 实测一次；ComfyUI 引擎需本地起实例实测（内置模板 ckpt_name 需按本机改）。

## 2026-07-14（SD 3.5 独立模型接入）

### Stable Diffusion 3.5 Large + IP-Adapter + AuraSR
- **独立提示词**：新增 `sd_prompts.py`，从 `TaskParams` 编译 SD 专属正向/负向提示词；Gemini 的长期对抗式提示词与 golden 快照完全不动。无缝、常规倒角、圆弧倒角按条件分流。
- **第三模型**：B2/Pro 固定双槽迁移为通用 `model_targets + model_runs`，同时保留旧字段兼容历史任务；前端可多选 B2、Pro、SD 3.5 并行生成，任务卡按 run 动态展示。
- **地板参考**：SD 3.5 Large 强制使用 InstantX IP-Adapter，地板小样以 data URI 输入；当前权重为 `ip-adapter.bin`、SigLIP 编码器、默认参考强度 0.5。首期仅开放纯效果图，失败绝不降级为无参考的纯文本生图。
- **交付链路**：约 1MP、64 对齐扩散 → AuraSR v2 4×（重叠分块）→ 按 2K/4K 长边交付。AuraSR 失败保留基础 PNG、任务标 partial，并新增 `/api/jobs/{id}/sd-upscale` 单独重试，避免重新生成 SD。
- **可靠调用**：真实校准发现 `fal.run` 长连接会被本地代理/TLS 中断，改为 FAL 官方持久队列：只提交一次、轮询同一 request_id、HTTP 202 视为正常排队/推理、完成后取结果；提交响应未知时不自动重交，防重复计费。SD/AuraSR 默认忽略系统与 Google 通用代理直连，另提供 `fal_queue_proxy` 专用配置。
- **前端与配置**：设置页新增 `sd_enabled` 实验开关及 SD35/AuraSR 成本单价；生成页新增三模型多选与折叠的 Seed/Steps/CFG/参考强度/正负追加；记录参数复用、候选切换和手动校色支持 sd35。
- **记录与安全**：记录保存 Seed、采样设置、基础图文件及正负提示词 SHA-256；队列持久化中的 SD 明文提示词继续混淆保存，API Key 不落队列状态。
- **真实付费校准**：`Deep_Saddle.jpg`、Seed 42035、Steps 24、CFG 4.0、参考强度 0.5；SD 队列 `IN_QUEUE → IN_PROGRESS → COMPLETED`，基础图 `1152×896`，AuraSR `4608×3584`。目检深棕色、直拼与大纹理方向基本保持，但比小样略亮、细纹偏软，后续需用 0.45/0.6/0.7 做小批量人工评审。
- **验证**：pytest **96 passed**，Gemini golden 未变；`npm run lint`、Next.js production build/TypeScript 通过；新增 `SD35_INTEGRATION.md` 并同步 README、DEVGUIDE、知识库与 SaaS 文档。

## 2026-07-14（SaaS 规划）

### 在线网页订阅版目标架构文档
- 新增 `SAAS_ARCHITECTURE.md`，把未来方向从“本地单机程序”明确为“多租户网页订阅服务”。
- 规划控制面与 Worker 分离、PostgreSQL 业务真源、Redis 队列/限流、对象存储/CDN、身份权限、项目管理、订阅支付和不可变额度账本。
- 多 API 方案采用合规的供应商能力池：能力匹配、额度感知、优先级/公平调度、背压、指数退避、熔断和成本路由，不依赖简单轮换 Key 规避限制。
- 给出任务状态机、幂等和预扣—结算—退款流程，以及从当前代码逐步迁移的 Phase 0~5 路线与上线检查清单。
- README 与知识库首页补充文档入口；本次仅更新规划文档，不修改当前运行逻辑。

## 2026-07-14

### 手动校色高级编辑：原图基准滑杆、双重置、1% 自动校准与安全放大
- **高级参数**：校色弹窗新增色温、色调、曝光、对比度、高光、阴影、白色色阶、黑色色阶、中间调、饱和度；自动校准范围改为 `0%~100%`、步进 `1%`。
- **原图基准语义**：后端从框选地板与参照小样的 LAB 统计推导满强度 `auto_adjustments`。自动校准百分比变化时，前端按比例移动高级滑杆；用户拖动任意高级滑杆后切换为 `manual`，直接以 Gemini 原图为零点处理，不再把手动参数叠加到自动处理图上。
- **双重置**：「恢复自动校准」回到当前/最近一次自动强度对应的原始 Reinhard 效果；「恢复 Gemini 原图」把自动强度与全部高级参数归零。预览、任务候选保存、记录保存统一携带 `adjustment_mode=auto|manual`。
- **实时预览**：自动强度继续使用原图与满强度结果的客户端线性混合，1% 调整即时生效；高级参数走 180ms 防抖本地预览，请求序号 + AbortController 防止旧响应覆盖和误启用保存。
- **放大报错修复**：开发环境/API 图片跨源时，Canvas 已被污染，调用 `toDataURL()` 会抛 `SecurityError`。结果放大改为 `ImageZoom` 内直接叠加「Gemini 原图 + 满强度结果图层」，不再导出 Canvas，`file://`/跨源开发方式均可用且仍保留当前自动强度。
- **图像算法**：高级调色按 LAB 色度、线性 RGB 曝光、LAB 亮度区间和 Rec.709 饱和度处理；Pillow LAB 的 a/b 通道按有符号字节解码，避免冷色/绿色跨中性点方向反转。现有自动 Reinhard 输出保持不变。
- **接口与兼容**：`ColorMatchPreviewRequest`/任务/记录请求新增 `adjustments` 与 `adjustment_mode`；预览响应新增 `auto_adjustments`。引擎层保留 `relative` 模式兼容旧的直接调用，服务端默认使用新的 `auto` 语义。
- **质量门槛**：修复 `ColorMatchDialog` 原有 React 19 ref/effect lint 错误；新增自动参数方向、原图零点、模式兼容、接口返回和边界测试。最终 `.venv/bin/python -m pytest` **88 passed**，`npm run lint` 无错误/警告，Next.js 生产构建与 TypeScript 通过。
- **文档同步**：更新 `DEVGUIDE.md`、上级 `DESIGN.md` 与 `PACKAGING.md`；明确当前商业主线是 FastAPI + Next.js + Nuitka，旧 `build.bat/floor_engine.spec/run_app.py` 只属于冻结 NiceGUI 原型。

## 2026-07-13（文档同步）

### README / DEVGUIDE 对齐当前功能
- **README**：补充批量生成、我的配方、参数复用、前后对比、评审复盘、成本估算、PPTX 品牌、深色模式与手动校色，并同步配置、运行期数据、使用流程和项目结构。
- **DEVGUIDE**：更新到 2026-07 的真实代码结构，补齐预览、品牌上传、自定义配方、评审复盘和校色端点，以及 `/review`、新增组件、`draft.ts`、`gen_context` 数据流。
- **数字修正**：API 路由说明由 40+ 更新为 50+，pytest 回归数量由 43 更新为 77。
- **边界**：仅更新文档，不修改校色或其他程序逻辑。

## 2026-07-13（晚·三）

### 非校色功能审阅修复：品牌导出 / 成本 / 复盘 / 对比 / 主题
- **PPTX 品牌**：修正 logo 像素被误当 EMU 导致封面几乎不可见的问题，按 `2.4×1.2in` 等比适配；新增移除 logo，替换/移除时只回收上传目录内由程序管理的 `logo_` 文件。
- **成本统计**：`NB2 Lite` 从 B2 正式模型中拆为独立 `Lite` 单价；历史 `preview/B2` 数据读取时无损归并到 Lite；部分模型缺单价时显示「已计价成本」并提示未计价成功图数；服务端拒绝 NaN/Infinity。
- **复盘与对比**：复盘页刷新同时更新 summary/gallery，并用请求序号阻止快速切筛选的旧响应覆盖；前后图比例差异超过 1% 时从叠加滑块降级为双栏完整对照，避免 `object-cover` 裁切错位。
- **深色模式**：主题按钮改为稳定 DOM + CSS 显隐日/月图标，移除 mounted effect 的 lint 错误；清掉 `next-env.d.ts` 自动生成差异。
- **边界**：按用户要求，本轮不修改任何校色算法、接口、弹窗或校色测试。
- **验证**：pytest 77 passed；本轮非校色 ESLint 通过；Next.js 生产 build 通过。全量 lint 剩余 7 项均位于明确不动的 `ColorMatchDialog.tsx`。

## 2026-07-13（晚·二）

### 手动校色二轮迭代：B2 支持 + 实时预览 + 大窗双栏对照（用户实测反馈三连修）
- **① B2 也能校**：初版 JobCard 校色入口写死 `Pro 候选 ?? B2 候选`，有 Pro 时 B2 永远校不了。改为**每个图槽各自一个「🎯 校色」**（在 ‹n/N›/↗原图 那行），点哪张校哪张（stage/当前候选随槽传入），底部单一按钮删除。
- **② 实时预览**：利用强度混合的线性性——`src·(1−m·s)+t·m·s ≡ 先按 s=1 出完整结果 F，再以 alpha=s 混合 src 与 F`。服务端预览固定 `strength=1.0`、只在**选区松手/换参照**时防抖(350ms)自动请求一次；**强度滑杆纯客户端 canvas alpha 混合，零网络即时生效**。提交仍带真实 strength（服务端全分辨率）。无头实测客户端混合 vs 服务端 s=0.5 逐像素均差 **1.24**（两次 JPEG 编码噪声内，等价成立）。请求带递增序号防乱序（仿 floorSeq）；加载中右栏小 spinner 不清旧画面。
- **③ 大窗双栏**：`ColorMatchDialog` 重构——`sm:max-w-[min(96vw,1500px)]`，左栏「原图·拖框选」（常驻对照）+ 右栏「校色后·实时」canvas，各 `max-h-[62vh]`，窄屏(<1000px)退化单列；左栏 🔍 放大原图、右栏点击放大当前混合结果（canvas.toDataURL）。移除「生成预览」按钮与预览态切换（双栏常驻取代）；保存条件=服务端预览与当前选区/参照对应（强度不再造成 dirty）。
- **根因补刀（既有 bug）**：shadcn `DialogContent` 默认 `sm:max-w-sm`(384px)，无前缀的 `max-w-[...]` 在 sm+ 断点被它覆盖——**此前所有「大弹窗」实际都被压到 384px 宽**（用户"图太小"的真正元凶）。校色/两处前后对比弹窗改用 `sm:max-w-[...]` 前缀修正；其余历史弹窗（批量/快速预览等）同病，留待后续统一。
- **后端**：仅 `_PREVIEW_MAX_SIDE` 1280→1600（大窗保细节，仍亚秒级）。引擎/端点/测试零改动。
- **验证**：pytest 73 passed；tsc 0 错 + build 过；无头 E2E——双图任务卡出现 4 个校色入口（B2/Pro 各二）、点 B2 槽弹窗 stage=b2、打开即自动首帧预览、大窗双栏截图确认、数学等价性断言通过。
- **补充（用户澄清"原图对照"=小样）**：右栏结果图右上角加**小样参照贴片**（`clamp(100px,14vw,180px)`，角标「小样」、✕ 可收起/再展开、点击 ImageZoom 全屏）——校色的目标色紧挨结果图比色，不再只有左下角 44px 缩略图。`pickRef` 换参照改存原图 URL（放大要看全尺寸）。tsc/build/无头截图复验通过。

## 2026-07-13（晚）

### 颜色校准重启 PR2：手动区域校色工具（人眼可校，纯本地零费用）
- **背景**：重启当年放弃的「颜色校准」（第一代"提取色码注入+自动校色"在建 git 前被提示词方案取代，见 `prompts.py:29`）。新形态遵循「人眼可校、不做重型自动 pass」哲学：**手动**框选地板 → 预览确认 → 才保存。
- **引擎**（`api.py` 新增 `match_color_region(src, ref, rect, strength, feather=0.05)`）：区域化 Reinhard LAB 迁移 + 三层防翻车，全部由真实图迭代出来：
  - ① **robust 统计**：全选区初估→剔除离群像素(d>2.5σ)→重估，迁移统计只来自地板本体；
  - ② **颜色相似度掩膜**：每像素到地板统计的归一化距离线性衰减(D0=2.0→D1=3.2)，选区内的绿植/家具自动豁免——初版真实图实测**绿植被外推成洋红**，此层修掉；
  - ③ **方差比钳制 [0.7,1.3]**：校色目标是对齐色调(均值)而非移植小样纹理对比度——不钳时 a/b 通道方差比放大把地板细微波动放大成**粉色斑块**，真实图实测后加。
  - 羽化按 src 短边比例（非固定像素）→ 预览(1280 缩图)与全分辨率提交视觉一致；crop 化处理，4K 峰值内存与磨缝校色同量级。
- **端点**（server_api.py）：`POST /api/color-match/preview`（同步，缩图处理返回 data URL，无临时文件）；`POST /api/jobs/{jid}/color-match`（全分辨率 → `add_candidate` 并入该 stage 候选，可 ‹n/N› 切回原图；**归属校验：图必须属于该任务候选集合**，in-flight 409）；`POST /api/records/color-match`（`append_edited_result_to_record` 回记录；b64-only 老结果 404）。路径校验：`_require_output_image_rel`(复用 `_safe_output_path`)、`_require_ref_image_path`(uploads/outputs 白名单)。`_job_view` 补 `floor_url/floor_path`(=png_path，随队列持久化)；`load_records` 的 gen_context 补 `image_url`。
- **前端**：新组件 `ColorMatchDialog.tsx`——图上 pointer 拖矩形（选区外蒙黑）、强度滑杆(默认 0.8)、参照小样缩略+「换参照」(复用 uploadRef)、「生成预览」→ CompareSlider 原图/校色后对比、**参数变更即预览标脏、保存按钮禁用**（强制人眼确认）。入口：任务卡「🎯 校色」（目标=当前浏览候选，Pro 优先）+ 记录页结果 🎯（有 gen_context 才显示）。
- **顺手修（既有 bug）**：`reject_cross_origin_mutations` 中间件不豁免**同源** POST（浏览器对一切 POST 都带 Origin 头）→ 生产同源部署下所有写操作会被 403（此前真机一直走 dev:3000 前端未暴露）。加同源豁免。
- **测试**：新 `tests/test_color_match.py` 14 项——选区内外行为/羽化连续性/强度恒等/**离群绿植豁免**/RGBA·P 健壮性/路径逃逸/参照目录白名单/候选归属/b64-only 404。全量 **73 passed**。
- **验证**：真实成图 E2E（`2806_(1)` 卧室图 + 对应小样）：选区外通道偏移 0.07(纯 JPEG 噪声)、选区内颜色移动 19.4、人眼确认地板拉向小样暖金调、绿植/家具完好；无头截图弹窗框选态+预览态全流程通。tsc 0 错、build 过、import 不拉 nicegui。

### 颜色校准重启 PR1：二改「保持原图色彩（防偏色）」开关回归
- **背景**：老 NiceGUI 二改有此复选框（`../floor_engine/webui.py:1633`，默认勾选），迁移 Next.js 时丢失——二改从此永远不校色。磨缝路径的 `_match_color_to_reference` 调用不受影响（保持硬编码全强度）。
- **后端**：`EditRequest`/`RecordEditRequest` 加 `color_match: bool = True`；edit 端点由写死 False 改为透传；**`_record_edit_bg` 原本完全无校色分支**（它不走 `_edit_bg`），补镜像分支（ref 强制 `convert('RGB')`——老记录 b64 源图可能是 RGBA/P，Pillow LAB 只支持从 RGB 转换）。
- **前端**：两处二改 Dialog（JobCard + 记录页）加复选框，文案照抄原型：「保持原图色彩（防偏色）」+「…若本次就是想改颜色，请取消勾选」。
- **⚠️ 行为变化**：二改默认恢复防偏色（与冻结原型一致）；想让模型改颜色时需取消勾选。

## 2026-07-13

### 第 4 批：评审复盘页 + PPTX 品牌定制（8 项功能扩展收官）
- **评审复盘页（开发日志 07-09 预留方向的落地）**：
  - `records.py` 新增 `load_review_summary()`（扫全部记录，按 工作流(`_short_mode_label` 归并)/风格/房间/缝型 四维聚合 pass/backup/rejected/unreviewed；**通过率口径 = pass/已评审数**，未评审不摊薄；问题标签 Counter；总览含覆盖率/最佳图数）与 `collect_review_gallery(filter='pass'|'best', limit)`（仿 `collect_favorites`，按出图时间倒序）。
  - `server_api.py` 加只读端点 `GET /api/review/summary`、`GET /api/review/gallery?filter=&limit=`（图片 URL 走 `_safe_output_path`+`_to_url`/`_result_thumb_url` 既有范式）。
  - 前端新页面 `web/src/app/review/page.tsx`（token-first，享受第 3 批令牌化）：总览 stat 卡（总出图/已评审+覆盖率/通过率/最佳图）→ 四维通过率进度条 → 问题标签横条 → 好图样本库网格（通过/最佳 chips 切换 + ImageZoom）；空态均有引导文案。`AppShell` NAV/TITLES 加 `/review`（记录与用量之间）。
- **PPTX 品牌定制**：`config.py` 加 `get_pptx_branding()`（pptx_company/pptx_contact/pptx_logo_path）；`records._build_pptx` 加 `branding=None` 参——封面 logo（PIL 量尺寸等比缩放置顶居中）+ 公司名副标题 + 联系方式底部小字，内容页右下角公司名 9pt 页脚，**logo 打不开/配置坏一律静默降级**（`branding=None` 保证旧行为逐字节不变）；两个导出函数传入。`server_api.py`：`ConfigPatch` 加 `pptx_company/pptx_contact`(≤200 字、入 strip 名单)，`_config_view` 回品牌 + `pptx_logo_url`；新端点 `POST /api/uploads/logo`（复用 `_save_upload(file,'logo_')` + 顺手 `update_config`）；**`_FLOOR_SWATCH_SKIP_PREFIX` 补 `'logo_'`**（否则 logo 混进最近小样网格）。设置页加「品牌导出」卡（公司名/联系方式输入 + logo 上传即生效带预览）。
- **验证**：pytest 59 passed；import 冒烟不拉 nicegui；`tsc` 0 错、`build` 过（`/review` 入静态导出）；隔离后端(7899)实调 `/api/review/*`（真实数据 397 张、全未评审 → 空态正确）+ 无头截图目检复盘页；临时目录 mock 断言聚合逻辑（overview/维度/标签/样本库全对）；`_build_pptx` 三态冒烟（完整品牌/坏 logo 降级/无品牌）全 ✅。
- **8 项功能扩展至此全部落地**（4 批：地基+复用参数+多地板批量+成本估算 → 自定义配方+对比滑块 → 深色模式 → 复盘页+PPTX 品牌）。**⚠️ 待真机确认**：真实评审数据积累后复盘页数值体感、PPTX 在 PowerPoint/WPS 打开的品牌版式。

### 第 3 批：深色模式（全站令牌化 + next-themes 接线）
- **令牌化清理（主要工作量）**：11 个 tsx 里约 190 处硬编码 hex 批量替换为语义令牌类（`text-[#2a241f]→text-foreground`、`bg-[#f2e9e0]→bg-accent`、`bg-[#efe9dc]→bg-muted`、`bg-[#faf7f0]→bg-panel`、`text-[#a8472a]→text-accent-foreground`、`hover:bg-[#a8472a]→hover:bg-primary-hover`、`text-[#b5503a]→text-destructive`、`border-[#d3c8b3]→border-border-strong` 等）。`globals.css` 新增语义令牌：`--success/--warn/--warn-soft/--destructive-soft/--destructive-ink/--primary-hover/--primary-soft/--primary-2(Omakase 软陶土)/--border-strong/--glass(顶栏毛玻璃)/--scroll-thumb(+hover)`，`@theme inline` 同步暴露为类。**`--secondary-foreground` 由 #2a241f 校正为 #6b6356**（全站次级文字实际色）。JS 内联色（JobCard BADGE、records REVIEW_STATUS、usage Stat/渐变）改 `var()`；SVG 描边改 currentColor/var()。**保留**：品牌 logo 渐变、配方木纹渐变、图上角标 rgba 遮罩（两主题通用）。
- **接线**：新建 `ThemeProvider.tsx`（next-themes，`attribute="class"` + `defaultTheme="light"` + enableSystem）；`layout.tsx` 加 `suppressHydrationWarning` 并包 Provider；`.dark` 块整套换**暖炭褐系**（`--background #171412`、`--card #221d18`、`--border #383028`，`--primary` 保持 #c15f3c，success/destructive 提亮）。`AppShell` 顶栏加 日/夜 切换按钮（mounted 后才渲染图标防水合闪烁；sonner 已消费 useTheme 自动跟随）。
- **顺手修**：静态导出 `trailingSlash:true` 下 `usePathname()` 带尾斜杠（`/records/`），顶栏标题查 `TITLES` miss 回落「生成」——AppShell 查表前归一化去尾斜杠（既有 bug，dev 下不显现）。
- **验证**：`npx tsc --noEmit` 0 错；`npm run build` 过；按 DEVGUIDE §六.6 隔离后端(7899, 只读) + Chrome headless CDP 截图 4 页 × 2 主题共 9 张逐一目检——浅色与原设计一致（令牌迁移零视觉回归），深色整体协调、品牌陶土保留、任务卡/记录列表/设置表单/用量表格均正常。
- **⚠️ 使用提示**：深色为新增可选项，默认仍浅色；右上角月亮/太阳按钮切换。

### 第 2 批功能扩展：自定义配方管理 + 前后对比滑块
- **自定义配方（我的配方）**：新引擎模块 `custom_recipes.py`（`recipes.py` 保持纯数据无 IO 不动）——`BASE_DIR/custom_recipes.json`，模块锁 + tmp/os.replace 原子写（仿 `_save_usage_raw`）；`list/add/update/delete_custom_recipe` 四函数，结构 `{id: uuid4hex, name(≤40字), created_at, updated_at, params}`。`server_api.py` 加 4 端点：`GET/POST /api/recipes/custom`、`POST .../{rid}/update`、`POST .../{rid}/delete`（POST-mutation 惯例，404=配方不存在）。前端生成页「智能配方」下新增「我的配方」行：卡片一键**整表回填**（`setParams` merge，区别于智能配方的 5 字段）+ 虚线「＋存为配方」卡 + hover 改名/删除；**存入时剔除 `last_image_path`（跟图走）与 `floor_tone`（跟当前地板识色走，配方是场景模板）**——与复用参数的取舍统一记录在 `recipeSnapshot()`。
- **前后对比滑块**：新组件 `web/src/components/CompareSlider.tsx`（~80 行零依赖：底层原图定高、上层效果图 `clip-path: inset(0 0 0 pos%)`、pointer capture 拖中缝把手）。两处接入：① 队列卡——`_job_view` 新增 `room_url = _to_url(retry_ctx.rp)`（替换类工作流才非空），JobCard 终态加「⇔ 对比」按钮，效果图取当前浏览候选（Pro 优先）；② 记录页——吃第 1 批 gen_context：`workflow_mode` 含「地板替换」或（墙板且 `panel_submode` 含「替换」）且有 `room_url` 时，结果图操作行加 ⇔，Dialog 内滑块对比。导出对比图暂缓（可后续纯前端 canvas 拼图）。
- **验证**：pytest 59 passed；`server_api` import 不拉 nicegui；`npx tsc --noEmit` 0 错；`npm run build` 通过。

### 第 1 批功能扩展：gen_context 快照地基 + 记录复用参数 + 多地板批量 + 成本估算
- **背景**：用户选定 8 项新功能分 4 批落地（复用参数/多地板批量/成本估算/自定义配方/前后对比滑块/深色模式/评审复盘页/PPTX 品牌）。本批做地基 + 3 个 S 级速赢。
- **gen_context 快照（地基，功能 3/4 共享）**：记录 JSON 原本只存摘要字段、无完整入参。`records.py` 新增 `attach_generation_context()`（`record_file_lock` + 原子写，仿 `toggle_result_favorite`）；`server_api._run_job_bg` 在 `update_job(json_path/record_id)` 后挂 `{image_path, room_path, ref_path, model_filter, params: p.model_dump()}`——**绝不含 api_key**。`GET /api/records/load` 顺手给 `gen_context.room_path` 换算 `room_url`（供第 2 批对比滑块）。老记录无此字段，前端按存在与否显隐入口，天然向后兼容。
- **记录一键复用参数**：`draft.ts` 新增独立 key `floor-engine:reuse-request:v1` + `saveReuseRequest/takeReuseRequest`（读后即删，绝不覆写作业草稿）。记录页记录卡（有 gen_context 才显示）加 ⟳「用这套参数再生成」→ 跳生成页；生成页初始化 useEffect **优先**消费复用请求：默认值打底 + 快照整表覆盖、由 path 重建 Swatch（`/uploads/`+basename）、`floor_tone` 沿用快照值**不重新识色**（保留当时手调）。
- **多地板批量（纯前端，后端零改动）**：生成页批量弹窗加 `Segmented`「多房间×同地板｜多地板×同场景」。多地板 tab 用 `api.recentSwatches(24)` 出缩略图多选网格（当前地板预选）；提交时**每块板先 `floorAnalyze` 本地识色**（PIL/numpy 不走生图 API、免费）拿各自 `floor_tone` 再 `createJob`。Omakase 语义成立（场景同、地板异）→ 批量按钮对 Omakase 放开、弹窗只显示多地板 tab（多房间维持禁用）；墙板模式仍整体禁用。
- **API 成本估算**：`config.py` 新增 `get_usage_prices()`（`engine_config.json` 的 `usage_prices`：key=`B2`/`Pro`（可带 `:fal` 线路后缀），value=元/张成功图；脏值静默丢弃）。`records.load_usage_summary(prices=None)` 行/总计补 `cost`（先查 `模型:线路` 再回落 `模型`；**只按成功计**，无单价=None）。`ConfigPatch` 加 `usage_prices`（put 时校验非负）；`_config_view` 返回。前端：用量页加「估算成本」stat 卡 + 明细成本列（无单价显示 —）+ 口径注脚；设置页加「成本单价」卡（B2/Pro 两输入，空=删除，保存时保留 `B2:fal` 等细分 key）。
- **验证**：`.venv/bin/python -m pytest` → **59 passed**；`server_api` import 冒烟不拉 nicegui；`npx tsc --noEmit` 0 错；`npm run build` 通过。
- **待续**：第 2 批（自定义配方 + 前后对比滑块，吃本批 gen_context）、第 3 批（深色模式令牌化，单独提交）、第 4 批（评审复盘页 + PPTX 品牌）。

## 2026-07-09

### 人工选图与问题标注系统（human-loop 质量闭环）
- **背景**：确认地板效果图好坏暂时不能交给机器自动判定——色差、缝相、木纹保真、空间高级感、客户可用性都需要人眼。于是本轮不做“AI 自动质检”，而是把人工判断结构化沉淀到记录里：系统负责记录、筛选、导出，人继续负责审美裁决。
- **记录 schema 扩展**：每张 `results[]` 效果图新增 `review_status`(`unreviewed/pass/backup/rejected`)、`review_tags`、`review_note`、`best`、`reviewed_at`。`favorite` 仍表示进入收藏夹 PPTX；`best` 表示同一条记录/SKU 下的最佳候选，二者语义分开。
- **后端**：`records.py` 新增 `update_result_review()`，复用 `record_file_lock` + `_save_records` 原子写；`best=True` 时同一 `record_id` 下其它结果自动清 `best`，保证最佳图唯一。HTML/PPTX 导出字幕补带评审状态、问题标签、备注。`server_api.py` 新增 `POST /api/records/result/review`，继续用 `_require_record_json_path` 做路径归属校验。
- **前端**：`records/page.tsx` 增加评审筛选（全部/最佳/未评/通过/备选/淘汰）、结果图快速状态按钮、最佳图按钮、标注弹窗（问题标签 + 备注）。筛选后仍使用原始 `result_index` 写回，避免过滤列表下索引错位。
- **类型/API**：`web/src/lib/types.ts` 增加 `ReviewStatus`、`ResultReviewPatch` 和结果评审字段；`web/src/lib/api.ts` 增加 `reviewResult()` typed 调用。
- **测试/验证**：`tests/test_hardening.py` 增加评审写入、标签去重、最佳图互斥、非法目标返回 `None` 的回归测试。`.venv/bin/python -m pytest` → **43 passed**；`cd web && npx tsc --noEmit` → 0 错；`npm run build` → 通过；`Floor_engine_server.server_api` import 冒烟通过。
- **边界**：不做机器自动评分、不新增项目/客户维度、不改变收藏夹语义。后续如果要加强，可基于这些人工标签做“失败原因统计”和“好图样本库”。

## 2026-07-08

### 新增第 6 个工作流「墙板模式」（护墙板/木饰面）+ 迭代参数化
- **背景**：同事提示词文档 `AI制图指令词汇总.docx` 里除地板外还有一套墙板提示词。目标：剥离地板、只留墙板，做成与现有 5 个工作流平级的「墙板模式」。经确认——模式内含子选择器 **再设计/替换/纯原创**（对应文档 V2.0/替换墙板花色/FOSB 原创），提示词输出**英文**（与全部模式一致）。剥离后的墙板中文原文沉淀在 [notes/墙板提示词.md](notes/墙板提示词.md)。
- **引擎哲学守住**：`api.py` 零改；墙板分支**自建**材质/规格/负向（`PANEL_CORE_MATERIAL_INSTRUCTION` 等），绝不复用地板管线的 `CORE_MATERIAL_INSTRUCTION/_floor_spec_block/全局 extra_cmd_en`（后者在无缝分支被塞了 `SEAMLESS_NEGATIVE`，是最大串味陷阱——墙板改用自建 `_panel_extra`，只从 `custom_addition` 造）。新参数默认值让**其余 5 模式 golden 逐字节不变**（`pytest` 已证 38→41 passed 中旧 case 全绿）。
- **图片通道零新增**：三子行为复用现有 `ref_path`(sref)/`room_path`(base) 通道——再设计=场景参照图当 sref（复用参照模式的 `analyze_style_image`），替换=原墙板场景图当 base，纯原创=只喂木纹小样。`server_api._run_job_bg`/`_preview_bg` 按 `panel_submode` 路由；`create_job`/`create_preview` 加"再设计缺 ref/替换缺 room → 400"校验。
- **Pro 变体**：墙板分支显式 `if "墙板": final_prompt_en_pro = final_prompt_en`（不含地板串，`.replace` 本就全 no-op，显式短路防未来串味）。
- **迭代修 5 点实测反馈（把模板从写死改成参数化）**：初版把场景/风格/镜头/灯光**写死**、忽略了界面已暴露的参数，且没墙板尺寸入口。修法：① 新增 `panel_size`（`prompt_data.PANEL_SIZES` 6 预设 + 自定义文本框，`PANEL_SIZE_MAP` 中→英，预设直接取表、只有自定义才调在线翻译）；②③ 注入 `en_room`(空间类型)+`en_style_raw/en_atmosphere`(所选风格)；④ 注入 `en_angle`+墙板构图；⑤ **魔改**地板 `LIGHTING_INSTRUCTION_MAP`（全是 floor 措辞）为 `PANEL_LIGHTING_INSTRUCTION_MAP`（同键、floor→wall-panel），由界面已选 `lighting` 驱动。再设计/纯原创暴露聚焦控件（空间类型·风格·光线·镜头·墙板尺寸·画幅），替换保留原图一切、隐藏这些控件。
- **测试**：`tests/` 加 `panel_redesign/panel_replace/panel_pure` 三个 golden（含 style/lighting/angle/panel_size，锁住参数化注入与墙板灯光/构图措辞），**现 41 passed**。前端 `npx tsc --noEmit` 0 错。**⚠️ 待真机人眼验证三子行为 4K 出图保真。**

---

## 2026-07-02

### 全面审视 + 批量修复（三方交叉审查 → 用户批准全修；本批与 7-01 未提交改动一起待真机点验）
- **背景**：用户要求"仔细审视整个程序"。三方审查（server_api 精读 + 后端引擎 + 前端各一路）发现 2 条威胁保真/数据的既有 bug、7-01 未提交批次里 3 条点验前该收的问题、测试零覆盖假象及若干中低问题；逐条人工核实后用户拍板全修。两个产品取舍：① Omakase 场景定稿**维持必填**（后端中性兜底只作最后防线）；② Omakase 提交时对隐藏的避免项/自定义**强制表单默认值**（避免项=默认全选保住地板保护词、自定义=空）。
- **测试基建（最重要）**：`tests/` 原来全部 `from floor_engine.*`——**测的是旧同级包，对本仓库零覆盖**（历次"pytest N passed"是假保护）。已重指向 `Floor_engine_server.*`（含 conftest 2 处 + test_hardening 3 处 monkeypatch 字符串），重指向后基线 33 passed（证实两包在 golden 路径逐字节等价）。新增 golden：`omakase`（锁散文+地板块焊接结构）、`seamless_square`、`seamless_hb`（锁人字拼字节）；新增语义锁 `test_seamless_square_has_no_chevron`、契约锁 `test_error_paths_return_8_tuple`。**现 38 passed**。
- **保真 bug（既有）**：无缝收尾段 `SEAMLESS_NEGATIVE` 硬编码 "printed chevron grain motif"，正方形拼分支没定义 Pro 措辞 → 「无缝+正方形拼」B2/Pro 都以最高优先级被命令渲染人字纹、与正文方格描述打架。修法：motif 抽成按拼法变量 `SEAMLESS_NEGATIVE_MOTIF`（人字=chevron 原文逐字节 / 直拼=平行直纹 / 方格=微色差方格新文案）。**字节比对护航**：改前后快照对比，人字拼 B2/Pro、直拼 Pro 逐字节不变；直拼 B2 仅记录/展示文本变化（实际发的是 Pro 词）；方格 B2/Pro 收尾换成方格措辞（4 个老 golden combined 各变 1 行，已核对后刷新）。**⚠️ 待真机人眼验证「无缝+正方形拼」实际出图保真。**
- **数据安全（既有）**：`config.py` `_save_config` 原直接覆盖写（截断=三把 key 全丢且会被下次保存二次覆盖）→ 改 tmp+`os.replace` 原子写 + PermissionError 短重试；`_load_config` 损坏时改名 `.corrupt_<ts>` 备份+报错日志（照抄 records 模式），不再静默清零。`records.py` `_save_records` 的 `os.replace` 在 Windows 撞无锁读者会 PermissionError（已计费成图丢记录条目）→ 写侧 5×0.1s 重试。
- **server_api.py**：重抽/多抽错误不再静默——`_regen_once` 收集每模型错误串返回、`_regen_bg` 写进 `job.error`（过滤用户取消）；`create_job`/`create_preview` 对非空 room_path/ref_path 加存在性校验（失效 400，不再静默丢图照常计费）。`prompts.py` 错误路径 7 元组补齐 8（原会让调用方解包崩、真实报错被吞）。`api.py` `call_gemini_edit` 的 `resp.json()` 加保护（透明代理 200 非 JSON 场景）。
- **前端 `web/`**：
  - 预览竞态三连修：`previewRun` 序号 + `previewWanted` ref——弹窗关闭时在途 createPreview 拿到 pid 立即补发取消（不再有"关了弹窗后台白计费"）；旧轮询响应按序号丢弃；轮询 catch 连续 3 次失败才置 failed 带文案（不再静默永久转圈）。预览按钮弹窗开着时禁用。
  - 批量防护：Omakase 下批量按钮禁用 + runBatch 拦截（提示词不含房间类型，批量=N 倍计费同图）；`batchSubmitting` 防双击双份计费。
  - Omakase 文案统一为必填（原"可选/留空走传统设置"与强制校验矛盾，且"传统设置"区已被隐藏）；设置页开关说明改准确（开关只挡 `/api/omakase/scenes`，不隐藏工作流）。
  - `JobCard` 三路快照统一入口 `applySnapshot`：父级 2.5s 轮询兜底同步 props（原 SSE 断流卡片永久冻结、"轮询兜底"其实不成立）；候选浏览 ‹n/N› 只在候选总数变化时重置（原被 SSE 每秒清零）；完成通知挪进共用路径（SSE 断流时轮询也能补通知）。
  - 小修：识色/记录页加载乱序防护（序号 ref）、刷新按钮补 catch、file input 用后重置（同文件重选不触发的坑）、ImageZoom 滚轮改原生非 passive 监听（React 根级 wheel passive，preventDefault 无效还报 console 错）。
  - 注释更正：`types.ts`/`models.py` 的 scene_override "仅纯效果图生效"→"仅 Omakase 生效"。
- **自测**：pytest 38 passed；快照字节比对 3 个"必须一致"全 PASS；离线断言 6 组全绿（配置损坏备份/记录写重试/重抽错误透传/取消过滤/双端点 400 守门）；`py_compile`+import 冒烟（不拉 nicegui）；前端 `tsc --noEmit` 0 错、`next build` 通过（首跑因软路由拉 Google Fonts 瞬断失败，重试即过，与代码无关）。
- **本轮明确不动**：磨缝失败 done→failed 语义（webui 同款）；预览文件清理/并发上限/「全部停止」不含预览；一键启动.bat 入库、.gitattributes；日志轮转（约定暂缓）。
- **待用户真机点验**：① 预览开→秒关→再开不串图、断后端 3 秒预览报错文案；② Omakase 必填提示、批量按钮置灰；③ **无缝+正方形拼 4K 出图人眼验保真（chevron 修复的最终裁决）**；④ 重启后端后运行中任务卡 2.5s 内解冻、重抽失败卡片有错误提示；⑤ 全部改动未提交，点验通过后一起本地提交（仓库无远程）。

### 生成页配置切页丢失 → localStorage 草稿持久化（`web/`）
- **背景**：用户反馈从生成页切到记录页再切回来，已配置的东西全被清空、得重弄。根因——App Router 下 `/`↔`/records` 是**别路由**、用 `next/link` 遷移，`GeneratePage` 每次**重挂载**；而配置全在组件内 `useState`（`params`/`floor`/`refImg`/`roomImg`/`modelFilter`/`recipes`），无 Context/store/localStorage 兜底 → 遷移即清零（送出的 `jobs` 因走后端重取才没丢）。用户拍板方案：**localStorage 持久化**，切页/刷新/重开浏览器都恢复；**参数 + 上传图片全记**。
- **改了什么**：
  - 新增 `web/src/lib/draft.ts`：`loadDraft/saveDraft/clearDraft`，版本化 key `floor-engine:generate-draft:v1`，`typeof window`(SSR) + `JSON.parse`(损坏) 双兜底、写失败静默（对齐 `notify.ts` 风格）。
  - `web/src/app/page.tsx`：把初始化 effect 里内联的默认值块抽成 `buildDefaultParams(o, prevWorkflow)`；`getOptions().then` 里读草稿，`params` 用 `{ ...defaults, ...draft.params }`（默认打底、用户存值覆盖，新增字段仍有默认——抗缺失/过期）恢复，并回填 `floor/refImg/roomImg/recipes/modelFilter`；新增**保存 effect** 监听这几项变化写回草稿，头部 `if (!options) return` 守门（恢复未完成前不写，防"仅含 workflow_mode 的空 params"覆盖好草稿=clobber 防护）。
- **为什么这么放**：恢复**合并进 `getOptions().then`（异步回调）**而非单开一个 effect——为避开 React19/Next16 新 lint `react-hooks/set-state-in-effect`（effect 体内**同步** setState 会报级联渲染错），同时保持 SSR 安全（只在 effect/回调里读 localStorage、不在渲染期）、且成为单一恢复点。
- **没动**：`ParamsForm` 是 props 受控组件，未改；记录页自身选中态、Omakase 输入途中文本（`omaIdea`/候选）本轮不持久化（最终定稿走 `scene_override` 已随 `params` 存）。**送出后不清草稿**（同板连出多张，配置留着是利好，正好与"消失"相反）。
- **取舍**：存的下拉值若日后后端 options 删项，可能残留不在列表的值（Select 显示空档，自用可接受，将来可在 `.then` 加校验回落）；图片存服务器路径，源文件被删会 404。
- **状态**：`tsc --noEmit` 0 错、`eslint`（改动两文件）干净；UI 端到端（切页/刷新/重开浏览器恢复、首次无草稿走默认、送出后保留、clobber 不发生）待用户本地点验。可选的「重置配置」按钮暂未加（要否待用户定）。

---

## 2026-07-01

### 改造 Omakase 为「独立第五工作流 + 精简表单」（原做成了纯效果图子特性，用户纠正）
- **背景**：上一条把 Omakase 做成"纯效果图表单里的可选区块"。用户要的是**与 纯效果图/地板替换/宠物友好/参照 平级的第五个工作流模式**，选中后非必填选项（风格/光线/镜头、地区、房产/房间/家具、避免项/自定义、宠物、国内市场）全隐藏，只留必填：地板图、地板规格(尺寸/拼缝/光泽/色调)、输出、模型线路 + Omakase 诉求/定稿。后端管道（scene_override 贯通 + DeepSeek 客户端 + `/api/omakase/scenes`）全复用，只改"入口形态"。
- **改了什么**：
  - `server_api.py`：`_WORKFLOW_MODES` 加第五项 `'Omakase (AI 代笔场景)'`（经 `/api/options` 自动下发，前端多一张工作流卡）；`GenParams.scene_override` 注释改"仅 Omakase 工作流生效"。
  - `prompts.py`：在 `elif "参照模式"` 之前加 `elif "Omakase" in workflow_mode:` 走精简散文版（散文空给中性英文兜底）；**把上一版塞进纯效果图 else 的 `if scene_override:` 还原**为原始纯枚举组装——纯效果图不再消费 scene_override，逻辑更干净。地板块 `_floor_spec_block(...)` 逐字节不变。
  - `ParamsForm.tsx`：加 `isOmakase`；`WF_SUB` 加副标题；Omakase 区 gating 从 `!isRef&&!isReplace&&!isPet` 改 `isOmakase`；用 `!isOmakase &&` 隐藏 市场切换/位置(海外+国内)/风格与镜头/高级；**保留** 板材工艺(尺寸/拼缝/光泽/色调)/输出/模型线路/工作流卡。隐藏字段仍以 GenParams 默认值发后端。~~（Omakase 分支不引用，无害）~~ **7-02 更正**：`avoid_items`/`custom_addition` 会经 `_floor_spec_block` 进入 Omakase 提示词（藏在被隐藏的「高级」区，切工作流残留旧值会看不见地生效）——已改为前端提交时对 Omakase 强制表单默认值（避免项=默认全选、自定义=空），见 7-02 条目。
  - `page.tsx`：`generate()`/`runBatch()`/`runPreview()` 统一加 `Omakase 需 scene_override` 必填校验。
- **自测（无网/无 key）**：`py_compile` 过；离线断言 **12 条全绿**（新增"纯效果图+散文应忽略散文"证明还原生效）——Omakase 精简散文版成立、纯效果图回归枚举版、两模式地板块逐字节一致；import 冒烟 `'Omakase (AI 代笔场景)' in _WORKFLOW_MODES = True`；前端 `tsc --noEmit` 0 错。
- **待用户真机验**：选「Omakase」工作流卡 → 表单只剩 地板规格/输出/模型/Omakase 区 → 诉求 → ✨生成 → 选/改一段 → 填地板规格 → 生成，记录 prompt 含散文+地板块完好；**回归**：切「纯效果图」确认原字段/出图不变。

### 接入 Omakase 模式 —— DeepSeek 代笔「自由场景层」，人审后拼进提示词（两层：场景放开 / 地板锁死）
- **背景 / 需求**：出图要人手填 ~30 个枚举参数，客户"天马行空"的诉求落不进来。想让开源文本模型（DeepSeek，OpenAI 兼容）把客户一句话诉求（可抽象，如"荷兰普通人家母子玩耍""体现耐刮耐磨""温馨时光"）转成**几段"怎么拍"的中文场景散文**，客户选/改后作为场景层拼进现有提示词。与用户逐轮敲定：**纯自由**（不塞回现有 STYLES/LIGHTING 枚举，先试自由的边界再谈约束）、**先填后审**（人始终在环、审后才生图）、**降级到手写**、v1 仅纯效果图。
- **核心架构（一锁一放）**：最终提示词拆两层——**场景创意层**由 AI 散文接管；**地板技术层**（物理规格 + 地板占比40-50% + 无缝/圆弧倒角 + 色锁 + 负面词 + 画质）**逐字节锁死、代码拥有**。散文只碰场景，碰不到保真。DeepSeek 只产候选、**绝不写最终提示词**；`_run_job_bg` 生成阶段**不调** DeepSeek（守住先填后审）。
- **改了什么**：
  - `prompts.py`：`save_task_files_html(..., scene_override="")`；仅**纯效果图**分支内 `if scene_override:` 用精简版（preamble 只留画幅/分辨率 + 散文 + `_floor_spec_block(...)`），丢掉 `[Style&Atmosphere]/[Camera]/[Lighting]/property/room/location/view` 枚举段；`_floor_spec_block` 调用**参数逐字节一致**，Pro/B2 替换逻辑因作用于地板块字符串故无影响。
  - `models.py`：`TaskParams` 加 `scene_override`；`task_params_to_kwargs` 加映射（`_run_job_bg` 走 `**p.model_dump()`，字段自动贯通，无需改编排）。
  - `config.py`：加 `get_deepseek_api_key/base_url/model`、`get_omakase_enabled`、`save_deepseek_settings`（照 `get_/save_auto_failover` 现式；`omakase_enabled` 默认关；base_url 可配＝将来可换端点/自托管）。
  - `api.py`：新增 `call_deepseek_scenes(idea,*,api_key,base_url,model)->(options,err)`，镜像 `analyze_style_image` 配管（`_req.post`+proxies+`_verify_arg`+`(结果,错误)`元组+`_redact_api_key`），OpenAI 兼容 `chat/completions`+`json_object`，system prompt = "创作守则"（地板须是视觉重点、不写划痕/损坏/对比、真实可拍、不出品牌名），清洗+保底恰一个 recommended。
  - `server_api.py`：`GenParams` 加 `scene_override`；`ConfigPatch`/`_config_view`/`PUT /api/config` 接 DeepSeek（`_config_view` 只回 `has_deepseek_key`，**绝不回明文 key**）；新增 `POST /api/omakase/scenes`（守卫 `omakase_enabled`+key，`asyncio.to_thread` 调客户端，错误经 `classify_failure` 转友好中文）。
  - `failure_kb.py`：在 `overload`/`network` **之前**插 `deepseek_auth`(401/402)/`deepseek_limit`(429)/`deepseek_error`(兜底) 三条——否则 "DeepSeek HTTP 429/5xx" 会被通用 overload 正则抢走、给出转 Fal 之类错误建议。
  - 前端 `web/`：`types.ts` 加 `scene_override`/`OmakaseOption`/config 字段；`api.ts` 加 `omakaseScenes(idea)`；`ParamsForm.tsx`（**仅纯效果图**）在 STYLE 段前加「✨ OMAKASE·场景代笔」区——诉求框 + 生成按钮 + 候选卡（text+why+推荐，点选写回 `scene_override`）+ 可编辑「场景定稿」框（同时是手写/降级入口）；`settings/page.tsx` 加 DeepSeek Key 输入 + 「启用 Omakase」开关。
- **自测（无网络/无 key）**：`py_compile` 六文件过；`import Floor_engine_server.server_api` 成功、`/api/omakase/scenes` 路由注册、config 默认值正确（omakase 默认关）、`classify_failure` 402→deepseek_auth/429→deepseek_limit/解析失败→deepseek_error、`call_deepseek_scenes(api_key='')`→`([], '未配置…')` 优雅返回；**离线两层锁断言（11 条全绿）**：`save_task_files_html(scene_override=<散文>)` 断言散文在、`[Floor Specification]`/色锁/`[Negative Prompt]`/`40-50%` 齐、`[Style&Atmosphere]`/`[Camera]` 消失，且**两模式地板块逐字节一致**；`scene_override=''` 退回原样。前端 `tsc --noEmit` 0 错。
- **未做（边界）**：v1 不接 地板替换/宠物/参照（各有场景逻辑，留 Phase 2）；不做物理属性图像识别（尺寸/拼缝/光泽/色调仍手动）；纯自由不预设"约束/守则"细化——先跑，坏点（地板被埋/不可拍/跑调/编损坏/与色锁顶牛/忽好忽坏）记本日志再迭代约束。
- **待用户真机验（需 DeepSeek key + 起服务/前端）**：设置里填 key+开开关 → 纯效果图输诉求 → ✨生成 → 选一段 → 填物理字段 → 生成，查记录 prompt 含散文+地板块完好；降级：关开关/错 key → 按钮报错 toast、场景框仍可手写、生成不受影响。

### 接入 Nano Banana 2 Lite —— 作为「快速预览/草稿（1K）」独立通道
- **背景 / 调研结论**：Google 6/30 发布 Nano Banana 2 Lite（model id `gemini-3.1-flash-lite-image`）。联网核实官方文档：**只出 1K、不支持 2K/4K**（"only supports 1K images"），快（~4s）便宜（$0.034/1K），支持图生图/多图输入；Fal 暂无对应 endpoint。因 1K 撞「4K 是硬需求」红线，**不能**当 B2/Pro 的平级 4K 生产模型（那要改 ~32-40 处且产出不达标）。与用户确认后定角色：**出 4K 大图前的快速预览/草稿**。
- **方案（轻量、与 4K 队列完全解耦）**：预览做成独立后台任务 + 前端短轮询，**零改动** `JobRecord`/`model_filter`/信号量/`CANDIDATE_SLOTS`/`_job_view`/4K 主编排 `_run_job_bg`。关键点：① Lite 不在 Fal → 预览直接调 `call_gemini_generate`（Google 直连），绕过 provider 路由/转 Fal；② 给 `prompts.save_task_files_html` 加 `persist=True` 开关，`persist=False` 时借用完整提示词逻辑但**不新建 JSON 记录**（默认 True＝4K 主流程逐字节不变）；③ 复用 `_spawn`/`_task_prep_lock`/`_save_api_result_jpg`/`_to_url`/`analyze_style_image`/`get_bevel_ref_image`。
- **改了什么**：
  - `config.py`：加常量 `LITE_PREVIEW_MODEL = "gemini-3.1-flash-lite-image"`（**不进** GEMINI_MODEL_MAP，避免被误当平级模型；注释标 1K-only/预览专用/仅 Google 直连）。
  - `prompts.py`：`save_task_files_html(..., persist=True)`，末尾写记录块包进 `if persist:`。
  - `server_api.py`：新增 `_previews` 注册表 + `_trim_previews`（收口 20）+ `_preview_cancel` + `PreviewRequest` + `_preview_bg`（prep 是 `_run_job_bg` 269-274 的精简版，刻意写副本不重构 4K 热路径）+ 三端点 `POST /api/preview`、`GET /api/preview/{pid}`、`POST /api/preview/{pid}/cancel`；import 补 `call_gemini_generate`/`LITE_PREVIEW_MODEL`/`uuid`。
  - 前端 `web/`：`types.ts` 加 `PreviewView`/`PreviewRequest`；`api.ts` 加 `createPreview`/`previewStatus`/`cancelPreview`；`app/page.tsx` sticky 底栏加「⚡ 预览」按钮 + 1s 轮询 + 结果弹窗（spinner→1K 图，点开 `ImageZoom`，关弹窗若在跑则请求取消）。
- **自测**：`py_compile` 三文件过；`import` 成功、不拉 nicegui、`/api/preview` 三路由注册、`persist` 参数在、`LITE_PREVIEW_MODEL` 正确；无网络脚本断言 `persist=False` 不写记录 / 默认 True 写 1 条、预览端点全链路跑到 `status=done`（model=lite、size=1K、回出 `/outputs` url）；`tsc --noEmit` 0 错、`next build` 通过（5 路由）；现有 `pytest` 仍 33 passed。
- **未做（边界）**：不加平级第三模型；v1 只在「生成」页出单张预览，批量/记录页预览、预览转存进记录暂不做；不给 lite 配 Fal。
- **待用户真机验**：上传地板 → 点「⚡ 预览」→ ~4s 出 1K 图；再点「生成效果图」确认 4K B2/Pro 链路无变化（需 API Key + 网络）。

### 修复：终态任务卡「重试/重抽/磨缝/二改」后前端冻结 + 两处编排层加固（P1+P2+P3，仅动 server_api.py）
- **P1（真 bug）背景**：已完成/失败的任务卡点 重试/重抽/磨缝/二改 后卡片冻结——不显示进度、后台图跑完也不刷新，要整页刷新才见结果（后台工作其实照常完成、不丢数据，纯 UI 同步问题）。
- **P1 根因（后端↔前端契约错位）**：这四个端点都是「先 `asyncio.create_task(...)` 排后台、再**同步** `return _job_view(job)`」，二者间无 `await`，后台"翻成 running"发生在回执之后 → HTTP 回执里的 `status` 仍是**终态**。前端 `JobCard` 用 `active = status∈{queued,running} || pro_polishing` 决定是否开 SSE，拿到终态快照→`active=false`→不订阅 SSE；而 `page.tsx` 2.5s 轮询的 `setJobs` 又进不了已挂载卡的 `useState(initial)`。于是死锁：状态不变→不开 SSE→状态永不更新。（`生成` 正常是因 `new_job()` 默认 `queued`；`停止`/记录页二改也不受影响。）
- **P1 修法**：四端点在 `create_task` 前**同步预置**与其后台 task 开场一致的状态转移（`retry/regen`→`status='running'…`；`polish/edit`→`pro_polishing=True…`），使 `_job_view` 立即回报 active、前端即刻开 SSE；后台再设同值幂等、零瞬时错乱。附带修复：这四类操作的完成通知（系统通知+提示音）此前从不触发，现随 SSE 正常回来。
- **P2（加固）**：全 6 处 `asyncio.create_task` 均未持返回值引用（事件循环仅持弱引用，长任务理论上可能被 GC）。新增模块级 `_bg_tasks` + `_spawn(coro)`（create_task + 收进集合 + done 回调自清），6 处调用统一改走 `_spawn`。
- **P3（加固）**：`_job_history` 只增不减（webui 当年的自动收口没搬过来）。新增 `_MAX_RESIDENT_JOBS=60` + `_trim_history()`（**只删最旧的终态卡，queued/running/磨缝中永不删**），在 `create_job`/`record_edit` 两处 `insert(0)` 后（仍持 `_job_lock`）调用。被删卡下次轮询后自然从队列消失，记录页仍可查。
- **自测**：`py_compile` 过；`import Floor_engine_server.server_api` 成功、`nicegui` 未加载、6 处已全换 `_spawn`；无网络验证脚本（后台 task 打桩 no-op、直接驱动端点协程）断言 P1 四端点回执 `status=running`/`pro_polishing=True`、P3 「66→60 且 running(最旧) 保留、最旧 6 条终态被删」全绿。
- **未动**：前端 `web/`、引擎（`api.py`/`prompts.py`/`records.py`…）零改。可 `git revert` 单次提交回滚。
- **观察（未处理，留待确认）**：`Floor_engine_server/tests/` 全部 `from floor_engine.*` 导入——测的是**同级旧包 `test/floor_engine/`**，并不覆盖本仓库（更不覆盖 `server_api.py`）。本次 `pytest` 33 passed 只是共享环境 sanity；要真正给本仓库上测试，需把 import 改成 `Floor_engine_server.*`（属改测试基建，按惯例先与用户确认再动）。

## 2026-06-29

### 退役并删除旧版 `webui.py`（STEP3 — 去 NiceGUI）
- **背景**：STEP1~2.5 + 视觉重设计完成后，`web/`(Next.js) + `server_api.py`(FastAPI) 已是唯一在用的 UI/服务链路（`一键启动.bat` 也只拉这两个、根本不经过 webui）。webui 在生产链路上早已是死代码，经用户确认可以退役。
- **改了什么**：
  - **删除 `webui.py`**（~2558 行，本仓库唯一 `import nicegui` 的业务文件）。
  - **`__main__.py` 改为退役提示桩**：`python -m Floor_engine_server` 不再起界面，打印新启动方式（`一键启动.bat` / `python -m Floor_engine_server.server_api`）后 `sys.exit(1)`，不再 `import webui`/`nicegui`。
  - **`requirements.txt` 去掉 `nicegui==3.12.1`**：fastapi/uvicorn 原由 nicegui 间接带来，已显式直列，去掉后服务独立可跑。
  - **`DEVGUIDE.md` 全量更新**：架构图标退役、迁移进度标 STEP3 已执行、模块图删 webui 行、铁律/§八/一句话回顾均改为「已退役」。
- **未动 / 注意**：
  - 其余文件里的 `webui` 只是历史注释（"移植自 webui"）或 config 兼容别名，无运行时影响，保留不动。
  - `themes.py` 退役后已无运行期消费者，但本次未删（留待后续清理）。
  - `test/dev_floor.py`、`test/run_app.py`（PyInstaller 原生窗口入口）引的是**另一个旧包 `floor_engine.webui`**，不在本仓库、本次不动。
  - **日常兜底仍在**：冻结原型 `test/floor_engine/`（同源、共享数据）保留作 fallback，并未随本次一起退役。
  - 工作区里 06-26「地板替换/参照模式上传通道」的前端改动属另一件事、仍待本地验证，**未并入本次退役提交**。
- **回滚**：`git revert` 本次提交即可恢复 `webui.py` 与 `nicegui` 依赖。

## 2026-06-26

### 修复：选「地板替换 / 参照模式」后看不到上传通道（待替换图 / 参照图）
- **现象**：用户点「地板替换」或「参照模式」工作流卡，但没出现上传"房间原图 / 参照图"的入口。
- **根因（迁移遗留的前后端缺口）**：
  - 后端 `server_api.py` 四个工作流（含 `地板替换`/`参照模式`）齐全，`api.ts` 也早有 `uploadRoom→/api/uploads/room`、`uploadRef→/api/uploads/ref`，后端 `JobSubmit.room_path` 也在用（line 270 `rp = req.room_path`）。
  - **`地板替换`**：新前端**根本没有房间原图上传组件**——`page.tsx` 把 `room_path` 写死成 `null`，老 NiceGUI（`webui.py:627`）本有的"房间原图"入口在 React 重写时漏了。
  - **`参照模式`**：`RefUpload` 其实有渲染，但被埋在长表单很深处（风格/镜头之后），用户滚动看不到，体感"没出现"。
  - 附带：`WF_SUB` 用了错的 key `实景替换`（真实短名是 `地板替换`），导致该卡副标题空白。
- **改了什么**（只动 `web/`，引擎/后端/`webui.py` 零改）：
  - `ParamsForm.tsx`：把 `RefUpload` 泛化成 `ImageUpload`（传 `uploadFn/buttonLabel/okMsg`，已选则按钮变"重新上传"+缩略图预览）；在**工作流选择卡正下方**新增醒目"源图上传"块——`地板替换`→房间原图（`uploadRoom`，附"保留原图空间家具采光、仅换地面"说明）；`参照模式`→参照图（`uploadRef`）+ 参照修正输入。删掉底部被埋的旧 `参照模式` 块（避免重复）。修 `WF_SUB` key `实景替换`→`地板替换`。
  - `page.tsx`：加 `roomImg` 状态并传给 `ParamsForm`；`generate()` 改发真实 `room_path: roomImg?.path ?? null` 并加"地板替换需上传房间原图"校验；`runBatch()` 同样补 `room_path` 与两种模式的必传校验。
- **自测**：`tsc --noEmit` 0 错、`eslint` 两文件 0 警告。UI 端到端（点两种模式看上传块即时出现、上传后缩略图、提交带 room_path/ref_path）由用户本地确认后提交。
- **未动**：4K 出图链路、引擎、`webui.py`、记录/用量页。

## 2026-06-25

### 重写 DEVGUIDE.md
- 按当前真实代码从零重写开发手册（不沿用旧稿）：开头「零、快速启动」置顶（一键 bat / 手动命令 / 端口表 / 启动逻辑），随后架构总览（绞杀者迁移图）、模块地图（哪些 headless / 谁 import nicegui）、数据与配置（BASE_DIR 共享/迁出自动独立）、后端 server_api（单 worker/作业生命周期/端点目录/加端点范式）、前端 web（栈/结构/设计令牌/数据流）、关键坑（Next16/CORS/单worker/bat/无头截图法）、开发工作流、旧版 fallback、构建与 git。

### 重设计后体验修复：问题汇总.docx 四项
用户实测重设计版后提的 4 个问题（带截图），逐项修复；只动 `web/` + `server_api.py`，引擎/`webui.py` 零改。
- **P1 下拉选项太长被裁**：`ui/select.tsx` 浮层宽度原锁死＝触发器宽(`w-(--anchor-width)`)，长选项(风格/光线含 emoji+中英文+描述)右侧被切。改 `min-w-(--anchor-width) w-max max-w-[min(90vw,560px)]` + `SelectItem` 文本 `whitespace-nowrap→normal` → 浮层按内容变宽、长选项换行整条可读；触发器仍单行不溢出。仅 ParamsForm 用此 Select，影响面可控。
- **P2 队列无清理**：用户要「顶部一键清完成 + 每卡单独清」。后端加 `POST /api/jobs/clear-completed`(删 `status=='done'`，留 部分/失败 供逐卡删/重试) + `POST /api/jobs/{jid}/delete`(删单条)，均仿 `cancel_all` 持 `_job_lock` 改 `_job_history` 后**锁外** `_persist_jobs()`(否则 2.5s 轮询/重启复活)。前端 `api.ts` 加 `clearCompleted/deleteJob`；`page.tsx` 队列头加「清除已完成」按钮 + 给 JobCard 传 `onRemove`；`JobCard` 终态卡动作行加「✕ 清除」→ `deleteJob` 后 `onRemove` 即时移除。**只清任务卡列表，不动出图文件与「记录」**。
- **P3 记录材料名截断**：`records/page.tsx` 左栏文件按钮 `truncate→whitespace-normal break-words leading-snug`(长名整名换行显示)；记录卡头 `id·room·workflow` 同样放开换行。
- **P4 看图框太小**：`ImageZoom.tsx` 弃用 shadcn `Dialog`(锁居中+max-w+圆角+留白)，改自绘全屏遮罩 `fixed inset-0 z-[100] bg-black/95`，图 `max-h-[96vh] max-w-[96vw] object-contain`；保留滚轮缩放/拖动/双击复位 + props 不变，新增 Esc/点背景/✕ 关闭。
- **自测**：`tsc` 0 + `next build` 通过；后端 46 路由(含两新端点)、import 不拉 nicegui。**无头截图实测**(prod+隔离后端 7871/CORS+Node-CDP 交互点击)：P1 下拉长选项换行不裁、P2 顶部按钮+卡片✕清除在位、P3 左栏长名整名显示、P4 点图全屏无边框。截图阶段**只读不触发清除/删除**(隔离后端共享 `.queue_state.json`，避免误删用户已完成任务)。验毕回滚 `.env.local`→7870、重 build、清临时进程，用户 app(3000/7870) 不动。

### 视觉重设计：落地 Claude Design「Floor AI 重设计.dc.html」（整站换皮 + 侧栏外壳）
- **背景**：用户把全量 UI 源码喂给 Claude Design 出了一份完整重设计原型（项目 `e23bc5ce`，`.dc.html` 静态稿），要求照视觉稿还原到真实前端。这是 **reskin + 重排版，不是重写**——`.dc.html` 内联的 `support.js`（假数据/simulate）全丢弃，真实 app 的 SSE/API/状态/handler **逐字保留**，只换 JSX 包裹 + className/内联样式。引擎、后端、`webui.py` 全程零改。
- **最大变化＝应用外壳**：从「顶部 Nav + 居中 `max-w-7xl` 各页」改为 **固定 236px 奶油侧边栏（cream，用户选的）+ 56px 顶栏（路由映射标题/副标题 + 帮助）+ 整高内容区**。新增 `components/AppShell.tsx`（client，`usePathname` 高亮 + 帮助弹窗），`layout.tsx` 改全高并包 AppShell，**退役并删除 `Nav.tsx`**。
- **主题地基**：`globals.css` 加 `--panel #faf7f0`、把 `--muted-foreground` 调 `#857c6e`、`@theme` 的 `--font-sans` 指向新引入的 **Hanken Grotesk**（`next/font/google`，与既有 Geist 同路径自托管，无新网络风险）+ PingFang/雅黑 CJK 栈；加自定义滚动条 + `scfade/dc-spin/toastin` keyframes；body 全高。珊瑚色板沿用 STEP2.5。
- **复用基元**：新增 `components/dc-ui.tsx` = `SectionHeader`（● 标题/ENGLISH 点头标）+ `Segmented`（分段控件）+ `Pill`（胶囊）。字段下拉**复用并 restyle shadcn `Select`**（令牌已对齐，无需改 ui/select.tsx，降风险保 a11y），`Switch` 同理沿用。
- **各页换皮**（逻辑红线全保留）：① 生成 `page.tsx` 改两栏全高（左 600px 参数列+sticky 底栏「生成效果图/批量」、右任务队列）；`ParamsForm` 工作流→2×2 卡、市场+模型→segmented、字段/胶囊/高级折叠按稿重排；`JobCard` 设计徽章(queued/running/done/partial/failed 配色)+圆角缝隙图带 model 角标+候选 ‹n/N›+动作按钮；`FloorUploader` 虚线上传区/已选地板行/8 列小样网格。② 记录页 280px 侧栏 + 房间胶囊筛选 + 记录卡。③ 用量页大数字 stat 卡 + 整体成功率条(teal 渐变)+ 明细表。④ 设置页拆密钥/网络两张卡 + segmented 线路·策略 + 开关。
- **验证**：`tsc --noEmit` 0 错；`next build` 通过（/ /records /usage /settings）。**无头截图比对**（踩坑法 [[design-handoff-match-visual]]）：dev(:3000) 因 headless 下 HMR WS 握手失败导致客户端不 hydrate（页内手动 `fetch` 实测 200，证明非代码/CORS 问题、纯 dev 环境怪癖）；遂起隔离后端(`FLOOR_API_PORT=7871 FLOOR_API_CORS=…3100`) + 临时把前端 base 指 7871 重新 `build` 起 prod(:3100) + 用 **Node 24 内置 WebSocket 走 CDP**（`Page.navigate`→真等 7s→`captureScreenshot`）逐页截图，**四页带真实数据均与设计稿吻合**（生成参数表/小样/任务卡出图、用量卡+成功率条+表、设置两卡+开关、记录左栏+占位）。验毕已回滚 `.env.local`→7870、重 build、清掉所有临时进程（7871/3100/9333），用户原 app(3000/7870) 不动。
- **范围**：侧栏 cream；结果网格 auto；帮助按钮=轻量弹窗；`webui.py` 仍不碰（[[dont-rush-cutover]]，STEP 3 仍无限期延后）。

### STEP 2.5 档3 + 品牌主题：parity 收尾
- **档3(打磨)**：① 增强放大 `ImageZoom.tsx`(滚轮缩放/拖动平移/双击复位,JobCard+记录页共用,便于人眼验 4K 保真)；② 设置补全——TLS 证书校验开关 + 每模型并发数,后端 `PUT /api/config` 扩 `tls_verify/tls_ca_bundle/max_concurrent_per_model`(直接写 `_save_config`)；③ 常见失败参考弹窗 + 后端 `GET /api/failure/rules`(从 `FAILURE_RULES` 出 title/cause/action,12 条)；④ 历史小样"更多"弹窗(`recentSwatches(200)`)。
- **品牌主题**：`globals.css` 的 `:root` token 换成珊瑚陶土橙 `#c15f3c` + 暖奶油米色(`--background #f7f3ec`/`--card #fffdf9`/`--secondary,--muted #efe9dc`/`--border #e3ded1`/`--accent-foreground #a8472a`),对标老版"Anthropic 暖陶米色"。shadcn 组件全跟随(按钮/徽章/聚焦环/胶囊变珊瑚)。
- **被砍(确认不做)**：任务卡内存态收藏+⭐过滤、A/B 外部图追加归档。
- **自测**：`tsc` 0、`next build` 通过(/ /records /usage /settings);后端 42 路由、import 不拉 nicegui、failure/rules 12 条。
- **至此 STEP 2.5 全档(1+2+3)+ 主题完成**，webui 功能基本全部迁移到新前端。`webui.py` 仍留作过渡基线,待 STEP 3 真正退休。
- **DEVGUIDE.md 更新**：开头新增「零、快速启动」(一键 bat / 手动命令 / 启动逻辑 / 端口表 / 数据位置)；技术栈、模块地图、开发工作流改为 server_api + web 架构,旧 NiceGUI 命令降级为折叠块。

### STEP 2.5 档1+档2：功能迁移补齐（webui parity）
- **背景**：对老 NiceGUI `webui.py` 做完整功能审计后,按三档迁移(用户选全做)。本次落地档1+档2。引擎仍零改,只加 `server_api.py` 端点 + `web/` 前端。
- **后端新增端点(11 个)**：`POST /api/jobs/{id}/regen?n=`(重抽/多抽,串行 n 次、整批保持 running 避免 SSE 中途关流)、`POST /api/records/edit`(记录内二改,建追踪 job)、`POST /api/records/result/{delete,favorite}`、`POST /api/records/delete`、`GET /api/records/export/{html,pptx,favorites-pptx}`(调 records.export_* 写盘后 FileResponse 下载,按"✅ 已导出：<名>"定位文件)、`GET /api/floor/analyze`(analyze_floor_tone + 服务端用 pick_option_key 把推荐配方解析成可直接套用的 style/light/angle/aspect/res)、`GET /api/usage`(load_usage_summary)。`_job_view` 加 `has_retry` 供前端 gate 重试/重抽。
- **前端档1**：记录页大改(`records/page.tsx`)——材料搜索、房间筛选、每图 二改/删除/收藏(⭐)/备注/下载、整条删除、提示词解密弹窗、导出 HTML/PPTX/收藏夹按钮;JobCard 加候选 ‹n/N› 真左右切换(调 /result 换显示图)+ 重抽 ×1/×2/×4/×6。
- **前端档2**：地板上传自动识色(floorAnalyze→自动选色调)+ 智能配方卡一键套用;用量统计页 `/usage`;批量跨房间对话框(勾多房间→循环 createJob);完成通知(系统通知+Web Audio 提示音,`lib/notify.ts`,进页请求权限,JobCard 终态触发);队列整体进度条/计数(轮询 listJobs 聚合)。
- **被砍**：任务卡内存态收藏+⭐过滤、A/B 外部图追加归档(商业版价值低,未搬)。
- **自测**:`tsc --noEmit` 0 错、`next build` 通过(路由 / /records /usage /settings);后端 import 不拉 nicegui、41 路由。
- **待验**:真实点一遍(导出文件能开、删除收藏即时、重抽出新候选可切、识色自动选中、二改回写记录)。

### STEP 2 补齐：参数表单全量选项（用户反馈“选项少了这么多”）
- **背景**：STEP 2 首刀的 ParamsForm 只放了核心 9 个字段，用户反馈比老 NiceGUI 版少很多。
- **后端**：`/api/options` 从 `prompt_data` 扩出全量真源——地区级联 `location_map`(大洲→国家→城市)、`styles`(32)、`views`、`property_types`、`lightings`、`angles`、`floor_sizes`、`market_furniture`、`avoid_items`(AVOID_LIST)、`pet_types/actions/focus`、`aspect_ratios`、国内 `cn_developers/cities/tiers/unit_types/delivery_choices/space_features/facilities`(后两者取 dict keys)。共 29 个键。
- **前端**：`ParamsForm.tsx` 重写为**按市场/工作流条件显隐**的完整表单——海外(大洲/国家/城市级联 + 物业/房间/视野/家具风格/小区) vs 国内(交付/开发商/城市/定位/户型/空间类型/视野 + 空间特征/配套 chips)；通用(板材尺寸/拼缝/光泽/色调/风格/光线/镜头/比例/画质)；宠物友好(种类/动作/焦点)；参照模式(参考图 + 参照修正)；更多(回避清单 chips 默认全选 + 自定义补充)。多选用 chip 切换、无新增依赖；`page.tsx` 进页时按 options 初始化全部默认值。
- **未动**：风格的“按色调排序”仅做了展平(全量可选)，未复刻 tone 排序(纯展示性优化)。
- **自测**：`tsc` 0 错、`next build` 通过。

### STEP 2：Next.js 商业级前端（对接 server_api）— web/ 工程落地
- **背景**：STEP 1 后端验证通过，用户选择直接上 Next.js（已装 Node v24.16）。在本仓库 `web/` 建前端，先把主链路在浏览器跑通。
- **栈**：Next.js **16.2.9**（App Router, Turbopack 默认）+ React **19.2** + Tailwind **v4** + shadcn/ui。`npx create-next-app` 脚手架 + `shadcn init` + 加 13 个组件（button/input/select/card/tabs/slider/dialog/badge/progress/sonner/switch/textarea/label/separator）。
- **⚠️ Next 16 是“你不认识的 Next”**（scaffold 的 `web/AGENTS.md` 明确警告，已读 `node_modules/next/dist/docs/` 的 version-16 破坏性变更后再动手）。关键应对：
  - 全部页面用 `'use client'` + `useEffect` 取数 → 绕开「`params`/`searchParams` 在 16 里变 Promise」的破坏性变更。
  - 图片一律普通 `<img>`，**不用 `next/image`** → 绕开 16 对本地 IP（`127.0.0.1:7870/outputs`）的默认拦截（`dangerouslyAllowLocalIP`）。
  - `NEXT_PUBLIC_API_BASE` 走 `.env.local`；CORS 后端已放行 3000。
- **新增文件**（均在 `web/`）：`.env.local`、`src/lib/types.ts`（JobView/GenParams/ConfigView…）、`src/lib/api.ts`（全端点 typed 封装 + `imgUrl`）、`src/hooks/useJobStream.ts`（EventSource 生命周期：terminal/卸载 close、原生断连交给自动重连）、`src/components/{Nav,FloorUploader,ParamsForm,JobCard}.tsx`、`src/app/{layout,page,records/page,settings/page}.tsx`。
- **后端配套**：给 `server_api.py` 加了 `GET /api/options`（从 `prompt_data` 取 ROOM_TYPES/CN_ROOM_TYPES/FLOOR_TONES/CONTINENTS/PROPERTY_TYPES + workflow/seam/gloss/res 常量），前端下拉用引擎真源、不硬编码漂移。
- **主链路已具备**：上传地板（+最近小样）→ 参数（工作流/市场/模型/房间/画质/拼缝/光泽/色调/自定义，参照模式带参考图上传）→ ⚡生成（`POST /api/jobs` 秒回）→ 任务卡 SSE 实时阶段/耗时 → B2/Pro 双图（缩略图点开放大、候选 ‹n/N›、原图↗）→ 停止/重试/磨缝/二改。另有 `/records`（列表/load/缩略图）、`/settings`（config 读写脱敏 + 连通性自检）。
- **自测（已过）**：`tsc --noEmit` 0 错（修了 shadcn 新版 `Select.onValueChange` 现签名 `string|null` 的 3 处类型）；`next build` 成功，`/`、`/records`、`/settings` 静态预渲染通过；`node_modules`/`.next` 已被 gitignore。
- **未验（留给用户）**：浏览器端到端真实 4K（需后端 7870 + `npm run dev` 3000 并行）。运行：后端 `python -m Floor_engine_server.server_api`；前端 `cd web && npm run dev` 开 `http://localhost:3000`。
- **范围**：STEP 2 主链路 + 记录/设置页已就位；未含鉴权/多用户/计费/部署。`webui.py` 仍留作过渡基线（STEP 3 删）。
- **提交**：见本仓库 git log。

### 本仓库诞生：从 floor_engine fork 出商业版 + 新增 server_api.py（无头 FastAPI 层）— 迁出 NiceGUI STEP 1
- **背景**：用户判定 NiceGUI 不好用（四类痛点全中：调样式跟 Quasar 打架、长 4K 请求卡顿/假死、写交互逻辑别扭、想要的效果做不出），且工具定位已确认为**对外商业产品**。决定走「真前端（Next.js）+ FastAPI」路线，用绞杀者模式分三步迁移。
- **关键前提（代码勘察确认）**：全引擎里**只有 `webui.py`/`__main__.py` 依赖 nicegui**，其余 ~5300 行（api/prompts/prompt_data/records/models/config/recipes/failure_kb）完全前端无关。所以迁移成本 ≈ 重写一个 UI 层，引擎原样复用。
- **STEP 1 做了什么（本仓库 = 商业版主线）**：
  1. 从 `test/floor_engine/` **1:1 复制**出 `test/Floor_engine_server/`（剔除旧 `.git`/`__pycache__`/`.pytest_cache`），`git init` 成**独立仓库**。老 `floor_engine/` 冻结、继续日常用，两者互不影响（对策：新版为主线、老版只兜底，控制引擎双份漂移）。
  2. 新增 **`server_api.py`**（唯一新增源码，~620 行）：FastAPI 无头服务层，把引擎能力暴露为 REST + SSE。**引擎模块零改动**。
- **server_api.py 要点**：
  - **编排移植自 `webui.py` 去 UI**：`_generate_one_model`（仅删掉 `_refresh_job_card` 一行 UI 调用）、`_run_job_bg`（移植 `_run_job` 的 prep 串行 + 接缝/倒角规则 + 按模型并发 + 取消判定）、`_retry_bg`、`_edit_bg`（磨缝/二改，移植 `_polish_pro`）。
  - **必守耦合点全部移植**：`_task_prep_lock`（同小样并发抢写 png）、接缝/倒角规则（直边倒角保 B2 自己的 cpt；压制倒角挂 `get_bevel_ref_image()`）、`_is_cancelled`（单任务集合 + 全局代次计数器）、已出图先存盘再判取消（不浪费已计费）、信号量在 `lifespan` 按本事件循环创建。
  - **长 4K 请求根治**：`POST /api/jobs` 秒回 `job_id`，生图在后台 task 跑，进度走 `GET /api/jobs/{id}/stream`（SSE 每秒推 `b2_stage`/`pro_stage`）——HTTP 不再为整个 4K 生成挂起，绕开软路由 reset 老毛病。
  - **24 个端点**：jobs(提交/列表/详情/SSE/取消/cancel-all/重试/result)、polish/edit、uploads(floor/room/ref)、swatches/recent、records(列表/load/reveal)、recipes、failure/classify、connection/test、config(GET/PUT 脱敏不回传明文 key)、models、healthz；外加 `/thumb/uploads`、`/thumb/outputs` 懒缩略图（移植自 webui，复用 `_safe_output_path` 越界防护）+ `/outputs`、`/uploads` 静态挂载。
  - **路径共享**：`config.BASE_DIR = dirname(dirname(__file__))`，本包在 `test/` 下时解析到 `test/` → **与老 app 共享** `output_files`/`engine_config.json`/`_ng_uploads`/`.queue_state.json`（测试时能直接看到真实记录/小样）。日后把本仓库迁出 `test/` 即自动获得独立数据目录，无需改码。
- **自测（已过）**：`py_compile` 语法通过；`import Floor_engine_server.server_api` 成功且 **`nicegui` 未被加载**（确认 API 层零 NiceGUI 依赖）；`TestClient` 启动跑通 lifespan（恢复 6 条历史任务、信号量按 conc=1 建好），healthz/models/config/recipes/records/swatches/jobs 均 200，`job[0]` 正确序列化出 `time_text`+`/outputs` URL；坏图提交→400、未知 job→404 校验正确。
- **未验（留给用户）**：真实 4K 生成全链路（需 API key + 网络 + 人眼）——`上传→POST /api/jobs→SSE 看进度→取 4K 结果→磨缝`。运行：在 `test/` 下 `python -m Floor_engine_server.server_api`（或 `uvicorn Floor_engine_server.server_api:app --port 7870 --workers 1`，**必须单 worker**）。
- **依赖**：`requirements.txt` 显式补 `fastapi`/`uvicorn[standard]`/`python-multipart`（现由 nicegui 间接带来，STEP 3 退 nicegui 后仍可独立跑）。
- **范围边界**：STEP 1 不含 React 前端（STEP 2）、鉴权、多 worker；`webui.py` 在本仓库暂留作过渡基线，STEP 3 前端就绪后删。打包/运行由用户自己跑。
- **提交**：见本仓库 git log（独立仓库，无远程，全本地提交）。

---

## 2026-06-24

### 撤回今天全部「圆弧倒角模式」实验，回到 2026-06-23 抽卡逻辑
- **背景**：今天围绕圆弧倒角做了两轮实验——① `bevel_image_only` 纯照图开关；② `bevel_mode` 四档策略选择器（current/highlight/visible/image_only）+ `bevel_attach_ref` 子开关 + 新提示词常量 + UI 选择器。用户实测后判定**都不如昨天**，决定全部撤回，圆弧倒角回到 2026-06-23 抽卡方式。
- **撤了什么**（仅圆弧倒角；今天另做的「队列模型级流水线」「分模型状态徽章」与此无关、**保留**）：
  - `config.py`：删 `get/save_bevel_image_only`、`VALID_BEVEL_MODES`、`get/save_bevel_mode`、`get/save_bevel_attach_ref`（保留 `get_bevel_ref_image`/`BEVEL_REF_IMAGE_DEFAULT=clean_a`）。
  - `prompts.py`：import 去掉 bevel config helper；删今天新增 4 常量（`BEVEL_IMAGE_ONLY_SEAM`/`_INSTRUCTION`/`BEVEL_HIGHLIGHT_SEAM`/`BEVEL_REF_IMAGE_INSTRUCTION_TARGET`）；删早段 mode 旗标块；三处 gate 塌缩回昨天缺省——直拼 `en_seam_bevel_pro=en_seam; en_seam=PRESSED_BEVEL_B2`、人字/正方 `+=PRESSED_BEVEL_CHARACTER`、`extra_cmd_en += BEVEL_REF_IMAGE_INSTRUCTION`。`PRESSED_BEVEL_B2`/`PRESSED_BEVEL_CHARACTER`/`BEVEL_REF_IMAGE_INSTRUCTION` 保留。
  - `webui.py`：删「🧪 圆弧倒角策略」select+「附倒角参考图」checkbox+两个 handler+`_save_api_settings` 两行 save；挂图门复原为 `_bevel_ref = get_bevel_ref_image() if _is_pressed_bevel else None`；import 去掉 bevel 开关（保留 `get_bevel_ref_image`）。
  - `engine_config.json`：删 `bevel_mode`/`bevel_attach_ref` 键。
- **自测**：`py_compile`(config/prompts/webui/models)+`import`+`engine_config.json` 解析通过；全仓 grep `bevel_mode`/`bevel_image_only`/`bevel_attach_ref`/`BEVEL_HIGHLIGHT`/`BEVEL_IMAGE_ONLY`/`..._TARGET` 在 .py 里 **0 残留**；`pytest floor_engine/tests` **33 passed**（golden 不变＝输出回到基线＝昨天）；定向校验（直拼+圆弧倒角，UTF-8 脚本）：B2=可见软缝(`CLEARLY VISIBLE`)/ Pro=近无缝(`joints almost disappear`)/ 两者带 clean_a `COPY` 指令 / 无任何今天新增文字。
- **保留**：圆弧倒角参考图机制（clean_a 仍随圆弧倒角自动附）、队列模型级流水线、分模型状态徽章。
- **结论留痕**：今天的 image_only/highlight/visible 多档实验经实测均不如 2026-06-23 方案，已全部撤回（本条上方今天的几条实验记录保留作历史，勿再重提同一思路）。

### 队列卡片状态徽章：运行中改为「分模型·生成中/排队中/✓」
- **背景**：上一条「模型级流水线」后，排队任务会立刻转 job 级「⚡生成中…」，即便此刻 B2/Pro 都还在等槽（实为「排队中」），单徽章把两模型状态混成一句、看不出真实情形。用户要求徽章按模型分开显示进行中/排队中。
- **改了什么**：
  - `models.py`：新增纯函数 `running_model_status_text(job)`——运行中按模型出串：已出图(`{m}_path`)→`✓`、阶段含「排队」→`排队中`、否则→`生成中`，按 `model_filter` 过滤。例 `'B2 ✓ · Pro 生成中'`。
  - `webui.py`：`_apply_status_badge` 运行中改用 `⚡ {running_model_status_text}`（空串兜底 `⚡ 生成中…`），其余状态仍走 `_STATUS_REFRESH`；`_tick_job_times` 每秒对运行中任务顺带刷一次徽章（随「排队中→生成中→✓」实时变、设同值 no-op 无闪烁）；import 增该函数；上一条的 `_on_stage('排队中…')` 口径统一为 `'排队中'`。
- **自测**：`py_compile` + `import floor_engine.webui` 通过；新增 `tests/test_model_status.py` 6 例（both 排队/ B2✓+Pro 排队/ both 生成/ 空阶段=生成中/ 单 B2 排队/ 单 Pro ✓）；`pytest floor_engine/tests` **33 passed**（27+6）。
- **未动**：`compute_final_status`、`job_time_text`、`time_lbl`（详细实时阶段+已用秒数，与徽章互补）、卡片 DOM、占槽/流水线逻辑。
- **回滚**：还原 `_apply_status_badge` 为单徽章 + 撤掉计时器那行徽章刷新即可。

### 队列「模型级流水线」：B2/Pro 各自占槽、跑完即放（下一队列任务的同模型立刻起跑）
- **背景**：用户反馈双模型队列效率低——B2 已出图、Pro 还在跑时，下一个队列任务也起不来，必须等当前任务两个模型都完成。根因：`_run_job` 用 `async with _acquire_model_slots(run_b2, run_pro)` **一次性占住 B2+Pro 两槽、持有到整任务结束**，B2 跑完也不放槽 → 下个任务拿不到空出的 B2 槽。诉求：谁先跑完先放谁的槽，下一个任务的同名模型立刻起跑。
- **改了什么**（webui.py，改动集中在 3 个函数 + 1 把锁；并发上限不变＝仍 ≤1 B2 + ≤1 Pro 在生成，只是这两张可来自不同任务）：
  - `_generate_one_model`：在真正发起生成的 `to_thread(call_image_generate,…)` 外套**该模型自己**的信号量（`_b2_semaphore`/`_pro_semaphore`），`async with` 退出（API 一返回）即放槽、不等另一模型；`_t0` 重置进 `async with`（计时只算真正生成、不含排队）；`sem.locked()` 时打「排队中…」阶段提示。
  - `_acquire_model_slots`：改为**不获取任何槽的结构占位**（`yield` 即返回）+ docstring。三处调用点（`_run_job`/`_retry_job`/`_regen_job`）原样保留、零缩进重排；因其不再占槽，与 `_generate_one_model` 内的占槽不会双重获取自锁。retry/regen 由此自动获得「按模型占槽」，与主队列一致。
  - `_run_job`：把 `save_task_files_html` 调用 + `last_img['v']` 更新包进新的模块级 `_task_prep_lock`（惰性建于信号量同处）。原因：去掉「整任务占两槽」后多队列任务 prep 会并发，而 `save_task_files_html` 按小样路径派生 `png/json`（同张小样的任务共享同一路径），并发首次处理同图会抢写同一个 png → 串行化避免写坏（同图处理过即命中缓存秒回，开销近 0）。
- **自测**：`py_compile` + `import floor_engine.webui` 通过；`pytest floor_engine/tests` 仍 **27 passed**（未碰 prompts/golden）；独立 async harness（复刻「按模型信号量 + gather + prep 锁」、fake 生成=不同时长 sleep）断言全绿——① 任意时刻 ≤1 B2、≤1 Pro；② **任务2 的 B2 在任务1 的 Pro 仍在跑时(@0.12s vs 0.52s)就已开跑**（解耦成立）；③ 同槽 FIFO；并跑旧『整任务占两槽』模型作对照，证实旧逻辑下 job2.B2 被卡到 job1.Pro 之后（@0.54s）。
- **行为变化**：队列里后续任务会**立刻转「生成中」**并在等槽时显示「排队中…」，而非停在「排队」直到轮到自己；底层生成并发上限不变。磨缝（仅 Pro，直接用 `_pro_semaphore`）与 Pro 生成仍互斥。
- **未动**：`compute_final_status`、`_refresh_job_card`、job 状态字段、`max_concurrent_per_model` 语义、Fal 失败转线、磨缝、prompts/golden。
- **回滚**：把 `_acquire_model_slots` 还原为原「获取两槽」实现、撤掉 `_generate_one_model` 内 per-model `async with` 与 `_run_job` 的 prep 锁即可（本机非 git，改动集中易还原）。

### 圆弧倒角『策略选择器』（四档可切 + 附参考图子开关）+ 纯照图实测更差结论存档
- **背景 / 测试结论存档**：用户实测上一版 `image_only`（纯照图、删全部文字只让模型照 clean_a 推算）**比现状更差**。本轮亲眼打开 `assets/bevel_ref_clean_a.jpg` 复盘：**这张实拍本身就是近无缝、淡高光软缝的浅橡木，并没有明显 U 形凹槽**——所以它是好「目标样例」、坏「照抄指令」，纯图模式删掉文字后模型没有清晰规则可抄、只能瞎猜 → 更差。再通读片段发现更深的根因：**一直在用「暗缝/凹槽阴影」当驱动线索**（B2 `10-15% darker` 暗槽、Pro `joints almost disappear`、ref 指令「copy U-groove」），而真目标（memory `floor-ai-rounded-bevel`：无缝底 + 板边**凸圆弧**、接缝近隐形**以高光为主**、对标 Coretec）恰恰相反。双峰 = 同一条暗线线索过强（黑线）/ 缺席（消失）。用户决策：**两种缝相都做成档位**自己抽批对比、**参考图挂/不挂做成子开关**两种都测；这版不 push。
- **改了什么**（全部 opt-in，默认 `current` = 现状逐字节不变）：
  - `config.py`：新增 `get/save_bevel_mode`（枚举 `current/highlight/visible/image_only`，**旧 `bevel_image_only:true` 自动迁移为 `image_only`**；`save` 时写 `bevel_mode` 并清掉旧键＝单一真相源；无效值回退 `current`）与 `get/save_bevel_attach_ref`（缺省 True）。旧 `get/save_bevel_image_only` 保留供迁移期兼容。
  - `prompts.py`：① 新增 `BEVEL_HIGHLIGHT_SEAM`（把线索从「暗槽」翻成「凸圆弧板边 + 一条淡高光」+ **双护栏**：决不是暗线/凹槽阴影、又决不彻底消失永远留一条淡高光）与 `BEVEL_REF_IMAGE_INSTRUCTION_TARGET`（把图从「照抄 U 槽」改成「成品范例 / 可见度上限——缝不得比它更深更重」）。② 早段把布尔 `bevel_image_only` 换成读 `bevel_mode`（`image_only` 缺图仍降级回 `current`）。③ gate 直拼分流：`highlight`→`BEVEL_HIGHLIGHT_SEAM`、`visible`→`PRESSED_BEVEL_B2`、`image_only`→`BEVEL_IMAGE_ONLY_SEAM`，三档均 `en_seam_bevel_pro=None` → **B2==Pro**（铲掉「B2 可见 / Pro 隐形」的双峰根源）；`current` 分支逐字不变。④ gate 指令：`current`→旧 COPY、`image_only`→DERIVE、`highlight/visible`→`attach` 为真才挂 TARGET-LOOK、否则不加任何参考图文字。
  - `webui.py`：把原单个 `🧪 照图推算` checkbox 换成 **一个 `🧪 圆弧倒角策略` select(4 档) + 一个 `附倒角参考图` checkbox**；都即选即存 + notify，并进 💾 `_save_api_settings`；旧 `bevel_image_only` 值迁移为 select 初值；生成处 `_bevel_ref` 挂图按 **同一 mode+attach 条件**门控（`current/image_only` 必挂、`highlight/visible` 看子开关），与 prompts 侧文字指令同步。
- **自测**：`py_compile`(config/prompts/webui) + `import` 通过；`pytest floor_engine/tests` 仍 **27 passed**（默认 current 零变化）；参数化脚本（直拼+圆弧倒角，monkeypatch 四档×attach，离线隔离）全绿——`current`=现状（B2软缝/Pro近无缝/旧COPY）、`highlight`=凸圆弧高光双护栏 B2==Pro+TARGET、`highlight 无图`=高光词在但无任何参考图指令、`visible`=B2==Pro可见软缝(Pro 去近无缝)+TARGET、`image_only`=照图句+DERIVE B2==Pro；config 往返脚本（指向临时配置、**不碰真实 engine_config.json**）：缺省 current/attach True、旧键迁移 image_only、save 清旧键、无效回退、其它键不丢。
- **未动 / 现实预期**：常规倒角缝 / 无缝路径、人字-正方拼的几何排布、参考图挂载机制其余部分；**默认 current = 生产零变化**。诚实预期：薄高频缝在生图模型里本就有随机性，提示词移动分布、未必消灭坏抽卡 → 仍要配合「多抽一批挑好的」，不做重型二次 pass。
- **怎么用 / 回滚**：UI「🔑 API 设置」里把 `🧪 圆弧倒角策略` 选 **近隐形·高光** 或 **可见软细缝**、按需勾/取消 `附倒角参考图`，选 **直拼+圆弧倒角** B2/Pro 各抽一批对比；回滚＝策略选回 **现行**（或删 `engine_config.json` 的 `bevel_mode` 键）即恢复现状。**注意**：当前 `engine_config.json` 仍留着旧 `bevel_image_only:true`，会被迁移成「纯照图推算」档——首次打开下拉框记得切到「近隐形·高光」再测。

### 把『照图推算』开关接到 UI（API 设置面板勾选框，即选即存·无需重启）
- **背景**：上一条的 `bevel_image_only` 只能改 `engine_config.json` + 重启。用户要在界面里直接点开/关，方便自己边点边测对比。
- **改了什么**：
  - `config.py`：加 `save_bevel_image_only(enabled)`（照 `save_auto_failover` 写盘）。
  - `webui.py`：「🔑 API 设置」expansion 里、`auto_failover` 行之后加 `ui.checkbox('🧪 圆弧倒角·照图推算（测试）')`，初值读 `_cfg0.get('bevel_image_only', False)`，带 tooltip；`on_value_change` **即选即存 + notify**，并并进 💾 `_save_api_settings`；import 增 `save_bevel_image_only`。
  - **无需重启**：`get_bevel_image_only()` 每次生成实时 `_load_config()` 读盘，UI 写盘后**下次生成即生效**。
- **自测**：`py_compile` + `import floor_engine.webui` 通过；持久化往返脚本（指向临时配置、**不碰真实 engine_config.json**）：缺省 False、save(True)→读回 True、save(False)→False、已有键不丢；`pytest floor_engine/tests` 仍 **27 passed**。
- **未验**：浏览器里勾选框的实际渲染/点击（代码与 `auto_failover` 行逐行对齐、import 已解析整个 `main_page` 闭包，渲染近乎必然）——留待用户启动应用时确认。
- **未动**：后端照图推算逻辑、默认仍关、其它设置项。

### 圆弧倒角『照图推算』测试版（opt-in 开关 `bevel_image_only`，默认关）
- **背景**：用户反馈圆弧倒角·**直拼**成功率仍低，失败呈**双峰**（缝忽被压成生硬黑线、忽直接消失，B2/Pro 都中）。排查现行倒角提示词：几段文字**自相矛盾**——Pro 直拼用 `TECH_DICT`「near-seamless / joints almost disappear」（要隐形）、B2 直拼用 `PRESSED_BEVEL_B2`「CLEARLY VISIBLE soft groove」（要可见）、同时两者都挂 `BEVEL_REF_IMAGE_INSTRUCTION` 让模型抄一张「可见 U 形凹槽」实拍。「要隐形」与「要可见凹槽」对拉 → 模型在两极漂移 = 双峰。用户决策：做一个**可开关的测试版**，把倒角文字整段删掉、强制照内置参考图(clean_a)推算边形；不行就删开关键回滚（**项目当前无 git，靠开关而非 revert**）。
- **改了什么**：
  - `config.py`：新增 `get_bevel_image_only()`（读 `engine_config.json` 的 `bevel_image_only`，缺省 False；沿用 ②temperature ③ref-image 的 opt-in 套路）。
  - `prompts.py`：① 新增 `BEVEL_IMAGE_ONLY_SEAM` / `BEVEL_IMAGE_ONLY_INSTRUCTION` 两常量——删一切文字几何描述，只留「参考图=唯一权威、照它推算边形」+ 两道护栏（别打成黑线/别消失=就照图；颜色/木纹/光线只取小样不取参考图）。② `save_task_files_html` 早段算 `bevel_image_only`（开关开且 `get_bevel_ref_image()` 非空才为真；**开了但无参考图 → `logger.warning` 降级回纯文字**，避免空描述）。③ gate 三处：`PRESSED_BEVEL_CHARACTER` 追加在照图模式**跳过**（人字/正方的几何排布描述仍保留）；直拼 B2/Pro 分流改为照图时**统一** `en_seam=BEVEL_IMAGE_ONLY_SEAM`、`en_seam_bevel_pro=None`（B2==Pro）；`extra_cmd_en` 的指令在照图模式换成 `BEVEL_IMAGE_ONLY_INSTRUCTION`。
  - 仅改 `en_seam` / `en_seam_bevel_pro` / `extra_cmd_en` 三个**值**，不动四套工作流拼装结构与下游。
- **自测**：① `py_compile` + `import floor_engine.prompts` 通过；② `pytest floor_engine/tests` **27 passed**（开关默认关 → golden 逐字零变化）；③ 手写双向验证脚本（直拼+圆弧倒角，离线隔离）：**OFF**=现状逐字不变（B2 含 `CLEARLY VISIBLE`、Pro 含 `joints almost disappear`、旧指令在、新指令不在）；**ON**=照图推算（新指令在、`CLEARLY VISIBLE`/`joints almost disappear`/旧指令全消失、B2==Pro 缝统一、颜色护栏在）。
- **未动**：常规倒角缝 / 无缝路径、人字/正方拼的几何排布描述、B2/Pro 其余差异、参考图挂载机制（webui/api 不变）。**开关默认关 = 生产行为零变化**。
- **怎么用 / 回滚**：`engine_config.json` 加 `"bevel_image_only": true` → 重启 `python -m floor_engine` → 测；删该键 → 立即回滚。示例图用现行内置 `clean_a`（用户拍板，本轮不换）；换图走 `bevel_ref_image` 键。
- **待用户测**：直拼+圆弧倒角，B2/Pro 各（建议配合队列 ×6 多抽）跑一批，看缝是否更稳（不再忽黑忽无）、是否贴合 clean_a 边形；结论回来再决定保留/微调/回滚。

## 2026-06-23

### 智能配方不再覆盖板材尺寸/拼缝/光泽度，只填「风格与画面」区
- **背景**：智能配方一键套用时会一次改 8 个选择器，其中包含「🪵 板材规格」区的**板材尺寸/拼缝/光泽度**。但这三项是「跟着地板小样一起定」的物理属性，应由用户手动控制；配方套用会把用户设好的值冲掉。用户要求配方只填「🎨 风格与画面」整区（风格/光线/镜头/比例/画质），板材三项不再被配方动（已确认范围＝整个风格与画面区）。
- **改了什么**（`webui.py` `_apply_recipe`）：删掉 `_set(floor_size_sel, r['size_kw'])`/`_set(seam_sel, r['seam_kw'])`/`_set(gloss_sel, r['gloss_kw'])` 三行（经 grep 确认这三个选择器全文别无 `.value=` 赋值，配方是唯一来源）。保留 light/angle/aspect/res/style 五项不变。结尾 `_render_steps/_render_checklist` 不读这三个 kw，故不受影响。
- **未动**：`recipes.py` 的 `STYLE_LOOK_PROFILE` 仍保留 `size_kw/seam_kw/gloss_kw` 字段（数据无害、不再被应用，删了反而牵动 `_make_recipe`）。配方卡副标题可能仍提到板材观感（如「浅木宽板」），但不再自动改板材规格——与诉求一致。
- **自测**：`py_compile` + `import floor_engine.webui` 均通过。

### 并发收紧到「每模型≤1 进行中任务」+ 磨缝并入 Pro 候选切换、去掉空白框
- **背景**：用户长期使用发现「一次只生成 2 张图最稳，任务多了容易出事」（4K 长请求并发一多就被公司软路由重置，见记忆 floor-ai-network-env）。旧实现是**全局信号量** `max_concurrent_jobs`（代码默认 5，活跃 `engine_config.json` 无此键→实跑 5），最坏 5 任务×2 模型=10 个并发 4K 请求，正是出事根源。另外队列卡上「Pro 磨缝」是个独立图框，没磨缝时一直空着占地方，用户要求去掉、把磨缝图改成像重抽那样在 Pro 槽内切换。
- **改了什么**：
  - **① 并发：全局信号量 → 每模型信号量（各 1）**（`webui.py`）。删 `_gen_semaphore`，换成 `_b2_semaphore`/`_pro_semaphore`（懒初始化读新键 `max_concurrent_per_model`，默认 1；旧键 `max_concurrent_jobs` 作废）。新增 `@asynccontextmanager _acquire_model_slots(run_b2, run_pro)`——按任务要跑的模型取对应槽，**固定顺序 B2→Pro**（一致加锁序，无死锁）。三处生成入口 `_run_job`/`_retry_job`/`_regen_job` 的 `async with _gen_semaphore:` 全改成按需取模型槽（`_run_job` 把 `run_b2/run_pro` 上移到函数早段算、删内部重复计算）。**效果**：双模型任务同时占两槽 → 主任务串行（一次只跑 2 张）；单 B2 与单 Pro 因占不同槽可并行（仍 ≤2 张），这是「按模型各限 1」相对「全局 1」的唯一收益。
  - **② 磨缝也排队**（`webui.py _polish_pro`）。原本磨缝后台直跑、不受并发限制；现把实际 `call_gemini_edit` 调用包进 `async with _pro_semaphore:`——有任务在跑时，点磨缝先显示「磨缝中…」并排队等空出 Pro 槽再发请求。
  - **③ 磨缝并入 Pro 重抽切换 + 去掉空白框**。`models.py`：`CANDIDATE_SLOTS` 去掉 `'pro_polish'`→`('b2','pro')`；`ensure_candidate_lists` 加幂等迁移，把老记录遗留的 `pro_polish_paths`/`pro_polish_path` 折进 `pro_paths` 后清空（dataclass 字段保留供老 JSON 反序列化）。`webui.py`：`_polish_pro` 出口 `add_candidate(job,'pro_polish',...)`→`add_candidate(job,'pro',...)`（磨缝图成为最新 Pro 候选、自动选中，‹n/N› 可切回原 Pro 对比）；建卡删掉「Pro 磨缝」独立图框（`_img_box('Pro 磨缝',...)`）及其全部 refs（`pro_polish_img/_dl/_nav/_prev/_cnt/_next`），只留 🪄 磨缝按钮；`_apply_images` 删磨缝展示块、按钮显隐简化为「有 Pro 图即显示，磨缝中显示『磨缝中…』」（不再因 `pro_polish_path` 隐藏，可对当前 Pro 候选反复磨）；`_polish_pro` 守卫去掉 `or job.pro_polish_path`。
- **自测**：① `py_compile` + `import floor_engine.models/webui` 均通过，`CANDIDATE_SLOTS=('b2','pro')`；② `pytest floor_engine/tests` **27 passed**（未触碰提示词逻辑）；③ 迁移单测：老 `pro_polish_*` 正确折进 `pro_paths`、二次调用幂等不重复、全新记录 no-op、`add_candidate(job,'pro',...)` 追加跳最新。
- **未动**：提示词生成/golden、记录持久化字段集（`pro_polish_*` 字段保留）、磨缝的去缝指令与校色算法、重抽/多抽逻辑。用户决策：并发=「按模型各限 1」、磨缝「也排队」。打包/构建归用户自己跑。阈值可调：`max_concurrent_per_model`（默认 1）。

### 内存优化三连:结果图走缩略图 + 磨缝校色峰值砍半 + 卡片/候选自动收口
- **背景**：用户反馈"使用时非常消耗内存"。排查定位到三个账户:① 浏览器把卡片/记录里的结果图按**全分辨率 4K** 解码(每张 ~67MB 位图,一屏多张就爆);② 磨缝校色 `_match_color_to_reference` 同时持有 3 份 4K float32(~576MB)瞬时峰值;③ `_job_history`/`_job_ui`/候选列表**只增不减**(只有手动「清除已完成」才释放),越用越涨。4K 解码本身是硬成本动不了,但这三处都低风险可改。
- **改了什么**：
  - **① 结果图缩略图**(`webui.py`)：新增 `/thumb/outputs/{relpath:path}` 端点(镜像已有 `/thumb/uploads`,`draft()` 解码即降采样、磁盘缓存、`_safe_output_path` 挡越界)+ helper `_result_thumb_url`。卡片三图(`_apply_images` 的 b2/pro/pro_polish)与记录视图 `_render_result_box` 的显示源改走缩略图(`s=480`),**点击放大 `_open_zoom`/下载链接仍用原图**。浏览器不再为列表解 4K。
  - **② 磨缝校色峰值**(`api.py:_match_color_to_reference`)：ref 只用于取每通道均值/方差,改为保持 uint8(4K≈50MB 而非 float32 的 192MB)、`mean()/std()` 走 float64 精确归约;src 用可写 float32 副本**原地变换 + 原地 `np.clip(out=s)`**,省掉 `out` 数组。峰值 ~576MB→~292MB。**实测:真实用例(ref 与 src 同尺寸)逐像素 diff=0,与原算法字节一致**,保真零损失。
  - **③ 自动收口**:`models.add_candidate` 加 `MAX_CANDIDATES_PER_SLOT=12`(超出丢最旧,磁盘文件不动);`webui.py` 加 `_MAX_RESIDENT_CARDS=30` + 模块级 `_auto_trim_cards()`(复用 `_clear_done` 删卡步骤,只删最旧的「已完成」卡,running/pending 永不删),在 `_validate_and_submit` 创建新卡后调用(那里有 client 上下文,`card.delete()` 安全);会话恢复循环只重建最新 30 张卡(更旧的记录管理仍可查)。
- **自测**:① `import floor_engine.webui` 通过、`pytest` 全套 **27 passed**;② 候选上限实测追加 20 张→稳定保留最新 12 张、idx 正确;③ 校色同尺寸用例 diff=0(噪声子采样最坏情形也已弃用,改 uint8 精确统计);④ `_result_thumb_url` 对 output 内路径生成 `/thumb/outputs/...?s=480`、越界路径返回空串。
- **未动**:4K 生成/解码的固有成本、`_match_color_to_reference` 的算法本身、卡片 UI 结构。`_MAX_RESIDENT_CARDS=30`/`MAX_CANDIDATES_PER_SLOT=12` 两个阈值如体感不合适可直接调常量。

### 预设选项补进词典:宠物品种 25 个(让在线翻译只兜手动输入)
- **背景**：承上一条 #5,用户要"把所有预设选项内容补进词典,翻译系统只对手动输入的文字翻译"。逐一核对所有 `translate_zh_to_en` 调用点后发现:**绝大多数选项早已被覆盖**——房间(ROOM_TYPES/CN_ROOM_TYPES 全在 `TECH_DICT`)、景观(VIEWS 在 `FALLBACK_DICT`)、城市/国家(LOCATION_MAP 全量已在 `FALLBACK_DICT`)、CN 城市(=CN_CITIES 8 城已覆盖)、地板规格/光泽/拼缝(`gloss_sel`/`seam_sel` 选项与 `TECH_DICT` key 逐字一致)、风格(STYLES 靠 `extract_en` 取括号英文)、CN 开发商/户型/档次/交付/空间/设施(各有专属 `_MAP`)、灯光/色调/家具市场/避免项(走英文 key 或关键词匹配,不经翻译)。**唯一真缺口 = PET_TYPES**——`extract_zh("🐶 金毛犬")="金毛犬"` 这些品种名一个都不在字典里,代理重置时就会裸中文进图。
- **改了什么**(`prompt_data.py` `FALLBACK_DICT`)：补进 25 个宠物品种(key 用 `extract_zh` 提取后的精确形式,含 `贵宾犬/泰迪` 带斜杠那条;猫类英文带 "cat" 帮模型区分,如 `布偶猫→Ragdoll cat`、`加菲猫→Exotic Shorthair cat`;狗类品种名本身无歧义不加 "dog")。`translate_zh_to_en` 本就先查 `TECH_DICT`/`FALLBACK_DICT` 再联网,补完后**所有预设选项查表命中,在线翻译只剩"自定义补充"等真手动输入会走**,正是用户要的效果。
- **自测**：① pet golden 如期 diff `A cute 金毛犬→A cute Golden Retriever`(仅此一处),`UPDATE_GOLDEN=1` 刷新 pet 基准;② 全套 **27 passed**;③ `translate_zh_to_en(extract_zh(...))` 验证 `金毛犬/贵宾犬泰迪/加菲猫/宠物兔/鹦鹉` → Golden Retriever / Poodle (Teddy cut) / Exotic Shorthair cat / pet rabbit / parrot,查表命中无联网。
- **没动**：翻译函数逻辑、所有选项列表、各 `_MAP`。仅 `FALLBACK_DICT` 增条目。

### 缺陷清单评估 + 三项小修(翻译兜底去括号 / 路径越界校验 / 琐碎清理)
- **背景**：用户给了一份 14 条缺陷清单要我评估"哪些值得做"。逐条落代码核实后结论：大半是把通用工程规范硬套到**单用户本机自用工具**上并不成立(全局状态是有意设计、JSON 已有按文件锁+原子写、并发已有 Semaphore+重抽串行避 429、字典外置成 JSON 反而自废 git-diff 追踪)；清单里 **#14 pet 黄金基准过期已在 06-22 `7769de0` 修掉、#15 replace 漂移是误报**(chevron 与 SEAMLESS_NEGATIVE 一致)。用户最终选定 3 项实修,#8/#9 大文件拆分本轮只交底功能逻辑、留待后续单独规划。
- **改了什么**：
  - **#5 翻译失败兜底**(`prompt_data.py` `translate_zh_to_en`)：彻底失败的返回从 `f"[{text}]"` 改为裸 `text`。翻译与生图走同一套软路由/代理,代理重置时会失败,带方括号的 `[金毛犬]`/`[原木色]` 进 Gemini 被当格式/指令噪音,比裸 CJK 更糟。这是唯一直接打到出图质量的真 bug。
  - **#12 重复 import**(`records.py`)：删 `import base64`(与 `import base64 as b64mod` 重复),`_obfuscate`/`_deobfuscate` 两处 `base64.`→`b64mod.`,全文统一一个别名。
  - **#10 启动 print**(`config.py`)：删模块顶层 `print("--- 系统启动 ---")` 噪音；缺 deep-translator 的 ImportError 告警 print 保留。
  - **#6 路径越界校验**(`records.py`)：新增 `_safe_output_path(rel)`(realpath+commonpath 锚定 MAIN_OUTPUT_DIR,跨盘符 ValueError/OSError 兜底 None),替换 `_result_image_source` 与 `_html_b64` 里"join+exists"裸拼接。威胁≈0(只防手改/历史污染 JSON),但便宜。
- **自测**：① 初次 `pytest floor_engine/tests/` 2 failed(pure/pet golden)——**diff 纯是 `[原木色]→原木色`、`[金毛犬]→金毛犬` 去括号**(旧基准等于在认证带括号的脏输出),核对确认是 #5 预期变化后 `UPDATE_GOLDEN=1` 刷新这两个基准;② 复跑全套 **27 passed**;③ `python -c "import records,config,prompt_data"` 通过,`_safe_output_path('../../etc/passwd')`/`''` 均返回 None。
- **未动/待续**：#8 `webui.py`(~2450 行,几乎全在 `main_page()`,深层闭包)、#9 `prompt_data.py`(1445 行,数据字典+翻译+numpy `analyze_floor_tone` 混居)只在计划里交底了功能逻辑,**拆分方案待用户后续单独规划**(直觉:翻译→`translation.py`、numpy 分析→`floor_analysis.py`、纯数据留 `prompt_data.py`)。仓库无远程,未 push。

---

## 2026-06-22

### 工作台左栏 UI 重排（方案 A · 顺手重排）：板材规格置顶 + 客户与地区提升蓝卡
- **背景**：Claude Design 出了增量设计 `地板AI引擎-顺手方案对比.dc.html`(方案 A·顺手重排)。基础「重排」(吸底操作栏/
  模型分段+单生成/核心参数出折叠/市场分段/暖陶米色主题+各 CSS) **已在 webui.py 落地**；本次只动左栏区块顺序与卡片层级，
  **零业务逻辑改动**。来源 claude.ai/design 项目 Floor_engine(MCP `DesignSync` 连通)。
- **与用户确认**：贴合度=核心结构+客户与地区卡内字段重排；房间类型保留在市场卡内(不动两套选项逻辑)；不做工作流固定顶部。
- **改了什么**(`webui.py`，左栏 `main_page` 滚动区)：
  - **板材规格置顶**：`floor_size_sel`/`seam_sel`/`gloss_sel` 从 `⚙️更多` 折叠移出，新建暖卡 `spec_card`(class `.spec-card`，
    header「🪵 板材规格 · 跟着地板小样一起定」)置于地板素材图正下方。
  - **客户与地区提升**：整段市场分段(`_market_tabs`/`_market_panels`/两 tab)上移到「风格与画面」之前，外包蓝卡 `region_card`
    (class `.region-card`，header「🌍 客户与地区 · 决定建筑/家具风格」)。海外 tab 内字段重排：大洲/国家/城市提到最前并加
    `.region-primary`(蓝描边强调)，房间/物业/视野/家具+小区降为其后；国内 tab 内部维持原序。
  - **改名**：`③ 场景参数`→`🎨 风格与画面 · 配方已填好`；`② ✨ 智能配方`→`✨ 智能配方`；`⚙️ 更多参数`→`⚙️ 更多 · 避免清单 / 自定义补充`(其内只剩避免勾选+自定义)。
  - **CSS**：design style block 新增 `.spec-card`(#FBF6EF/#EAD9C4)、`.region-card`(#EEF2F5/#D8E2EA)、`.region-primary .q-field__control:before{border-color:#BBD2E0}`，仿现有 `.ctx-*`。
  - **闸门**：`_set_left_gate` 改为 `spec_card.visible`/`region_card.visible`(替代旧 `_market_tabs/_market_panels.visible`)，保持地板上传后才展开。
- **没动**：右侧队列/记录管理/批量/磨缝/重试/`_run_job`/`_generate_one_model`/`_on_mode`/`_apply_recipe`/所有 select 的 options 与默认值、
  生成链路读取的字段变量名(`room_type_sel`/`cn_room_type_sel`/...)。房间类型仍在市场 tab；工作流未固定顶部。
- **自测**：① `python -c import floor_engine.webui` 通过；② `pytest floor_engine/tests/` 26 passed(UI 不碰提示词；`pet` 历史遗留失败，与本次无关)；
  ③ 临时无浏览器起服务(7895)+临时打开闸门，`GET /` **200 / 164KB**——完整(含全部重排区块)渲染路径无构建异常；Edge 无头截图在本机环境产不出文件(JS 应用+Edge --screenshot 限制)，已回退为「200 构建校验+代码核对」，临时改动(闸门/runner)已全部还原清理。
- **待办**：左栏视觉是否完全贴设计稿(板材卡在地板下、客户与地区蓝卡在风格之上、海外大洲/国家/城市蓝描边在前、更多只剩避免/自定义)，
  由用户在本地实例传一张小样后肉眼确认；确认后再提交(仓库无远程，未 push)。

### 参照模式重做：参照图直接喂模型(图+文混合) + 可选国内/海外语境 + 收紧地板可见度
- **背景/根因**：用户反馈参照模式"很怪、出图很多没法用"。排查确认：**参照模式从不把参照图喂给生图模型**——
  先用视觉模型把参照照片转成 9 个文字字段(`analyze_style_image`)，再把这段文字塞进 prompt，生图模型只拿到
  「地板小样图+文字」，**从没"看见"过参照图**(`webui.py` 旧 `_sref_api=None`)。而 `call_gemini_generate`/`call_fal_generate`
  本就支持 `style_ref_image_path`(双 provider 对称、地板小样仍排最后)，通道现成却没用。加上分析提示词硬往高端国际设计词
  上靠(ANCHOR "generic hotel lobby"、品牌 Vitsœ/HAY/String/MUJI)、参照分支同时丢了海外实拍层和国内语境(两头不靠)、
  地板可见度最松——共同导致风格跑偏/构图比例怪/像样板间·CGI/地板看不清。
- **用户拍板方向**：① 图+文混合(喂图，文字降级为辅助强调)；② 语境可选(遵循现有全局市场开关 `_is_cn`)；③ 修上述四痛点。
- **改了什么**：
  - `webui.py`（核心一行）：参照模式 `_sref_api = (ref_path['v'] or None)`，把参照图直接当 sref 喂模型；仍 `rp=None`。
    `retry_ctx` 已存 `sref`，重试/重抽自动重喂(无需改码)。
  - `prompts.py` 参照分支(672-753 整段重写)：① 新 header `[REFERENCE IMAGE …]`——声明附图是"风格/材质/配色/氛围参照、
    非布局照搬"，要求重构图 + 可信住宅比例(修构图比例怪)；② 9 字段原文 dump 降级为精简 `[STYLE SUMMARY]`(signature/
    materials/mood/avoid 各一行；无标准字段时回退附原文不丢信息)，删去原来重复的 mandatory/materials/prohibitions/mood
    独立块；③ 按 `cn_mode` 注入语境：海外→`en_overseas_realism`(强抗样板间/CGI)，国内→`en_cn_context`；参照图的
    `REALISM_CUES` 仅在国内语境下补进来(海外层已含写实，避免重复)；④ 地板可见度从宽松"Floor Visibility(允许遮挡)"
    换成普通模式同款严格 `en_floor_visibility`(40-50% 覆盖)，删 `en_floor_visibility_ref`。保真尾巴(_floor_spec_block/
    CORE_MATERIAL_INSTRUCTION/FLOOR COLOR)逐字不动。
  - `api.py`：① 分析提示词改"**如实描述照片本身**(含国内普通装修)、不拔高不套高端词/品牌/流派"，精简到实际消费的 5 字段
    (SIGNATURE/MATERIALS/MOOD/REALISM_CUES/PROHIBITIONS)；② `_STYLE_CACHE_VERSION` v1→**v2**，作废旧偏置缓存。
- **没动**：地板 4K 保真路径(走 `pnp`/`image_path`，与 sref 独立)；`last_img` 取地板小样不取参照图；其它 3 套工作流；
  `build_overseas_realism_layer` 原样复用。
- **自测**：① golden——`ref` 用例按预期破基准，人工核对 diff 只有参照分支的 header/语境层/地板可见度变、其它 3 套+保真尾巴
  逐字不变后，`UPDATE_GOLDEN=1 pytest -k ref` **只刷新 ref 基准**(未动 pet)，全套回到 **26 passed**。② 代码层冒烟：用带标准
  字段的分析文本，海外/国内各生成一次——海外含 `[Overseas Real-Home Layer]`、国内含 `[Chinese Domestic Market Context]`
  +参照写实线，两者均 40-50% 地板覆盖、保真尾巴完整，结构符合预期。
- **遗留(非本次)**：`test_prompt_golden[pet]` 仍失败——是之前改 `prompt_data.py` 海外实拍层文案("House/villa realism"→
  "House realism")没重生成 pet golden，**与本次无关**，待用户确认后单独 `UPDATE_GOLDEN` 刷新。
- **待办**：真实计费冒烟(传参照图+地板小样，海外/国内各一张，看日志 `style_ref=True` 确认图真发出去 + 肉眼核四痛点)由用户本地确认后再提交。

### 在线翻译后端 MyMemory → GoogleTranslator，并让翻译走本地代理
- **问题**：日志频繁 `翻译第N次失败: SSLError(SSLEOFError ... UNEXPECTED_EOF_WHILE_READING ... EOF occurred in violation of protocol)`，打的是 `api.mymemory.translated.net`。根因三叠加：① MyMemory 是**国外端点**，4K 之外的普通国外长连接也会被软路由(透明代理) reset；② 它由 deep-translator 自己发请求、**完全不吃我们给生图调好的代理/TLS 配置**，比生图还脆；③ 匿名免费额度有限、重试只 2 次。失败兜底返回 `[现代别墅]` 这种带括号中文塞进 prompt（本次挂的就是「现代别墅」）。
- **改了什么**（用户定方案：换 GoogleTranslator + 强制走本地代理）：
  - `config.py`：探测导入 `MyMemoryTranslator` → `GoogleTranslator`（`TRANSLATOR_AVAILABLE` 语义不变）；新增公共读取器 `get_proxy()`（读 `engine_config.json` 的 `proxy` 字段、`.strip()`，与生图侧 `cfg.get("proxy","").strip()` 同一个值）。
  - `prompt_data.py`：`translate_zh_to_en` 在线路径改用 `GoogleTranslator(source='zh-CN', target='en', proxies=proxies)`（**注意 Google 用 `en` 不是 MyMemory 的 `en-US`**）。`proxies = {"http": p, "https": p} if p else None` —— **配了本地代理就强制走它，没配则 `None`=默认走软路由**，与 `api.py` 生图侧逐字一致。删掉 `_translator` 全局缓存：GoogleTranslator 构造极廉（仅存参数、无网络），改成每次按当前代理新建，让改代理即时生效；真正省钱的结果缓存 `_translation_cache` 保留。
- **没动**：`TECH_DICT`/`FALLBACK_DICT`/`PROPERTY_TYPE_DICT` 离线词典优先级、`all(ord(c)<128)` 的 ASCII 校验、失败兜底 `[text]` 行为、函数签名——故 golden/离线测试输出逐字不变。旧单体 `app_27/28*.py` 不在活跃范围，未动。
- **自测**：① `pytest floor_engine/tests/` → 26 passed；唯一 1 failed 是 `test_prompt_golden[pet]`，diff 是 prompt 静态英文文案（`prompt_data.py:1254` 现产 "House realism…" 但 golden 仍是旧 "House/villa realism…"）——**改前就过期、与本次翻译无关**，待单独重生成 pet golden。② 真实在线冒烟（本机 `proxy=http://127.0.0.1:7897/`）：现代别墅→modern villa、原木色→Wood color、北欧风格的简约客厅→Nordic style simple living room、靠窗的飘窗读书角→…by the window，全成功、缓存命中正常。
- **状态**：代码改完、自测通过；UI 端到端由用户本地确认后再提交（仓库无远程）。

### 浏览器标签图标换成新设计的 Logo（favicon）

- **需求**：用户给了新设计 `地板AI引擎-Logo.svg`（512×512 陶土转角空间 + 米底 + 火花），要换掉浏览器标签页图标。
- **改了什么**：① SVG 拷进包内资产、用 **ASCII 文件名** `floor_engine/assets/logo.svg`（避开非 ASCII 路径坑）；② `config.py` 加常量 `FAVICON_PATH = _PKG_DIR/assets/logo.svg`（仿 `BEVEL_REF_IMAGE_DEFAULT`）；③ 两个入口 `floor_engine/__main__.py`（`python -m floor_engine`）与 `dev_floor.py`（热重载）的 `ui.run(...)` 都加 `favicon=FAVICON_PATH if os.path.exists(...) else None`。
- **机制**：NiceGUI 把文件路径 favicon 挂到 `/favicon.ico` 路由、并在页面 `<head>` 注入 `<link rel="shortcut icon" href="/favicon.ico">`；SVG 现代浏览器原生支持。
- **自测**：无头起服务 → `GET /` 头部含该 `<link>`；`GET /favicon.ico` → **200 + `content-type: image/svg+xml`**、body 即该 Logo SVG；Edge 渲染确认陶土图形非空白。临时自检脚本已删。
- **没动**：应用内左上角 `🪵 地板 AI 提示词引擎` 文字标题（用户只要标签图标）；其余逻辑。

### 风格提示词中性化 + 配方按地板色重做（联网搜索佐证）

- **背景/需求**（紧接 v7 试用反馈，两件事）：① 智能配方"不够好"——要**按现有地板色调**出方案、并**联网搜索更好的搭配**；② 很多风格出图"像国内风格"，要"认真检查、把风格提示词中性化"。
- **排查（两个独立根因，用户确认"文案+脚手架都改"）**：
  - **风格文案泄漏（小，仅 3 个）**：`STYLE_ATMOSPHERE_MAP` 里只有 3 个本该国际化的风格夹带中文/国内卖场黑词——Raw Wood 的 `磁吸轨道灯/吊顶/"China's #1 trending style 2025"`、Mid-Century 的 `杏灰色`、Light Luxury 的"developer show flats(样板间)"框定。其余国际风格文案干净；新中式/港式/老上海/禅意/日式 等是设计上就该是东亚风，保留。
  - **公共脚手架（大，影响所有风格）**：`build_overseas_realism_layer()`（仅海外 cn_mode=False 用）只给"`{prop}` in 未指定地点"、**没有任何国际/西式住宅锚点**，模型就回落到训练里最常见的"国内样板间"先验 → 很多国际风格出图都偏国内。
- **联网搜索佐证**（用于配方）：浅暖橡=北欧/海岸/极简(白·灰米墙)；蜂蜜中深暖木=中古/美式/Maillard/英式(奶油·橄榄·藏青对比)；深胡桃=暗奢/静奢/中古/现代(奶白·灰米·黄铜)；冷灰木与水泥石=工业/意式极简/包豪斯(暖中性或同色灰)；奶油洞石=地中海/法式奶油/意式极简。**与项目已有的 `FLOOR_TONE_STYLE_RECOMMEND_MAP` 高度一致**。Sources：flooringstores.com、solidshape.com、stylebyemilyhenderson.com/hunker.com、jamesandjamesfurniture.com。
- **Part A — 配方按地板色重做（重写 `recipes.py`，`webui.py` 零改动，配方 dict 形状不变）**：
  - 旧的 5 条手攒通用配方 → 改成**复用 `FLOOR_TONE_STYLE_RECOMMEND_MAP`**：新增 `STYLE_LOOK_PROFILE`（27 个**国际/中性风格**的"拍摄·工艺画像"：光线/镜头/比例/板型/拼缝/光泽，取自搜索佐证）；`recommend_recipes(tone)` = 取该色调推荐分(✨✨>✨>🟡)最高且有画像的风格、按分排序取前 6。**故意不给 新中式/港式/老上海/禅意/(国内样板感的)轻奢 建画像** → 它们不作为自动配方出现（仍可手选），配方默认国际中性。全画像 `res=4K`。
  - 兼容：保留导出名 `FLOOR_RECIPES`、`pick_option_key`，故 webui 的 import 与 `_render_recipes/_apply_recipe` 全不动。
  - 自测：12 个 `FLOOR_TONES` 各调 `recommend_recipes` → 非空、随色调正确变化（浅暖橡→海岸/原木/奶油；深胡桃→电影侘寂/中古/美拉德/英式/静奢/暗奢；冷灰→工业/意式极简/包豪斯）、7 字段关键词对真实选项列表**0 未匹配**、东亚/国内风格 0 出现。
- **Part B — 中性化（改 `prompt_data.py`）**：
  - B1 清 3 处 + 顺手清 Mediterranean 的 `(微水泥/灰泥)` 双语注 + Light Luxury 的中文 UI 描述"样板间"→"酒店套房"。`磁吸轨道灯→recessed/minimal ceiling track lighting`；`吊顶 cove→dropped-ceiling cove`；`(杏灰色)`删；`China's #1…`删；Light Luxury atm→"boutique-hotel-suite glamour…"。**自测：英文氛围提示词区 CJK 残留 = 0**。
  - B2 海外真实感层加**国际中性锚点**（"render as an authentic internationally-styled Western/European/international home; avoid Chinese/East-Asian developer-apartment / sample-flat (样板间) / show-home styling…"），**对东亚风格自动让路**（Neo-Chinese/Hong Kong Retro/Shanghai Deco/Zen Tea Room/Japandi/Minimal Japanese/Wabi-Sabi → 改走"stay true to East-Asian"，不含 avoid 句）；并把别墅那条"garden/yard/deeper proportions/layered groupings"弱化成"honest, not exaggerated proportions; avoid sales-villa showpiece scale"。自测：中性风出锚点、新中式走让路分支、别墅 showpiece 已软化。
- **没动**：`webui.py`、生成链路 `_run_job/_generate_one_model/api.py`、`records.py`、`failure_kb.py`、灯箱/批量/磨缝、`themes.py`、`models.py`、其余 ~29 个风格英文文案、国内(cn_mode)路径的 layout/delivery/tier 文案。
- **自测/状态**：三文件 `py_compile` + `import floor_engine.webui` 通过；无头起服务、Edge(经 PowerShell `Start-Process`)截图确认"②智能配方"卡随色调变（深胡桃示例出 电影侘寂/中古/美拉德/英式/静奢/暗奢）。临时自检脚本/开关已删回干净。**真实出图保真与"海外不再像国内"由用户本机肉眼确认**（出图观感仍人工，遵 [[floor-ai-fidelity-manual]]）。

### v7 体验优化：地板图优先的引导式三步流（步骤条 / 智能配方 / 渐进展开 / 校验清单 / 队列汇总）

- **背景/需求**：Claude Design 出了新原型 `地板AI引擎-体验优化.dc.html`（在已上线「重排」版上的下一轮迭代）。目标——把"手填一长列下拉"改成**「① 上传地板图 → ② 一键套配方 → ③ 微调生成」**的引导流。**与用户确认了两点**：智能配方=我内置精选配方（用户后续自行改名/调内容）；渐进展开=未上传地板图前下游参数全部收起。
- **关键事实**：现版 `webui.py` 已实现设计稿约 70%（拆栏/吸底/工作流胶囊/模型分段/市场分段/更多参数/hero+色调识别/灯箱/批量/记录页/失败KB内联）。故本次是**增量改造、非重写**，surgical edits，**生成/队列/记录/磨缝/批量逻辑零改动**。
- **新增文件 `recipes.py`**（纯数据，无 UI/IO）：`FLOOR_RECIPES` 5 个精选配方（北欧原木亮/现代灰雾/侘寂暖白/工业复古/轻奢人字拼）；每个存的是**关键词束**而非写死整串选项值，UI 侧用 `pick_option_key(选项真键, 关键词)` 就近匹配（选项文案小改不失效，也不必复制 prompt_data 长串）。`recommend_recipes(tone)` 按识别色调排序（冷/灰调把冷系提前）。**约束**：全配方 `res='4K'`（2K 不达标）；配方**不覆写色调**（始终用 `analyze_floor_tone` 的识别值），style 只在当前色调的 `get_style_choices` 里挑最贴近项。自测：5 配方 × 7 字段关键词对真实选项列表**0 未匹配**。
- **`models.py`**：`JobRecord` 加 `favorite: bool=False`（仅内存态，供「⭐仅看收藏」过滤，不持久化）。
- **`webui.py`（全部 UI 层）**：
  - **CSS**：`design-style` 末尾加 `.step-bar/.step-dot--{done,active,idle}/.step-line` `.recipe-card{,--on}` `.chk-chip--{ok,idle,miss}` `.guide-zone` `.qsum-track/.qsum-fill/.qchip{,--on}`。版本号 `v6.0.1→v7`。
  - **渐进展开（gate）**：因左栏深嵌、`with` 缩进敏感，**不重排大块**——而是给已有元素加句柄（`_floor_lbl/_floor_change_row/_core_lbl/core_grid/_market_tabs/_market_panels/more_exp`）+ 新建 `recipe_wrap/floor_empty_guide`，用 `_set_left_gate(on)` 统一切 `.visible`。未传图只露引导卡（地板图优先）；`_use_floor_image` 末尾 `_set_left_gate(True)+_render_recipes(tone)`。🔑 API 设置不 gate（首次没 key 也能配）。
  - **顶部步骤条** `_render_steps()`：① 地板图 ② 配方·参数 ③ 生成，按 floor/必填上下文图 算 done/active/idle。
  - **② 智能配方块** `_render_recipes(tone)`→3 列卡片（按色调排序）；点卡 `_apply_recipe(r)` 用 `pick_option_key` 填 light/angle/aspect/res/floor_size/seam/gloss + 当前色调下的 style，更新「③ 场景参数」徽标。
  - **校验清单 chips**（吸底栏）`_render_checklist()`：地板图 / 按工作流出 房间原图·参照图 / 参数就绪；在上传/切流/套配方处刷新。
  - **③ 场景参数**：核心参数区改标题「③ 场景参数 · 配方已填好，点击可改」+ 配方徽标（2 列网格本就在）。
  - **队列汇总头** `_render_queue_summary()`（挂进 1s `_tick_job_times`）：有活跃任务才显示整体进度条 + ETA（近 12 张实测秒均值 × 剩余槽；无样本不显 ETA）。「⭐仅看收藏」`_toggle_fav_filter` 按 `JobRecord.favorite` 隐卡，卡头加 ⭐ 切换；空状态换成居中图标+引导文案。
- **没动**：`_run_job/_generate_one_model/_compute_final_status`、`api.py`、`records.py`（含 `result_image_file` 文件存储）、`failure_kb.py`、灯箱 JS、磨缝/重试/批量/二改/解密/追加/导出、主题 CSS `themes.py`。
- **自测**：`py_compile` 三文件通过；配方关键词全解析；**无头起服务 `/` HTTP 200、`main_page()` 服务端构建零异常**；Edge 无头截图（经 PowerShell `Start-Process` 才写得出文件，Git Bash 下不写）肉眼比对设计稿——**空状态**（步骤条①激活、工作流胶囊、地板图优先引导卡 + 下游参数正确隐藏、吸底校验 chips 灰态、模型分段、生成钮）与**上传态**（用临时 env 开关验证：步骤条✓✓③、5 张配方卡按暖调排序、③场景参数+未套用配方徽标、校验 chips 转绿）均与设计稿一致。临时开关与自检脚本已删回干净。UI 端到端（真实上传→识别→点配方填参→出图→队列进度/收藏过滤）待用户本地确认后提交（仓库无远程）。
- **注**：无头 curl 客户端断连时 NiceGUI 的 per-page `ui.timer` 会抛一次 `parent slot deleted`（属 NiceGUI 内部 teardown 竞态、非本次引入；真实浏览器长连不触发）。

## 2026-06-21

### 记录管理加「房间类型」筛选（选项+计数都按当前文件）

- **背景/需求**：用户在记录管理选中一个记录文件(=一个地板小样的 `{材料名}_记录.json`)后，右栏列出该文件下所有出图记录；想挑张好看的只能一条条翻。要按"生成时选的房间类型"筛。
- **范围的反复（最终定）**：一度按"下拉列全库房间类型+全库计数、只筛当前文件"做，用户看了说**计数错了**——"要显示当前记录下面的同类项，不是所有记录的"。遂改成**选项与计数都只来自当前选中文件**（选哪个文件，下拉就只列那个文件里有的房间类型、计数也是那个文件的）。
- **数据现成**：`room_type` 早就持久化(`prompts.py:825`，海外存 room_type/国内存 cn_room_type 都落记录级 `room_type`)，右栏卡片标题已在显示它(`webui.py` `{ts}·{wf}·{room}`)。故只加下拉+过滤，**不动记录 schema**。
- **`records.py`**：加纯函数 `room_type_counts(records)→{房间:计数}`(忽略空/缺失/strip空白，可单测)。（曾加的全库 `all_room_types()` 因改回按文件，已删。）
- **`webui.py`**（记录管理 tab）：① import `room_type_counts`；② `_rec_state` 加 `room_filter`；③ 右栏头部 `_cur_file_lbl` 与按钮组之间加 `_room_filter_sel` 下拉(默认 `🏠 全部房间`，初始隐藏)；④ `_refresh_room_options(recs=None)`：用**当前文件**记录(给定 recs 直接用、否则按 json_path 现读)算 `room_type_counts`→建选项 `房间 (n)`(降序、同计数按名)→当前选中类型若不在该文件则复位→有选项才显示；⑤ `_render_entries` 按 `room_filter` 过滤当前文件记录(与 ⭐只看收藏 AND 组合，fav 仍在 `_render_entry_card` 按结果级筛)，无匹配按 无文件/无此类型/空文件 给三档空态文案。
- **选项刷新时机**：选文件(`_select_file`，复用已读 recs)、🔄刷新、删整条记录(`_del_record`)、出图完成(`_run_job` 的 `_render_file_list` 旁，刷新当前文件计数)、tab 初始化(无选中文件→空→隐藏)。
- **刻意没做**：不补存 cn_mode/不区分国内外同名房间(按记录里那个标签分组够用)；空 room_type 老记录不进下拉但"全部"下仍可见。
- **自测**：`py_compile` + `import` + `pytest` **27/27**；`room_type_counts` 单测(计数/空值排除/strip/降序同分按名)通过；无头起服务 `/` HTTP 200、服务端零异常(tab 初始化无选中文件时下拉空且隐藏不报错)。UI 端到端(选文件→下拉只列该文件房间类型及该文件计数→选某类只剩该类、切文件下拉随之重建、与收藏叠加)待用户本地确认后本地提交(仓库无远程)。

### 重抽/再生成/查看重做：同卡内「候选累积 + ‹n/N› 左右切换对比」

- **背景/痛点**：用户嫌重抽体验差。查实根因——`_regen_job` 每抽一张都 `_new_job`+`_add_job_card`+`card.move(0)` **新建一张卡插到队列顶**，`×6` 就在顶部堆 6 张新卡把原卡挤下去，要对比得在一堆卡间上下滚。候选图其实都进了同一条 `record_id`（记录管理 tab 能看网格），但**队列里是散的**；且数据模型每槽只有单 `b2_path/pro_path/pro_polish_path`，一张卡天生只能显示一套图、没法左右翻。
- **用户拍板的两个设计点**：① 切换粒度＝**每个图槽各自翻**（B2/Pro/Pro磨缝 各有独立 `‹n/N›`，可混搭挑）；② 全屏放大**也加**左右箭头 + 键盘 ←/→ + Esc 关闭。
- **核心设计**：把"每槽一张图"升级为"每槽**候选列表 + 当前下标**"。不变量：`{slot}_path` 始终 = `{slot}_paths[{slot}_idx]`（slot∈`b2/pro/pro_polish`）。**`*_path` 保留为"当前选中"，所以所有现有下游（缩放/下载/磨缝/写记录/状态判定）零改动照常工作**。
- **`models.py`**：`JobRecord` 加 `b2_paths/pro_paths/pro_polish_paths`(List) + `b2_idx/pro_idx/pro_polish_idx`(int)；加 3 个纯函数 `add_candidate`(追加+跳最新+同步*_path) / `nav_candidate`(移动下标钳边界+同步) / `ensure_candidate_lists`(老任务只有*_path→回填单元素列表，幂等)。持久化无需改 `records._JOB_FIELDS`（自动派生，list/int 走 JSON）。
- **`webui.py`**：
  - 生成出口**唯一改一处**——`_generate_one_model` 的 `setattr(*_path)` → `add_candidate(job, stage_key, path)`（主生图/重试/重抽共用，全覆盖）；磨缝出口同改 `add_candidate(job,'pro_polish',ppath)`。主流程后续 `_update_job(b2_path=b2j)` 只是把 *_path 设回同值，不重复 append。
  - **重抽改为同卡追加**：重写 `_regen_job(src_job)`——不再建卡，而是在**源卡上重跑全部模型并追加候选**（仿 retry 生成段，但跑全部模型、运行中保留旧候选可见、跑完只要有候选就 done/partial；本次部分失败但有历史候选→只 notify 不挂 error）。`_regen_job_n` 串行多抽驱动/停止药丸/进度几乎不动（本就在源卡上跑）。
  - **卡片图槽加 ‹n/N›**：`_img_box`/Pro 内联槽加 `_nav_bar`（绝对定位浮于图底、不挤图高），`_apply_images` 末尾对三槽调新 `_apply_slot_nav`（>1 张才显示、两端禁用箭头），新增 `_nav_slot`(切换→同步→刷新)。配套 `.cand-nav/.cand-arrow/.cand-cnt` CSS。
  - **全屏放大候选切换**：`floorZoomShow(urls, startIdx)` 改收数组、加 `_floorZoomAt/floorZoomNav` + 左右箭头/计数浮层 + 键盘 ←/→（`offsetParent` 判弹窗可见才拦截）；Esc 走 Quasar 原生 dismiss。新增 `_open_zoom_list`，队列卡三处点击改传整槽候选列表（`[_to_url(p) for p in j.b2_paths], j.b2_idx`）；`_open_zoom` 保留为单图薄包装（记录 tab 调用方不变）。
- **`records.py`**：`load_persisted_jobs` 重建每个 job 后调 `ensure_candidate_lists`——老持久化只有 `b2_path` → 回填成单元素候选列表（单张无 nav），重启后老卡正常、之后重抽即累积。
- **自测**：`py_compile` 三文件 OK；`pytest` **27/27**；候选 helper 单测（追加/跳最新/左右钳边界/向后兼容/asdict round-trip）通过；无头起服务 `/` HTTP 200、注入的 `floorZoomImg/floorZoomPrev/floorZoomNav/cand-nav` 标记齐全、**启动恢复 8 个历史任务走新卡片/导航/ensure 代码路径零异常**。UI 端到端（出图→重抽×N 不再新增卡、候选累积原卡、‹n/N› 左右翻、全屏方向键翻、停止多抽、刷新恢复后 nav 仍在、记录 tab 不受影响）待用户本地确认后本地提交（仓库无远程）。

---

## 2026-06-18

### 生图线路下拉「即选即存」——修「选了 Google 却仍走 Fal」的困惑

- **背景/排查**：用户反馈"明明选了 Google 直连，日志却还在走 Fal 中转"。查后**不是路由 bug**：调度器 `api.call_image_generate`（api.py:562）每次出图都现读 `engine_config.json`（`_load_config()` 无缓存），严格按 `image_provider` 走线（`google`→`[API生成]`、`fal`→`[Fal生成]`）。今天日志与用户多次来回切换的时间线完全吻合；那批走 Fal 的任务在 15:54:16 发起请求，而用户把线路存回 `google` 是 15:54:44（晚约 28s）——任务在派发那刻已读一次配置锁定 Fal，中途改配置不回切。
- **真正的坑**：`生图线路` 下拉（`provider_sel`，webui.py:597）选中后**不会自动保存**，必须点 `💾 保存`（→`_save_api_settings`）才写盘；下拉本身没绑 on-change。用户很可能下拉点了 Google 但没点保存就去提批量，盘上仍是 `fal`。
- **改了什么**（`webui.py`，API 设置区 `_save_api_settings` 之后，约 +12 行）：给 `生图线路`/`出图重试策略`/`自动转 Fal` 三个离散控件各加 `.on_value_change(...)`，选中即调 `save_provider_settings(None, …)`/`save_speed_profile`/`save_auto_failover` 写盘 + 轻提示。`provider` 自动存时 fal_key 传 `None`（按 config.py:143 语义只改线路，不动文本框里可能半填的 Fal Key）。沿用仓库惯用写法 `.on_value_change`（同 488/540/742/1310 行）。
- **保留**：`💾 保存` 按钮——Gemini Key/Fal Key/本地代理 三个文本框仍只在点保存时写盘（不宜每敲一键就存）；按钮顺带再存一次三个开关，无害。
- **没动**：api.py / config.py / 调度逻辑——路由本身没问题。对**已派发/在跑**的任务也不会回切线路（设计如此），改动只保证"下次提交"用对线路。
- **自测**：`py_compile` + `ast.parse` 通过。UI 端到端（切线路即弹"已保存"、旁验 `engine_config.json` 的 `image_provider` 即时变化、再提一单看日志 `[API生成]`/`[Fal生成]`）由用户本地确认后提交。

### ⬛ 会话：风险加固（裁剪 Codex 方案后实施）

**背景**：评估 Codex 给的风险加固方案。结论是「通用 Web 加固」对本程序（本机自用、未来可能打包给同事、走公司软路由透明代理）校准不足，只取真有价值的，并把最危险的 TLS 项改成「先可配置、实测后再翻默认」。用户拍板两点：TLS 先可配置+自检实测、默认不变；记录里的 prompt 明文**完全不动**（本机自用不在乎）。

实施（均已测，仓库无远程未提交）：
- **job_id 唯一化（真 bug）** `models.py:new_job`：`f"job_{int(time.time()*1000)}"` → `f"job_{uuid.uuid4().hex}"`。原毫秒 ID 同毫秒创建会撞，而 job_id 是 `_job_ui` 的 key，撞 id 会串掉彼此卡片按钮。排序仍用 `ts`。
- **用量记真实线路** `api.call_image_generate` 改返回 `(img, err, provider)`，provider∈{'google','fal'} 是**实际**出图/尝试线路（自动转 Fal→'fal'，失败归主线 google）；`webui._generate_one_model` 解包并把 `record_usage` 的线路从 `get_image_provider()`(猜) 换成真实 `provider`。修掉代码自己注释承认的「自动转线后统计记错」。
- **默认只监听 127.0.0.1** `__main__.py`：`ui.run(host=os.environ.get('FLOOR_AI_HOST','127.0.0.1'), ...)`。原来不传 host→NiceGUI 默认 0.0.0.0=局域网任何人可开。将来共用才显式设 `FLOOR_AI_HOST=0.0.0.0`（且需自配访问控制）。自检：ready 行只剩 127.0.0.1。
- **上传安全化** 新增 `config.safe_upload_path(orig_name, prefix='')`：basename 去目录(防 `../`/`C:\` 逃逸) + 扩展名白名单(png/jpg/jpeg/webp/bmp，非图返回 None) + 危险字符清洗 + **同名自动加序号防覆盖** + commonpath 兜底校验在 UPLOAD_DIR 内。`webui` 三处上传(`on_floor_up`/`on_room_up`/`on_ref_up`)改用它（顺带修了「同名静默覆盖」这个实际 bug）。
- **TLS 校验可配置（默认不变）** `config.get_tls_verify()`/`get_tls_ca_bundle()` + `api._verify_arg(cfg)`（tls_verify=false→False；否则有存在的 tls_ca_bundle→该路径；否则→True）。把 api.py 6 处 `verify=False` 换成 `verify=_verify_arg(cfg)`。**默认 tls_verify=false 保持现状**，避免一翻默认就（在 MITM 软路由上）断网。`test_connection` 加「证书校验」探针：强制 verify=True(+CA) 试一发，报「✅可开启 / ❌不通过(需装CA) / ⚠️无法判定」，确认能过再让用户把 tls_verify 设 true。
- **`webui._to_url` 收紧** 子串 `if UPLOAD_DIR in p` → realpath+commonpath 归属判断；越界路径(如记录里塞 `../engine_config.json`)返回 ''，不再能转成可服务 URL。
- **明确没做**：不加密 prompt_en（用户不在乎；现有 _pe+口令 reveal 已够）；不收紧配置文件权限（Windows ACL 折腾、单用户边际）；不改 API key 掩码（password=True 已遮挡）。

**测试**：`pytest floor_engine/tests` **27/27**（原 5 + `test_hardening.py`）。无头起服务 `/` HTTP 200 且只绑 127.0.0.1；`_to_url` 正常路径/越界路径手验通过。UI 端到端（上传各类图、放大、出图后用量统计、连通性自检）由用户本地确认。

**追加（同会话，用户跑完自检后）**：自检结果 = 证书校验 ✅ 可开启、Google 直连 ❌ Key 无效(HTTP 400)。据此：
- **TLS 默认翻 True**：`config.get_tls_verify` / `api._verify_arg` 的默认从 False 改为 **True**（本网络实测证书校验能过；保护用户自费 Fal key）。坏网络可设 `tls_verify=false` 退回。
- **`failure_kb` 加 `tls_cert` 规则**（排在 network 兜底前）：证书错(`CERTIFICATE_VERIFY_FAILED` 等)不再被误判成「网络波动·重试」，直接提示「设 tls_verify=false 或配 CA」。
- **`test_connection` 改进**：① Google 直连不再把 400 一律算「Key 无效」——读真实报错体区分 **geo-block(落地地区不支持) / Key 无效 / 其它**；② 主请求加 SSLError 分支；③ 证书探针改成「证书校验状态」行（默认已开启→✅；被关→探本网络能否开回）。
- **关于用户当前的 ❌(HTTP 400)**：高度怀疑是今早那个 geo-block 复发(海外出口掉了)而非真 Key 问题——旧版自检会把它误标成「Key 无效」，本次改进后重跑自检即可看到真实原因。

**背景**：用户反馈结果图全屏放大有两个问题——① 没有滚轮缩放；② 放大后图片被按默认比例（约 16:9）显示，而近期出的都是 4:3，比例错了。

- **根因**：放大弹窗用的是 `ui.image`（Quasar `q-img`），q-img 用 padding 比例盒呈现，会把图按默认/容器比例撑开 → 非 16:9 的图（4:3、竖图）比例失真；且 q-img 本身不带任何缩放交互。
- **改了什么**（`webui.py`，仅放大弹窗一处）：
  - 把 `_zoom_img = ui.image('')` 换成**原生 `<img id="floorZoomImg">`**（经 `ui.html` 注入），`max-width:96vw; max-height:96vh; object-fit:contain` → 永远按图片自身比例显示，4:3 就是 4:3。外层 `_zoom_stage` 仍是点背景关闭（img 上 `onclick=event.stopPropagation()` 防误关）。
  - 用 `ui.add_body_html` 注入一段 JS（`window.floorZoomShow`）做交互：**滚轮以光标为中心缩放 1~8x**、放大后**按住拖动平移**、**双击复位**。缩放用 CSS `transform: translate() scale()`（origin center），以光标为锚点的位移按 `t += off*(1-k)` 推导，回到 1x 自动归位。监听 lazy 绑定（首次 show 时挂、`_wired` 去重）。
  - `_open_zoom(url)` 改为 `_zoom_dlg.open()` + `ui.run_javascript('floorZoomShow(<json url>)')`（`json.dumps` 转义，兼容 `/outputs/...` URL 与 records 的 `data:` base64）。
- **未动**：队列卡/记录页的缩略图仍是 `ui.image`（只改放大视图）；所有 `_open_zoom(...)` 调用点签名不变。
- **状态**：语法/导入自测通过；无头起服务 `/` HTTP 200、served HTML 内含注入的 `<script>` 与 `<img id=floorZoomImg>`、服务端无异常。滚轮缩放/拖动/双击复位的实际手感由用户本地点一下确认。仓库无远程，未提交。

### ⬛ 会话：Gemini 地区封锁（User location is not supported）→ 失败卡分类 + 纳入自动转 Fal

**背景**：生产日志连续出现 `HTTP 400: User location is not supported for the API use.`（B2 与 Pro 同时失败）。排查确认是 Google Gemini 的**地区封锁**——请求已到达 Google 被干净回绝（返回规整 JSON 错误，非透明代理重置、非 Key 问题），根因是出口线路的境外落地节点掉了/换了、4K 生图请求从大陆 IP 出去被 Google 按地区拦下。

- **`failure_kb.py`**：新增 `geo_block` 规则（排在 `bad_input_image` 之后、`overload`/`network` 之前），匹配子串 `User location is not supported` / `location is not supported` / `not supported for the API use`。此前这条 400 落到 `_UNKNOWN`、失败卡显示「❔ 未知错误」（误导成疑难杂症）；现在显示「🌍 线路落地地区不支持」并给「换海外节点 / 切 Fal」的处理建议。文件头「日志归类」注释同步补了这一类。
- **`api.py` `_is_network_class_error`**：把含 `location is not supported` 的错误纳入「可转线」。原逻辑只认 429/5xx + 网络关键词，HTTP 400 一律当内容/请求级**不转**；但这条 400 本质是**线路落地问题**，Fal 不走这套地区封锁、转线常能直接出图，故破例放行。效果：开了「直连失败自动转 Fal」+ 配了 Fal Key 时，遇地区封锁会自动走 Fal 备线出图（走用户自己的 Fal 额度）。docstring 已补该例外说明。
- **未动其它**：400 仍不重试（同地区重发无用）；`test_connection` 暂未改（它仍把 400 笼统报「Key 无效/无权限」，地区封锁场景会有点误导，如需可后续单列）。
- **状态**：纯字符串匹配/分类逻辑改动，`classify_failure` 手验该串命中 `geo_block`；UI 失败卡展示由用户本地确认。仓库无远程，未提交。

### ⬛ 会话：webui.py 轻量整理（同日早些时候）

**背景**：用户问 `webui.py`（1975 行）是不是快成屎山、要不要再拆。核查后结论：跨模块分层是健康的（api/records/prompts/config/models/failure_kb 委托清晰），真正的债在 webui **内部**——`main_page()` 一个函数吃了约 1740 行、`_run_job`(205行)/`_refresh_job_card`(88行) 巨型、job 的 UI 引用散在 7 个并行 dict。评估「拆成 `webui/` 包 + PageCtx」的完整方案后判定对内部单机工具+无 UI 测试网而言用力过猛，用户拍板**只做轻量版**（不拆多文件、不动 NiceGUI 闭包架构）。本轮做了三类低风险整理。

### A. 纯逻辑下沉到对应后端模块（零行为风险）
- **config.py**：新增 `UPLOAD_DIR`/`THUMB_DIR`（原在 webui 顶部；BASE_DIR 已 frozen-aware，与旧 `sys.frozen` 写法等价）。webui/records 都 import 用。
- **models.py**：新增 `new_job`(JobRecord 工厂)/`update_job`/`compute_final_status`/`job_time_text`（均纯函数，无 UI/IO）。
- **records.py**：新增 `persist_jobs(jobs)`/`load_persisted_jobs()`(+`QUEUE_STATE_FILE`/`QUEUE_PERSIST_MAX`/`_JOB_FIELDS`) 与 `_list_recent_floor_swatches`（纯文件 IO；records 现 import `.models.JobRecord`，无环）。
- **webui.py**：`_persist_jobs()` 改薄包装（加锁取 `_job_history` 快照 → 交 records）；`_new_job` 仍持有 `_job_history.insert`（所有权不动）；`_update_job` 留同名别名指向 `models.update_job`；`compute_final_status`/`job_time_text` 调用点改用 models 版。顺手删掉变无用的 `import sys`/`dataclasses`/`json as _json_mod`。**后端 5 模块 grep `nicegui` 仍零命中**。

### B. 原地拆解两个巨型函数（不搬出 main_page，仍是嵌套闭包）
- **`_refresh_job_card`(88行) → `_STATUS_REFRESH` + 5 个 `_apply_*`(status_badge/action_buttons/border_and_time/error/images) + 瘦身后的派发器**。各 `_apply_*` 只读模块级全局 + 入参 `(refs, job)`、**不捕获 main_page 局部**，便于阅读/将来外迁。`_refresh_batch_ui` 改为复用 `_apply_action_buttons`（消重；批次中 status 恒 done/partial，stop/retry 同值再设无副作用）。
- **`_run_job`(205行) → 抽出前两段** `_validate_and_submit`(校验/建卡，返回 (job,api_key))、`_maybe_analyze_style`(参照模式风格分析，返回 (text,ok)，失败就地中止)。**生成核心未再拆**：它局部状态(cpt/ims/ar/rp/bevel_ref…)耦合强，且无 API Key 无法离线验证，硬拆属高风险低回报，刻意保留线性内联（已加注释说明）。

### C. 合并 7 个分散 job dict → 一个 `_job_ui[job_id]`
- `_job_ui_refs`+6 个按钮/下拉 dict（`_job_stop_btns`/`_job_retry_btns`/`_job_regen_btns`/`_job_regenN_sels`/`_job_regen_pods`/`_job_batch_btns`）合并成单个 `_job_ui[jid]={...}`（键：原 refs + `stop_btn`/`retry_btn`/`regen_btn`/`regenN_sel`/`regen_pod`/`batch_btn`）。`_add_job_card` 不再分散 6 次写、末尾一次性建；`_clear_done` 的逐 dict pop 改单次 pop。**`_regen_batches` 保留独立**（是批次运行态、非 UI 引用、生命周期不同，合并进去得不偿失）。

### 结果与验证
- `webui.py` 1975 → **1888 行**，仍单文件；旧名残存 grep 零命中。
- 自动验证全绿：`import floor_engine.{config,models,records,webui}` OK（启动日志正常恢复 3 个历史任务）；`pytest floor_engine/tests` **5/5**；后端零 nicegui 依赖；无头起服务 `/` **HTTP 200**、159175 bytes（与改前逐字节一致）、DOM 标记在、**服务端无任何异常**（复元循环对 3 个历史 job 实跑了新 `_add_job_card`+分割版 `_refresh_job_card`）。
- **未提交**：UI 端到端（出图/磨缝/重试/再来一张/多抽×N+停止/参照模式分析失败中止/国内 tab/记录管理）需用户本地确认后再本地提交（仓库无远程）。重点验 C：失败卡 ⓘ、停止单个/全部、多抽进度药丸、出图后刷新浏览器卡片恢复——确认 7-dict 合并后按钮显隐未错位。

---

## 2026-06-17

**背景**：用户让我"再看一下系统还缺什么"。用 3 个探查 agent 全仓扫 + 核验后，排除一批假缺口
（密钥不会泄漏——`engine_config.json` 在 git 根的上级目录够不到；队列持久化其实做得不错；Fal 路由已可用；
多用户/SaaS 不适用于内部单机工具），确认真实缺口后，本轮做了 3 件（成本可见性按用户意见改成"计数"不估金额）。

### 1. P0：提示词黄金回归测试（纯本地，零 API 花费）
- **为什么**：`prompts.py`（4 套工作流的 prompt 组装）是核心壁垒，此前改它只能人眼对照，无回归网（DEVGUIDE 自列头号待补）。
- **新建 `tests/`**：`conftest.py`（把 `records.MAIN_OUTPUT_DIR` 重定向到 pytest 临时目录 → 不污染真实 `output_files/`；
  强制 `prompt_data.TRANSLATOR_AVAILABLE=False` → 翻译走离线 FALLBACK、确定且不联网；生成极小纯色 fixture 图当 image_path），
  `test_prompts_golden.py`（4 套工作流各跑一次 `save_task_files_html()`，把返回的 combined/Pro 两段 prompt 与
  `tests/golden/*.txt` 基准做字符串相等比对；另有一个"同参数连跑两次必须逐字相同"的确定性测试），
  `tests/golden/`（8 份基准文本，已入库）。
- **用法**：`python -m pytest floor_engine/tests/ -q`；有意改 prompt 后 `UPDATE_GOLDEN=1 pytest` 刷新基准并人工核对 diff。
- **杂项**：`requirements.txt` 放开 `pytest>=7.0.0`（仅开发/测试，不进生产依赖）；`.gitignore` 加 `.pytest_cache/`；`DEVGUIDE.md` 3.2/3.3 更新测试基建状态。
- **自测**：5/5 通过；确认不联网、不写真实 `output_files/`；从仓库内或上级目录跑都 OK（conftest 用绝对路径塞 sys.path）。

### 2. P1：磨缝/二改健壮性对齐主生图
- **核实修正**：`call_gemini_edit` 的【重试次数其实已对齐】主生图（早就走 `_retry_plan()`，非旧"3 次"）。真正缺的是主链路有、它没有的：
  ① 无实时状态回调 → 重试时 UI 像卡死；② 无取消 → 用户停止后仍发计费请求。
- **`api.py`**：`call_gemini_edit` 加 `on_stage=None, should_cancel=None`（与 `call_gemini_generate` 同契约、各自吞异常）；
  重试循环里每轮起始查 `should_cancel()` 命中即停（不再白计费）、并 `on_stage("📡连接中/🔁网络重试 N/M")`。
- **`webui.py`**：磨缝(`_polish_pro`)接 `on_stage`→写 `job.pro_stage`(由 1 秒 timer 显示) + `should_cancel`→`_is_cancelled(job.job_id)`，结束清 `pro_stage`；
  二改(`_do_edit`)接 `on_stage`→经对话框内 0.5 秒 timer 把"网络重试 N/M"写进 `_edit_status`（worker 线程只碰普通 dict，不跨线程碰 UI 元素）。
- **未做(列二期)**：编辑也走 Fal 自动转线（需仿 `call_image_generate` 包一层 `call_image_edit`，较重）。

### 3. P2：用量统计（按模式/线路计数，不估金额）
- **为什么**：用户区分 Fal=自己的钱/Google=公司的钱，但 UI 看不到出了多少、失败多少。按用户意见**只计数不估价**（计价多变不好控）。
- **`records.py`**：加 `record_usage(mode, model, provider, ok)` / `load_usage_summary()`，落盘 `usage_stats.json`（原子写 + 锁 + 全程吞异常，绝不拖垮生图）；
  维度 = 模式 × 模型(B2/Pro) × 线路(google/fal)，各计 成功/失败。
- **`models.py`**：`JobRecord` 加 `workflow_mode` 字段（建 job 时写入，重试/再生成沿用同一 job → 统计自动归类）。
- **`webui.py`**：埋点放在主+重试共用的逐模型出口 `_generate_one_model`（出图=ok、没出图且非取消=fail；线路用 `get_image_provider()` best-effort）；
  记录管理 tab 顶部加可折叠「📊 用量统计」小表 + 刷新按钮。
- **已知误差**：自动转线后真实线路可能与配置不同（best-effort 按配置线路记）；精确回传 provider 列二期。
- **自测**：record_usage/load_usage_summary 冒烟（计数、短标签、totals 正确）；webui 导入 OK；pytest 仍 5/5。

### 其它
- 仍未碰用户明确缓办的：日志轮转（当前才 ~491KB，远未到 ~20MB 提醒线）、记录页分页、地板保真自动校验。
- 改动均**未提交**，待用户本地端到端确认（起服务跑磨缝/二改看重试状态、生成几张看统计表）后再本地提交（仓库无远程）。
- 注：`failure_kb.py` 与上次的 `开发日志.md` 改动在工作区仍未入库（历史遗留），本次未代为提交。

---

## 2026-06-16

### 1. 失败原因知识库 + 失败卡片改专属配色 info 图标 + 顶部标签右移
- **背景/需求**：① 生成失败时卡片只显示红字 `err_lbl`，且 `job.error[:120]` 被卡片宽度截断看不全；② 用户想要"下次同样失败时，提示里不仅有回传报错，还有提前写好的原因+处理建议"，省去每次翻 `app_local_save.log`；③ 顺带把顶部两个标签页推到最右。
- **新建 `failure_kb.py`（知识库 + 分类器）**：翻 `app_local_save.log`（2517 行 / 174 ERROR / 403 WARNING）+ 对照 `api.py` 报错路径，把真实失败归成一张**有序规则表** `FAILURE_RULES`（特例在前、网络兜底在后）+ `classify_failure(err)→{title,cause,action}`。覆盖：网络/线路重置(软路由对 4K 长连接重置，最多)、服务端过载(503/429)、额度用尽(spending cap)、输入图无法处理(400)、未配 Fal Key、缺地板素材图、安全拦截、双线路都失败、未返回图、已取消/重启中断、未知兜底。**以后新增报错类型只改这一个文件**。自测 18/18 样本分类正确。
- **`webui.py`**：
  - 失败卡片（`_add_job_card`）：红字 `err_lbl` → `err_row`＝**暖橙 ⓘ 图标(`#E0852E`，刻意区别于 API 设置里那批灰色 `var(--text-secondary)` ℹ️)** + 短原因标题(`#B5713A`)。点图标弹 `_fail_dlg` 小窗，分区显示【📋原因+🛠建议】与【完整原始报错】(限高可滚动、可选中复制、不截断)。`_refresh_job_card` 改为 `classify_failure(job.error)['title']` 当短标题。
  - 工作台 tab 顶部建一次性 `_fail_dlg`/`_open_fail_detail` 与 `_fail_summary_dlg`(遍历 `FAILURE_RULES` 列全套)，仿 `_open_zoom` 模式。
  - 左下角"● 线路正常"旁加暖橙 ⓘ → 打开 `_fail_summary_dlg`（"常见生成失败原因汇总"，即用户要的"大汇总分析"，也给原本想要的网络说明一个家）。
  - 顶部 tabs（line 322）`classes('flex-grow')`→`+justify-end`，两个标签贴右。
- **作用域/复用**：`_open_fail_detail`/`_fail_summary_dlg` 与卡片代码同属 `main_page()`，lambda 点击时解析、可达（同 `_open_zoom` 跨 tab 复用）；重启恢复的失败卡片经 `_refresh_job_card`(line 1045) 也带新图标。
- **没动**：api.py / records.py / models.py。
- **自测**：语法/导入通过；备用端口 7899 无头起服务 → `/` HTTP 200、140KB、新弹窗 DOM 标记齐全、无服务端报错。UI 端到端(制造各类失败看分类/弹窗、点汇总、标签右移)由用户本地确认后提交。

### 2. 队列卡片结果图加点击全屏放大（复用 `_open_zoom`）
- **需求**：之前只有"记录管理"tab 的结果图能点击放大；用户想在**渲染队列卡片**里直接点生成图就全屏放大，不用切到记录里找。
- **改了什么**（`webui.py`，全部在 `_add_job_card` 内，约 5 行）：
  - 给 B2 / Pro / Pro磨缝 三张结果图样式补 `cursor:zoom-in;`（`_img_box` 的 `_im` + Pro 内联图）。
  - 三张图各绑一次点击 → `_open_zoom(_to_url(j.<path>))`，带空值保护（无图时点击不动作）。
- **为什么绑在创建处而非 `_refresh_job_card`**：后者每次状态刷新都跑，绑那里会**重复叠加** handler；创建处只跑一次。创建时图还没 URL，所以 handler 在**点击时**才从 `job` 读当前路径（`job` 原地被 `_update_job` 改，点击拿到的是最新图；重抽/再来一张是新卡各自捕获自己的 job）。
- **复用点**：`_open_zoom`（记录管理 tab，maximized 深黑底、点任意处关闭）与队列卡片同属 `main_page()` 一个函数作用域，lambda 调用时才解析名字、点击时整页已建好，可达。`_to_url` 是模块级函数。
- **没动**：`_refresh_job_card`、`_open_zoom`、`models.py`、records.py 均未改；无新路由、无新依赖。结果图在 `/outputs`（优化过的 95 质 JPEG），放大直接用 `/outputs/` URL，不需要缩略图。
- **状态**：语法/导入自测通过；UI 端到端（出图后点击放大、磨缝图放大、重启恢复的卡片也能放大）由用户本地确认后再提交。

### 3. 历史小样/地板预览加载慢 → 加 `/thumb` 懒缩略图路由
- **问题**：历史小样弹窗（`_open_history_dialog`）打开很慢。根因——弹窗每个 120×90 格子用 `ui.image('/uploads/<原名>')`，而 `/uploads` 是 `app.add_static_files` 挂的**原图静态目录**，浏览器下载的是全尺寸原图、只靠 CSS 缩。实测 `_ng_uploads` 28 张共 **645MB**，单张最大 **53MB / 8192×5464（8K）**，一开弹窗就拉几百 MB + 浏览器解码 28 张 8K 图。
- **改了什么**（`webui.py`）：
  - 顶部加 `from fastapi.responses import FileResponse, Response`。
  - `add_static_files` 之后加缓存目录 `THUMB_DIR = <_ng_uploads 同级>/_ng_thumbs` + **同步** `@app.get('/thumb/uploads/{name}')` 路由：懒生成 JPEG 缩略图并缓存，缓存名带源图 `mtime`（同名重传自动失效），原子写 `os.replace`，失败回退原图永不白屏。性能核心是 **`im.draft('RGB',(s,s))`**——让 8K JPEG 在解码阶段就降采样，单张 ~0.3–0.45s（实测 51MB→8KB）。
  - 助手 `_thumb_url(p, s=320)` → `/thumb/uploads/<name>?s=<s>`。
  - 三处"原图当小图"的预览改用缩略图：历史小样格子（`?s=320`，补 `loading=lazy`）、已选地板英雄卡片 88×66（`?s=320`）、参照图全宽预览（`?s=960`）。
- **没动**：**只改预览展示**。生图链路仍读 `floor_path['v']`/`ref_path['v']` 的原图绝对路径，全屏放大走 base64/`/outputs`——**4K 出图保真完全不受影响**。
- **效果**：单格响应从几十 MB 降到几~几十 KB；首次开弹窗为 28 张各生成一次（线程池并行，配 draft 很快），之后命中磁盘缓存秒开。
- **杂项**：`.gitignore` 加 `_ng_thumbs/`（缓存可随时重建，不入库）。
- **状态**：代码改完并跑过语法/导入/缩略图生成自测；UI 端到端由用户本地确认后再提交（仓库无远程）。

### 4. 圆弧倒角默认参考图换成米白板特写
- **改了什么**：`assets/bevel_ref_clean_a.jpg` 换成一张米白橡木板边斜拍特写（用户提供的新图）。
- **为什么**：旧 clean_a 凹槽偏深、易被模型渲成黑线；新图凹槽清晰可见但不发黑、暖调软细缝，更贴合"无缝底 + 圆边过渡、近无缝以高光为主"的方向。
- **机制回顾**：选「圆弧倒角 (Pressed Bevel)」且非无缝时，`config.get_bevel_ref_image()` 自动把这张图喂给生图模型（B2/Pro 都带），模型**只抄板边圆弧凹槽形状**，颜色木纹仍取地板小样。可在 `engine_config.json` 的 `bevel_ref_image` 字段换成任意自定义路径。
- **旧图备份**：原 clean_a → `assets/bevel_ref_clean_a.jpg.bak`（已被 `.gitignore` 忽略，仅本地保留，想回退就 `cp` 覆盖回去）。
- **题外**：用户另给的两张整间房效果图（远景、非倒角特写，不适合当参考图槽位）已归档到**上级目录** `../参考样图/`：`浅橡木_海景厨房.jpg`、`灰橡木_看书客厅.webp`、`米白橡木_倒角特写_clean_a源图.jpg`（最后一张就是本次 clean_a 的源图）。
- **提交**：`9842882 feat(圆弧倒角): 默认参考图换成米白板特写 clean_a`

### 5. 渲染队列改为新任务置顶（newest-first）
- **改了什么**（`webui.py`，共 2 行）：
  - `_add_job_card()` 卡片构建完成处加 `card.move(target_index=0)`，让新卡真正插到队列**顶部**（此前 `with queue_container:` 默认追加到**底部**，与该函数 docstring 及数据层 `_job_history` 的 newest-first 不一致）。
  - 启动恢复历史卡片的循环（约 1001 行）改为 `for _existing_job in reversed(list(_job_history))` —— 否则"正序遍历 + 逐张置顶"会被**二次反转**成"旧在上"。
- **为什么**：用户嫌新任务出现在底部、要从下往上找，不便审阅；改成最新的排最上面。
- **未改**：数据层 `_job_history` 本就是 newest-first（`_new_job` 用 `insert(0, job)`），不动。
- **环境注**：NiceGUI **3.12.1**，`Element.move(target_index=...)` 受支持。
- **提交**：`dc2bad6 feat(队列): 渲染队列改为新任务置顶 (newest-first)`

### 其它
- `.gitignore` 新增 `*.bak` 规则。
- 改动均经用户本地测试通过后提交；**仓库未配远程，全部为本地提交（无 push）**。
- API key 在上级 `../engine_config.json`（含 gemini/fal key），**不在本仓库范围内**，未被提交。
